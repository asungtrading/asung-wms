# 2026-08-04 SO-14129 — 팩 무단완료 (조사 중)

## 확정 (실측)
- 팩 완료 쓰기 경로는 packer.html doneBtn 단 하나. 자동 판정·타 화면·EF 경로 없음
- 애플리케이션 트리거 0건 (pg_trigger 실측 — 6건 전부 Supabase 내장)
- cron 잡 2건뿐: wms-poll-orders(*/5), wms-reap-stale-claims(*/2). 둘 다 완료 못 만듦
- short_after_pack insert 도 packer.html 한 곳. 배열 1회 insert → 밀리초 동일은 정상 지문
- verified 합 0 완료를 막는 가드 없음. 미검수와 부족을 구분하지 않고 전 라인 short
- doneBtn 에 lines.length>0 가드 없음
- 완료 UPDATE 는 .eq("id") 만 — CAS 없음. ensureMine() 은 네트워크 오류 시 통과(best-effort)
- responsible = enterPack 시점에 읽은 wms_pick_tasks.assigned_to (단일 행, 라인 조인 아님)
- wms_pack_tasks 에 completed_by 없음
- 롤백은 pack_task_lines/pack_tasks 를 delete → 증거 소멸
- 롤백이 wms_discrepancies 를 건드리지 않음. 그리고 discrepancies 에 pack_task_id 없어
  "그 단계의 산물" 식별 불가 → 링크 컬럼이 선행 조건
- reaper 는 무작업 클레임을 pending 으로 풀지만 행을 남김 → 팩 큐에 "Resume verify"
  카드로 재등장 (verified=0)
- ← List 만 무작업 릴리즈. ☰ Menu·탭 닫기·뒤로가기는 릴리즈 안 함

## 반증됨
- 자동완료("0 vs 0" 판정) 가설 — 호출 지점이 코드에 없음
- 로드 전 창 가설 — startPack/resumePack 은 라인 생성 후 화면 전환
- pg_cron reaper 원인설 · 픽 자동완료설

## 미확정
- H1 스캐너 말미 Enter 가 native confirm 의 기본 버튼(OK)을 승인 — 유력, 재현 미실시
- H2 공유 태블릿 제3자 (Joon Kwon 로그인 세션) — 배제 불가
- 귀속 엇갈림(Jeff 배치 → Joon 귀속) 후보 3개:
  (A) 픽 단계 이어받기 (B) enterPack enrich 레이스 (C) resumePack 이 reaper 가 풀어둔
  pending 을 잡음 ← 가장 단순
- 팩 진행 중 포커스 위치 / 모달 닫힌 뒤 포커스 / doneBtn 이 포커스를 받는 순간 — 미조사
- wms_orders.status='ready_to_close' 쓰기 경로 — 전수 미조사

## 내일
1. 재현: 테스트 오더로 스캔 0 상태에서 Complete pack → confirm 떠 있는 동안 스캔
   (⚠️ 실오더 금지 — 롤백이 증거를 지운다)
2. Joon Kwon 질문: 영어 팝업을 봤는지 / "Resume verify" 카드를 탭했는지
3. 창고별 스캐너 접미(CR) 설정 비교 — 에드먼턴 편중의 진짜 상관 후보
4. SQL: 배치 2 픽태스크 assigned_to / 같은 서명 전수 조회 / 59건 responsible=null 정리
5. 수정 1단: DOM 모달 교체(+Enter 삼키기·autofocus 금지) · verified 0 완료 차단
   (전 라인 stock_short 선언은 예외) · lines.length 가드
