# 2026-08-04 SO-14129 — 팩 무단완료 (원인 확정 · 대응 배포 2026-08-05)

⚠️ **이 문서는 2026-08-05 에 정정됐다.** 08-04 밤 버전은 H1(스캐너 CR)을 "유력" 으로 두고 있었는데
**실측에서 반증**됐고, 근거로 쓰인 관찰 하나가 **관찰이 아니었다**(아래 「정정」). 08-04 판을 근거로
재활용하지 말 것 — 스킬 쪽 정본은 `.claude/skills/asung-wms/SKILL.md` 규칙 9 「완료 확인은 공용 마찰 모달」.

**근거 등급 표기** (「기록 규칙」의 "근거의 출처를 표시한다"): **[실측]** 날짜·값이 있는 확인 /
**[코드]** 코드 읽기로 확인 / **[증언]** 사람 말, 미확인 / **[설명]** 일반 동작에서 유추.

---

## 확정된 원인 (2026-08-05 [실측] — 상시 재현)

⚠️⚠️ **물리 탭 2회(footer `Complete pack` 오탭 → native confirm 의 `OK` 오탭)만으로,
스캔 0건 상태에서 60줄 오더가 완료된다. 조건도 타이밍도 필요 없고 언제나 재현된다.**

- 특별한 상태·레이스·스캐너가 필요 없다 — **누구나 아무 때나 두 번 탭하면 끝난다.**
- 정황: **footer 인접 버튼 오탭** + **읽지 않은 영어 확인창**. 두 버튼이 손가락 이동거리 안에 있어
  첫 오탭의 관성이 그대로 `OK` 위에 떨어진다.
- 즉 "왜 하필 그날 그 오더인가" 를 설명하는 특별한 원인은 **없다.** 구조가 열려 있었다.

## 정정 (08-04 판이 틀린 부분)

- ~~**H1 — 스캐너 말미 CR 이 native confirm 의 기본 버튼(OK)을 승인한다** (08-04 "유력")~~
  → ⛔ **[실측] 반증됨** (2026-08-05, 안드로이드 태블릿 실물): confirm 이 떠 있는 동안 스캔하면
  **스캐너는 읽었으나(비프) 팝업은 무반응**. 접미는 **CR 뿐이고 Tab 은 없다** → 08-04 의 후보 2
  ("Tab 접미로 포커스가 옮겨간다") 도 같은 실측으로 **사망**.
- ⚠️⚠️ ~~**"완료 후 다음 스캔 시 alert 가 떴다"**~~ → **사고 당시 관찰이 아니다.**
  이것은 **[설명]** — *일반적인 초과 스캔 동작*에 대한 설명이었는데, 조사 과정에서 **[관찰]로
  잘못 승격**됐다(08-04 조사 프롬프트에 "재현으로 확인" 이라고 적혀 있었지만 확인된 게 아니었다).
  - **[코드]** 초과 스캔은 **사이렌 + `#flash` 배너**다. **다이얼로그가 아니므로 Enter 를 삼키지 않는다.**
  - **[실측]** 이 오더의 discrepancy 에 **over 계열 0건** — 초과 스캔이 실제로 일어나지 않았다는 것과 정합.
  - ⚠️ 이 한 줄이 하루치 조사 방향을 정했다. 교훈은 SKILL.md 「기록 규칙」의
    "근거의 출처를 표시한다" · "UI·입력 동작은 코드 읽기로 결론내지 않는다" 두 항목으로 승격됐다.

## 확정 [코드] — 08-04 조사에서 확인된 것 (유효)

- 팩 완료 쓰기 경로는 packer.html doneBtn 단 하나. 자동 판정·타 화면·EF 경로 없음
- 애플리케이션 트리거 0건 (pg_trigger [실측] — 6건 전부 Supabase 내장)
- cron 잡 2건뿐: wms-poll-orders(*/5), wms-reap-stale-claims(*/2). 둘 다 완료 못 만듦
- short_after_pack insert 도 packer.html 한 곳. 배열 1회 insert → 밀리초 동일은 정상 지문
- verified 합 0 완료를 막는 가드 없음. 미검수와 부족을 구분하지 않고 전 라인 short
- doneBtn 에 lines.length>0 가드 없음
- 완료 UPDATE 는 .eq("id") 만 — CAS 없음. ensureMine() 은 네트워크 오류 시 통과(best-effort)
- responsible = enterPack 시점에 읽은 wms_pick_tasks.assigned_to (단일 행, 라인 조인 아님)
- wms_pack_tasks 에 completed_by 없음 → **2026-08-05 추가**(`20260805000000_completed_by_pack_link.sql`)
- 롤백은 pack_task_lines/pack_tasks 를 delete → 증거 소멸
- 롤백이 wms_discrepancies 를 건드리지 않음. discrepancies 에 pack_task_id 없어 "그 단계의 산물"
  식별 불가 → **2026-08-05 `pack_task_id`/`pick_task_id` 링크 컬럼 추가**(FK 없음 = 롤백 delete
  에도 증거 보존, **읽는 쪽은 의도적으로 미구현** — 나중 롤백 무효화 판단 근거로만)
- reaper 는 무작업 클레임을 pending 으로 풀지만 행을 남김 → 팩 큐에 "Resume verify" 카드로
  재등장 (verified=0)
- ← List 만 무작업 릴리즈. ☰ Menu·탭 닫기·뒤로가기는 릴리즈 안 함

## 반증됨 (08-04 + 08-05)

- 자동완료("0 vs 0" 판정) 가설 — 호출 지점이 코드에 없음 [코드]
- 로드 전 창 가설 — startPack/resumePack 은 라인 생성 후 화면 전환 [코드]
- pg_cron reaper 원인설 · 픽 자동완료설 [실측]
- **H1 스캐너 CR 이 confirm 을 승인** — [실측] 반증 (위 「정정」)
- **후보 2 Tab 접미로 포커스 이동** — [실측] 반증 (접미는 CR 뿐)

## 대응 (2026-08-05 배포 · 커밋 b426275)

**하드 차단이 아니라 마찰 모달** — 사용자 결정. 스킬 정본은 규칙 9 · 상세는
`references/frontend.md` 「2026-08-05 — 되돌리기 어려운 완료의 마찰 모달」.

- 공용 `wms-confirm-modal.js`: 부족 수량(base)을 **정확히 타이핑**해야 `End` 활성화 ·
  **Enter 확정 불가**(캡처 차단 — 스캐너 CR 포함) · Escape=취소 · autofocus 금지 ·
  표시 중 `scanBusy` 로 processScan 차단 · 큰 `Keep …` 버튼을 **모달 맨 아래**(오탭한
  손가락의 두 번째 탭이 떨어지는 자리 = 안전 방향), `End` 는 작게 마찰 입력 옆
- 티어 2(부족 ≥ 주문 base 의 50%, 검수 0건=100%)는 빨간 경고 추가 — **마찰 로직은 티어 무분기**
- `stock_short` 선언 라인은 부족 계산에서 제외 → 선언만 남으면 가벼운 confirm
  (규칙 41: 정직한 기록을 벌주지 않는다)
- ⚠️ **버튼 위치 변경·하드 차단은 하지 않았다**(사용자 결정) — 마찰 모달 하나로 끝낸다
- ⚠️ **현장 미검증**: 장갑 낀 손 숫자 입력 · 부족 50% 경계 · picker 경로 — SKILL.md 백로그 「검증 대기」

## 미해결로 남긴 것

- ⬜ **auth_user `339315ab-…` 신원 미확인** — 이 세션의 로그인 주체가 누구인지 못 밝혔다.
- ⬜ **과거 동일 사고 전수 조회 미실시** (사용자 결정 — 하지 않기로 함).
  같은 서명(verified 합 0 + 전 라인 short + 밀리초 동일 insert)으로 조회하면 찾을 수 있다.
- ⬜ **SO-14129 의 `responsible=Joon Kwon` 59건 미정리** — 원인이 오탭으로 확정됐으므로
  이 귀속은 실수 집계에서 부당하다. 정리 방법은 미결정.
- ⬜ **H2 공유 태블릿 제3자** — 배제하지 못했지만, 원인이 "누구나 2탭" 으로 확정됐으므로
  누가 눌렀는지는 이제 사고 원인이 아니라 귀속 문제다(위 59건).
- ⬜ **귀속 엇갈림(Jeff 배치 → Joon 귀속) 후보 3개 미판별**: (A) 픽 단계 이어받기
  (B) enterPack enrich 레이스 (C) resumePack 이 reaper 가 풀어둔 pending 을 잡음 ← 가장 단순.
  `completed_by` 가 08-05 부터 찍히므로 **앞으로는 배치 소유자와 완료자를 구분할 수 있다.**
- ⬜ **`wms_orders.status='ready_to_close'` 쓰기 경로 전수 미조사**
