# 2026-08-25 — 같은 자(尺) 완결: C 병기 · vm 오염 · 리시빙 공제

커밋 4건 + 이 문서, 전부 배포·검증 완료 (어제 2329e19 에 이어짐):
```
4c5e98d  C 단계 — "minus holds" 병기 + 닫기 RPC(서버 시계)
19fd9b5  pack verification_method 덮어쓰기 결함 수정
69cddb2  신뢰 시점 기입 (2026-08-26 00:00+00)
a4a63b0  리시빙·트랜스퍼도 같은 자 — Hold·선적대기 공제
```
「무엇을」은 커밋 메시지에 있다. 이 문서는 **왜 그렇게 됐나**와 **다음 세션이 모르면
손해인 것**이다. 불변식·상수는 `asung-wms` 스킬 규칙 37 계열이 정본.

---

## 실측 (전부 프로덕션 조회 — 추정 아님)

| 실측 | 값 | 의미 |
|---|---|---|
| pick 1767 (Joyce Lee) | **989.7분 → 30.8분** | 한 배치가 그 사람 픽 평균을 **3.6배** 부풀리고 있었다 — 화면이 특정 작업자에 대해 사실과 다른 인상을 주던 것 |
| 자동 Hold 첫 발동 | 21시간에 1건 (마지막 스캔 10:51 → 재개 11:28 · 36.7분 부재) | **오탐 아님** — 10분 상수의 첫 실증. ⚠️ 표본 1건이다 |
| pack `verification_method` 분포 | scanned_base 18,364 · **null 3,702** · manual 3,449 · scanned_variant 2,687 | null 이 manual 보다 많고 당일까지 생성 — 아래 발견 ① |
| picker 동일 분포 | **null 0** | 대조군 — 픽은 구조 결함 없음(두 로드 경로 모두 복원) |
| pick manual 비율 (수량대별) | 1개 4.9% · 2~5개 13.5% · **6개+ 38.7%** | 하나 스캔하고 수량을 탭으로 채우는 패턴으로 보인다 — **추정**(확인 수단이 없다 → 미해결 2번) |
| 리시빙 PO-01151 | created 14:10 → completed 15:30 = **80분**인데 화면 "< 1 min" | 라인 20건의 `last_received_at` 이 15:30:54.271~57.438 = **3.2초** — saveLine 큐가 시각을 전송 시점에 만든다(스캔 시점이 아니라) |

---

## 발견 3건 (앵커 계상은 문서 끝 — ①만 「조용한 결함」 · ②③은 구현 전 예방)

**① pack `verification_method` 오염** — `resumePack` 이 `verified_base` 는 복원하면서
vmethod 만 null 로 버렸고(조회 select 에도 없었다), 완료·Hold RPC 가 전 라인의 방법을
`r.vm` 그대로 다시 써서 **스캔으로 저장된 라인이 Hold·재개·새로고침 뒤 완료되면 null 로
덮였다. Hold 를 성실히 쓴 사람일수록 기록이 더 지워졌다.** 4조건 충족(에러·정지·신고
없음·데이터 틀림). ⚠️ 이 컬럼은 「실물 스캔 없이 눌러 넘긴 작업」을 찾는 **직접 증거**라
오염이 비가역이다 — 수량은 다시 세면 되지만 "스캔했는지"는 지나가면 끝.
수정 = packer 복원 2줄(근본 — 픽과의 비대칭 해소) + RPC `coalesce(r.vm, 기존)` 심층 방어
(verified_at 보존 전례와 같은 수법 · 새 값이 항상 이겨 정당한 변경은 통과).
**신뢰 시점 = 2026-08-26 00:00+00** (배포 19:51 + 하루 여유 — 옛 화면이 태블릿에 남는 창).

**② admin Reopen 이 receiver 재개 분기를 안 지난다** — Reopen 은 DB 를 직접 in_progress 로
바꾼 뒤 이동하므로 openReceipt 의 held/partial 분기가 미발동. 리시빙 이력 닫기를 receiver
쪽에만 걸었다면 **startWave 를 13일 놓친 것과 같은 모양**이 될 자리였다 — 구현 전 조사에서
잡아 양쪽에 걸었다.

**③ 리시빙 고유 엣지 — A 가 Hold 한 문서를 동시 작업자 B 가 그대로 완료할 수 있다**
(finishReceipt 에 status 조건이 없다 — 의도된 동시 다인 설계의 부산물). 픽·팩엔 구조적으로
없던 경로(held 상태에서 완료 불가). 처방 = **완료 시에도 `wms_resume_hold` 호출** — hold 의
종점이 완료 시각이라는 의미로 정확하다.

---

## ⚠️ 내가 오늘 틀린 것 — 패턴이 문제다 (감추지 않는다)

- `wms_receipts` 컬럼명을 **추측**했다 — `references/schema.md` 에 있는데 안 읽었다.
- "리시빙에는 Hold 가 없다"고 **단정**했다 — grep 한 번이면 나왔다(receiver.html:323).
- 2026-08-24 를 일요일이라고 했다. 그 위에 "주말이라 근무시간 0" 이라는 **그럴듯한 진단**까지
  얹었다 — Caleb 이 안 잡아줬으면 그 오진으로 계속 갔다.

📌 **세 번 다 오후에 몰렸다.** 성공이 이어지면서 확인 단계를 건너뛰기 시작한 것으로 보인다.
그리고 각 답이 그럴듯해서 스스로 눈치채지 못했다 — 프롬프트마다 「필드명 추측 금지」를
받으면서 정작 안 지켰다.
⇒ **교훈: 틀린 답이 자연스러워 보일수록 검증을 안 하게 된다. 말하기 전에 "이건 봤나,
추측인가"를 나눌 것.** (스킬 「근거의 출처 표시」 규칙의 실패 사례로 이 날짜를 인용할 것)

**보고 절차 정정 1건 (같은 계열)**: 같은 커밋에 대한 `--stat` 이 시점마다 달랐다(admin 81→45 ·
receiver 38→26) — 「구현 보고 → 검토 지적 → 재수정 → 재보고」 사이클이 커밋 전에 여러 번
돌았기 때문(무단 후수정은 아님 · 매번 재보고함). 그러나 **움직이는 파일을 검증하면 검증이
무효**라는 지적이 맞다 ⇒ 앞으로 재수정 보고에는 **"N차 판" 표기 + 그 시점의 `--stat` 수치**를
먼저 적는다 — Caleb 쪽 수치와 다르면 판이 다른 것이 즉시 드러난다.

---

## partial 의 정확한 의미 (재조사로 확정 — 첫 보고의 근거 등급 실패 포함)

**「PO 당 receipt 1건」**(`cin7_purchase_id` unique · 20260806000000:32) + **partial 은 Apply
게이트 통과 불가**(admin :2756 `status==="completed"` · EF receiving :626 throw — 이중 차단).
선적마다 Apply 하면 I&R 이 여러 개 생겨 「하나의 I&R 만」 설계와 충돌하므로, **한 receipt 에
수량을 누적하다 전량 도착 후 completed → Apply 1회 = I&R 하나. 모순이 아니라 한 설계의 양면.**
⚠️ 첫 보고는 이것을 UI 문구에서 **추론으로 단정했고 근거 등급을 안 밝혔다** — Caleb 이 설계
지식으로 잡았고, 재조사가 제약·게이트 실물로 확정했다(결론은 유지·등급이 승격).
[실측] 현재 partial 0건 · completed-미적용 0건 / 전체 52건.

---

## 검증된 것 (오늘 실물)

- 닫기 RPC 서버 시계 — `resumed_at > held_at` 확인(태블릿 시계 음수 소멸)
- 리시빙 Hold → 재개 이력 · admin Reopen 경로 닫기 · 삭제된 receipt 를 hold_leak 이 안 잡음
- pack verification_method 원격 반영(두 RPC prosrc 에 coalesce)

## 미해결 · 다음

```
1. ⬜ Throughput 사람별 리시빙 — last_received_at 이 전송 시각이라 근본 데이터가 깨져 있다
   (PO-01151: 20줄 3.2초). 선행 = saveLine 큐에 **스캔 시점** 캡처(전송 시점이 아니라).
   ⚠️ 검증된 저장 경로 변경 — 위험 높음(동작 동일성 증명 필요).
   고쳐도 「여러 명이 나눈 시간의 배분」 규칙은 별개로 남는다. **내일 작업.**
2. ⬜ 스캔 횟수 기록 (보류 — Caleb 지시): verification_method 는 마지막 조작만 남아
   「물건 안 보고 넘김」과 「스캔 후 수량만 탭」이 구분 불가 — 위 manual 분포(6개+ 38.7%)가 근거.
3. ⬜ 10분 상수 미확정(실증 1건뿐) · wave Hold 이력 미검증 · 자동 Hold 문구 실물 미확인
4. ⬜ RECV_PAUSE_SINCE(2026-08-26 00:00Z)는 실측이 아니라 여유 상계 — 배포 후 첫 receipt
   이력 행으로 확인할 것:
   select min(held_at) from wms_task_holds where task_kind='receipt';
5. ⬜ in-place Resume 버튼(백로그 — 자동 Hold 발생 빈도 보고 판단) ·
   inv-sku-types cron 미등록(원장 — asung-inv-ledger 스킬 「검증 대기」)
```

## 📌 앵커 — 「세션당 조용한 결함 발견」 08-25: **1건**

= 발견 ①(vm 오염 — 4조건 충족 · **실제로 3,702행이 오염돼 있었다**).
⚠️ **②③은 여기 세지 않는다 (Caleb 확정)**: 이 앵커는 「실제로 데이터가 틀어지고 있던 것」을
세는 지표다 — ②③은 구현 전 예방이라 아직 아무 데이터도 안 틀렸다. 같이 세면 나중에
"그날은 3건"을 볼 때 실제 사고 규모를 오해한다. **값어치가 없어서가 아니라 세는 대상이
달라서다** — 별도 항목으로:

**구현 전 예방 발견: 2건** — ②(admin Reopen 우회 — startWave 13일 미발견과 같은 모양이 될
자리) ③(A Hold → B 완료 엣지 — 리시빙 고유 · Claude Code 자체 발견).

**세션 성격: 깊이 판 구현+검증 세션** — 작업시간 자 하나를 픽·팩·wave·리시빙·트랜스퍼
전 워크플로에 완결하며 각 단계마다 조사를 선행했다(②③이 그 조사의 산물).
