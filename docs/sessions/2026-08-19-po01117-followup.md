# 2026-08-19 — PO-01117 추적 결과 · 리시빙 목록 노출 처리 · 6종 개별 검증

어제(`docs/sessions/2026-08-18-purchase-source-redesign.md`) 「다음 세션에서 먼저 할 것」 5항목에
대한 **결과 보고**. 관측 = GAS `ppa_cardIdSnapshot` 09:01(토론토) + Cin7 화면(ST-01220) +
`wms_receipts` 조회. 코드 커밋: `2c36107`(EF 주석) · `591b892`(Applied 배지).

**PO-01117 오늘 아침 상태**

```
Type=Advanced · LastUpdated=2026-08-19T12:36:54Z (08:36 토론토)
listSRS="AUTHORISED"(어제 PARTIALLY RECEIVED) · listCRS="PARTIALLY RECEIVED"
SR [DRAFT]      50줄 · CAN01620=192 · loc=null · CardID 전부 새 값   ← 오늘 아침 생성
PA [AUTHORISED] 51줄 · CAN01620=192 · loc 있음 · CardID 어제와 동일(51/51)
```

---

## 1. 어제 5항목의 결과

| # | 항목 | 결과 |
|---|---|---|
| 1 | `putaway:DRAFT` 해소 | ✅ **해소** — 오늘 `PA=AUTHORISED`. 화면/API 불일치는 **지연**이었다 |
| 2 | `cron.job` 4·5 실행 확인 | ⚠️ **판정 불가로 확인됨**(아래 §3) — 결과물 확인 방법 **별건** |
| 3 | 미달 24개의 행방 | ✅ **PO 채움 + `ST-01220` adjustment**(아래 §2) |
| 4 | 분할 인보이스 비율 조사 | ⬜ **미착수** |
| 5 | 리시빙 라인별 작업자 검증 | ⬜ **미착수** |

### 1-1. `putaway:DRAFT` = 지연 확정 (①)

Convert 직후 `DRAFT` 였던 PA 블록이 다음 날 아침 `AUTHORISED` 51줄로 바뀌었다.
**화면이 먼저 바뀌고 API 가 따라온다.** 원장은 `AUTHORISED` 만 기표하므로 그 구간의 문서는
**다음 회차에 기표된다 — 유실이 아니라 지연**이다.
⇒ 정본 4부 「신규 미확정」의 해당 항목 닫힘 · 스킬 함정 표에 "PA 는 DRAFT 로 생성 → 승인" 기재.

⚠️ 오늘 아침 **SR 블록이 50줄로 새로 생겼다**(CardID 전부 새 값 · `loc=null`). PA 축을 읽는
현재 구현은 이걸 무시하므로 영향 없다 — **축 선택이 옳았다는 반대 방향의 증거**이기도 하다.

---

## 2. 미달·초과 처리의 실무 흐름 (③) — 이번 세션 최대 수확

⭐ **정본은 `ledger-design.md` 1부 「미달·초과 입고의 실무 흐름」** — 여기는 관측 사실만.

`ST-01220` (`Reference: Initial Warehousing - Parfums de Coeur, Ltd. (PO-01117)`):

```
CAN01620  201 → 177   (−24)   ← 미달분 되돌림
CAN01547  503 → 527   (+24)   ← 초과분 반영
```

- PA 라인 `CAN01620` 은 **168 → 192**(인보이스 수량)로 채워졌다 — **PO 를 닫기 위한 형식**이지
  재고를 늘리려는 게 아니다. 그래서 차액을 adjustment 로 되돌린다.
- **한 입고가 두 소스에 걸친 사건**을 만든다: `po_in`(발주) + `adjustment`(조정).
- Adjustment 는 **절대값**이라 앞선 오차를 흡수한다 → **창고 총량은 자동으로 맞는다.**
- ⚠️ **bin 단위는 어긋난 채 남는다** — 조정 bin(`E030501`)과 입고 bin(PA `3d3c6295`)이 다르다.
  자리 단위 원장으로 올릴 때 드러난다.

### 2-1. ⚠️ WMS discrepancy 큐는 최종 진실이 아니다

`CAN01547` — WMS `apply_note` 는 초과 **16** 을 클램프했다고 기록했는데, 매니저 실사는 **24** 였고
`ST-01220` 도 **+24** 로 조정됐다. 작업자 보고와 매니저 실사가 다를 수 있고 **Cin7 조정은 매니저
실사 기준**이다.
⇒ 대조에서 「WMS 수량 ≠ 원장 수량」은 **오류가 아니라 구조적 차이**로 분류한다(정본 3부 2단계).
📌 최종 재고의 기준은 **매니저 실사가 반영된 Cin7**, 원장은 그것을 읽으므로 원장이 맞다.

### 2-2. `ON CONFLICT DO NOTHING` — 하루 만에 실례가 나왔다

어제 「실무 빈도 확인 대기」로 열어둔 시나리오 ①(수량 정정)이 **정규 절차로 확인**됐다:
PA `CAN01620` 168→192 · **`CardID`·bin·문서번호 전부 동일, 수량만 다르다.** `DO NOTHING` 이면
192 가 조용히 버려진다.

⚠️ **다만 긴급도는 낮아진다** — adjustment 절대값이 총량을 확정하므로 168 이 남아도 잔고는 맞는다.
총량 ✅ / bin 단위 ❌ / 구간 오차(PO 채움~adjustment 사이) ❌.
⇒ **⑤ 전에는 「키는 같은데 값이 다른 행」 감지·경고만으로 충분**하고, 역분개는 자리 단위 원장 때로
미룬다(정본 4부 3번에 반영).

---

## 3. ⚠️ cron/EF 건강 판정법 (②) — 오늘 발견

정본은 `asung-wms` 스킬 규칙 12(⚠️ **`asung-ops` 로 옮길 것 — 별건**). 요지:

- `cron.job_run_details.status='succeeded'` = **요청 발송 성공**일 뿐. **EF 가 401 로 죽어도 succeeded.**
- `net._http_response` 는 **매 실행마다 5초 타임아웃**을 남긴다(연결·SSL 은 수십 ms 정상 — 응답만
  못 기다림). EF 는 그 뒤에도 계속 돌아 자기 일을 마친다.
  ⚠️ **이 로그를 보고 "폴링이 죽었다"고 판단하면 멀쩡한 것을 고치게 된다.**
- ⇒ **판정은 결과물로만.** 폴링 = `select max(checked_at) from wms_polled_sales`
  ([실측] 09:09 조회 시 9분 전 갱신 = 정상, 주기 5분).
- ⬜ `product-images`(cron 4·5) 결과물 확인 방법 **미정 — 별건**(`wms_image_sync_runs` 가 후보).

---

## 4. 오늘 추가로 나온 것 — PO-01117 이 리시빙 목록에 떴다

**현상**: 「READY TO RECEIVE (CIN7)」에 PO-01117 이 `Start` 버튼과 함께 노출
(receipt id=58 · `status=completed` · `applied_at` 2026-08-18 19:41).

**원인 — 오작동이 아니다**:
- Cin7 이 정확하다 — 인보이스 192/입고 168 이라 **부분 입고**로 열려 있었다.
- `listOpenPOs()` ④번(`StockReceivedStatus==="AUTHORISED"` 완전일치)은 Convert 후 값이
  `PARTIALLY RECEIVED` 라 통과 — **어휘 완전일치의 구조적 한계**.
- ✅ **데이터 안전은 이미 확보돼 있었다** — `startPo()` 의 `applied_at` 가드가 토스트만 띄우고
  return. **이중 입고는 구조적으로 불가능.** 남은 건 작업자 시간 낭비뿐이었다.

**판단·처리**:
- ⊘ **목록에서 제외하는 안은 철회** — 숨기면 "내 PO 가 목록에 없다"는 새 혼란이 생긴다
  (PO-01083·PO-01130 실사고 계열). 한 번 구현했다가 되돌렸다.
- ✅ **`Applied` 배지로 표시**(`591b892`) — 카드는 남기고 버튼 자리에 비활성 배지 + 카드 onclick
  비활성 + sub 줄 `Already applied to Cin7`. `startPo()` 가드는 **두 번째 겹으로 유지**.
- ✅ ④번 필터는 유지하되 **주석을 보조 방어선으로 격하**(`2c36107`) — 어휘 나열 확장 금지 명시.
- ⚠️ **배지는 실사용 미검증** — PO-01117 은 오늘 `listSRS=AUTHORISED` 로 바뀌어 ④번에 걸려
  목록에서 빠졌다. **다음 미달 입고가 첫 검증이다.**

📌 부수 확인: 리시빙 `Delete` 는 **물리 삭제**다(admin.html — `wms_rollback_archive` 보존 후
행 delete · 라인 FK CASCADE · soft-delete 컬럼 없음).

---

## 5. 오후 — 6종 개별 검증 (발주 축 재설계 후 처음)

`@2026-08-18.2` 로 **6종 소스를 하나씩** 돌렸다(사이 `sleep 90` — 연속 호출은 429).

| 소스 | 창 | 결과 |
|---|---|---|
| 발주 | `from_since=2026-08-18` | `candidates 20/20` · `ledger_rows` **727**(어제 676 → **+51**) · `putaway_block_status_counts={AUTHORISED:15}` · `blocks_skipped={stock_received:NOT AVAILABLE:6}` — **`putaway:DRAFT` 0**(어제 1) · `merged_lines`·`card_id_fallback`·`adv_no_putaway`·`non_inventory_lines`·`received_false_lines` 전부 0 · `warnings` 없음 · `wh=["Asung Trading Inc."]` · `bins=["C011001","C011102","C011201","C011202"]` |
| 판매 | 같음 | `candidates 145 / detail_fetched 40` ⚠️ **캡** · `ledger_rows 563` · `merged_lines`·`missing_date_items` 0 · `warnings` 없음 · `line_ref` = `<TaskID>:<ProductID>` 확인(같은 fulfilment 3줄이 앞부분 공유) |
| 반품 | 같음 | `4/4` 캡 없음 · `ledger_rows 2` · 전부 0 · `warnings` 없음 |
| 이동 | 전량(커서 없음) | `ledger_rows 3,172` · `wh=["Asung - Edmonton","Asung Trading Inc.","IN_TRANSIT"]` · `events=["transfer_in","transfer_out"]` · ⚠️ **`bins=[""]`** (⚠️ 데이터 부재가 아니라 **미판독** — 5-4) · `warnings=["NO FLOOR - scanning from the very first document"]` |

### 5-1. ☑ 게이트 3번(재등장 전제) 닫힘 — 이번 검증의 최대 수확

어제 「⚠️ 이 전제는 미확인」이라 적어둔 바로 그것이다. PO-01117 이 08-18 저녁 `PA=[DRAFT]` 라
기표 보류(`blocks_skipped: putaway:DRAFT 1`)됐다가, 오늘 아침 `PA=[AUTHORISED]` 승인 후
**같은 창의 `ledger_rows` 가 676 → 727 로 정확히 +51**(PA 라인 수) 늘며 들어왔다.
⇒ **「보류하되 커서는 전진」 설계가 유실을 만들지 않는다**는 실증. 정본 4부 체크리스트 ☑3.

### 5-2. 👁 게이트 2번(adv_no_putaway) 하향 — 게이트가 아니라 관찰 대상

**WMS 는 put-away 없이 Apply 하지 않는다**(Caleb 확인 + 코드: Simple 은 Convert 시 SR→PA 이동,
Advanced 는 stage2 가 put-away 를 반드시 호출). ⇒ 「Advanced 인데 PA 없음」은 **우리 시스템이
만들 수 없는 상태**이고, 이 코드는 **예외 감지기**(WMS 밖 직접 입고 · stage2 부분 실패 방치)다.
실패 방향도 안전하다 — 기표를 **안 하는** 쪽이지 틀린 행을 만들지 않는다. ⇒ ⑤ 진입을 막지 않는다.
⚠️⚠️ **복귀 조건: Advanced PO 기본값을 켜면 다시 게이트로.** 그때는 WMS 가 Advanced 경로로 쓰므로
stage1 성공/stage2 실패 문서가 실제로 생길 수 있다.

### 5-3. 새 사실 4건

1. ⚠️ **판매 캡이 3.6배** — 하루치 창에서 후보 145 대 캡 40. 하루치를 따라잡는 데 **4회차**가
   필요하다(유실은 없으나 실시간성 저하 — shadow 대조 때 전날 판매가 아직 안 들어와 있을 수 있다).
   ⇒ **해결은 캡 상향이 아니다**(1,200ms×40=48초로 이미 창의 80%). **회차 주기**를 짧게, 소스별로
   캡·주기를 다르게 두는 안을 ⑤에서 정한다(판매·발주 자주 / 반품 드물게 — 하루 4건).
2. ⚠️ **트랜스퍼의 bin 은 헤더에 있다 — 원장이 안 읽고 있을 뿐이다** (⚠️ 아래 5-4 정정 참조).
   `FromLocation`/`ToLocation` 이 `"창고: bin"` 문자열이다([실측] TR-02645
   `"Asung - Edmonton: EZ01Pallet03"` → `"Asung Trading Inc.: F0300PALLET01"` · 목록·상세 양쪽).
   **문서 하나 = bin 하나 → bin 하나**라 라인에 bin 이 없는 것이 정상이다(트랜스퍼는 문서 하나 =
   자리 하나가 업무의 성격 — WMS 도 Apply 시 **출발 bin 별로 문서를 쪼갠다**).
   현재 코드는 `From`/`To` GUID 만 `resolveLoc` 에 넘겨 창고만 해석 → `bins=[""]`.
   **창고 단위는 지금도 정확** · 자리 단위로 올릴 때 `": "` 파싱 + 매핑 대조로 고친다.
   ⚠️ `Lines[].ProductCustomField1/2` 는 상품 마스터 필드라 쓰면 안 된다(TR-02644 는 null).
3. ✅ **되돌림 트랜스퍼는 이미 정확하게 하고 있다** — 잘못 간 물건은 **WMS 를 거치지 않고 Cin7 에서
   직접** `From`/`To` 를 반대로 한 트랜스퍼 백으로 해결하며, 실물 7건(`Search=error` · 전부
   COMPLETED) **전부 `창고: bin` 으로 출발·도착이 명시**돼 있다(사람이 `ToLocation` 에도 bin 을
   찍는다). `Reference` 에 사유도 남긴다(「EDM Transfer Error」·「Revised Transfer Error -
   TR-01126」). ⇒ 원장이 그 데이터를 안 읽는 것이 문제였을 뿐(2번). ⚠️ 남는 질문 = 실물 이동이
   없는 장부 조정이면 원장은 실제 이동과 구분하지 못한다(총량은 왕복 상쇄 · **자리 단위에서는
   두 번의 이동**으로 기록) — 자리 단위 설계 때 판단. ⇒ 정본 1부 이동 절에 등록.
4. ⚠️ **이동 소스는 커서 seed 가 필수** — `from_cursor` 없이 부르면 TR-00001(2025-10-27)부터
   전량을 훑는다. dry 는 정상이나 쓰기를 켜면 **매 회차 처음부터 재처리** → 게이트 6번으로 신설.

### 5-4. ⚠️ 정정 — 「트랜스퍼는 bin 이 없다」는 오판이었다 (같은 날 재조사)

처음에 **dry 응답의 `bins=[""]` 와 `raw.line` 필드 목록만 보고 "bin 이 없다"고 결론**냈다.
**Cin7 화면에는 bin 이 보인다**는 Caleb 지적으로 GAS 프로브를 돌렸더니 **헤더
`FromLocation`/`ToLocation` 에 `"창고: bin"` 문자열로 있었다.** 「데이터가 없다」가 아니라
**「우리가 안 읽는다」**였다.

📌 **교훈: 응답의 한 부분만 보고 "없다"고 결론내지 말 것** — 발주에서 SR 만 보고 「Advanced 는
bin 이 없다」고 판단했던 것과 **정확히 같은 실수를 이틀 만에 반복**했다. 그때도 답은 다른
블록(PA)에 있었고, 이번에도 다른 위치(헤더)에 있었다.
⇒ 점검 질문: **"이 값이 라인에 없다면, 헤더·다른 블록·다른 엔드포인트에는 없나?"**
그리고 **화면에 보이는데 API 에 없어 보이면 API 를 덜 본 것**이다.

---

## 6. 다음에 할 것

1. **페이싱 수정** (⑤ 게이트 5) — `DETAIL_SLEEP_MS` 1,200ms + `Retry-After` 백오프
2. **ON CONFLICT 감지 장치** (⑤ 게이트 4) — 「키 같고 값 다름」 경고
3. **6종 동시 실행 1회** — 개별 검증의 마지막 항목 (**1번 완료 후에만**)
4. **8/22 기초 스냅샷** ④
5. **이동 커서 seed** (⑤ 게이트 6) — `inv_sync_state` 를 쓰기 전에 채울 것

**별건**: 분할 인보이스 조사 · 리시빙 라인별 작업자 검증 · `product-images` cron 결과물 확인 ·
GitHub private 전환(집) · `Applied` 배지 검증 대기 · `asung-ops` 이관

⚠️ **⑤(쓰기) 진입 게이트 현황** — **정본은 설계 4부 체크리스트**:
☑1 CardID 안정성 · 👁2 adv_no_putaway(관찰) · ☑3 재등장 전제 ·
⬜4 ON CONFLICT(오늘 실례 확보 — 「값 다른 행」 감지·경고로 범위 축소) ·
⬜5 캡·페이싱(수치 확보, 코드 미반영) · ⬜6 이동 커서 seed
