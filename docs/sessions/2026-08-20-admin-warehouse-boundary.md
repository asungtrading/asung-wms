# 2026-08-20 — 레포 public 복귀 · admin 창고 경계 · 롤백 벨트 · Finalized 실 출고량

어제(`docs/sessions/2026-08-19-po01117-followup.md`)에 이어지는 날이지만 **문서를 나눈다** — 날이 다르고,
어제 밤에 되돌린 결정(레포 private → public)이 어제 문서의 결론을 뒤집기 때문이다.

**이날의 성격**: 코드 3건(창고 경계·롤백 벨트·Finalized 표시)은 전부 **SO-13893 한 사고에서 뻗어 나왔다.**
조사가 그 사고를 「두 결함의 합작」으로 분해했고, 각 결함이 각자의 수정으로 갔다.

커밋: `9e61285`(Finalized 실 출고량) · `d05ec51`(창고 경계 + 롤백 벨트) · `e75ef52`(Stats `~` 병합)
+ 이 문서 커밋. 어제분 참조: `975d149`·`9ed8f5b`(private 관련 — §0 에서 상당 부분 되돌림).

---

## 0. ⚠️ 레포 private 전환 — 시도 후 복귀

**public → private(08-19 저녁) → public(08-19 밤).**

**기술적으로는 문제가 없었다** — 개인 계정 GitHub Pro 로 무중단 전환됐고 Pages 설정
(Source `main /(root)` · `wms.asung.ca` · Enforce HTTPS)이 전부 유지됐다. 어제 문서에 적은
「미착수 전제 2개가 둘 다 틀렸다」(배포 방식 변경 필요 · 무중단이 어려울 것)는 그대로 유효하다.

⚠️ **되돌린 이유는 작업 방식이다** — 대화 세션의 Claude 가 `raw.githubusercontent`·codeload 로 레포를
읽지 못하게 되면서(404 실측) **진단·프롬프트의 정밀도가 떨어졌다.** 코드 위치를 직접 특정해
「④번 필터가 뚫린다, 339~343행」처럼 쓰던 것이 「목록 필터 어딘가를 확인해줘」가 된다.

**위험 재평가**: 실제 시크릿은 레포에 없었다(주석마다 금지가 박혀 있었고 값은 Supabase Secrets·
Script Properties 에 있다 — **규율이 지켜지고 있었다**). 남는 노출은 `docs/sessions/` 의 실측 데이터·
설계·직원 이름 정도다. ⚠️ **이미 몇 달간 public 이었으므로 지금 닫아도 과거는 지워지지 않는다.**
⇒ **위험은 낮고 만성적, 비용은 확실하고 즉각적.** public 유지가 현재 결론이다.

📌 재검토한다면 **`docs/sessions/` 만 분리**하는 쪽. `tools.asung.ca`·`gas-system-automation` 도
같은 기준(`docs/` 유무·코드 내 시크릿)으로 판단한다.

📌 **부수 소득 — 되돌렸다고 사라지지 않는 것**: 이 과정에서 「레포가 PUBLIC」을 전제로 한 서술 7곳을
찾아 정리했고, **시크릿 금지의 근거를 「PUBLIC 이라서」가 아니라 「git 히스토리 영구 · 발행 사이트 공개 ·
private=접근권한자 열람」으로 갈았다.** ⚠️ **그 정정은 그대로 둔다** — 레포 상태와 무관하게 유효한
근거이고, 전제에 매달린 금지는 전제가 흔들릴 때마다 무너진다.

📌 **교훈**: 비용·난이도를 추정으로 적어 두면 착수가 미뤄진다(어제 기록). **이번엔 반대로, 착수해 보고
나서야 진짜 비용(작업 방식)을 알았다** — 추정으로는 안 나왔을 항목이다.

---

## 1. SO-13893 전모 — 두 결함의 합작

⚠️ 08-12 에 발견하고 08-13 에 부분 수정한 그 사고의 **나머지 절반**이 오늘 드러났다.

`wms_rollback_log` id=178 (2026-07-30): **Jan Ko(`warehouse_access=edmonton`)가 toronto 오더를**
오더 단위 `pack` 롤백. 당시엔 어떤 벨트도 없었고, 롤백이 `finalized_at` 을 안 되돌려
**closed 인데 팩 기록만 사라진** 상태가 남았다 — **closed 896건 중 유일**.

- **① 창고**: admin 은 `warehouse_access` 를 **한 번도 안 봤다**(grep 0회) — 전 탭 전 창고.
  경계를 지키던 것은 작업자 화면들뿐(picker `inWh` · packer · fulfillment · receiver).
- **② 순서**: 롤백이 Finalize 단계를 모델링하지 않았다. 08-12~13 에 오더 단위·배치 pack 벨트가
  생겼고 **배치 pick 벨트가 마지막 잔여 구멍**이었다.

📌 `id=228`(finalized → pack_complete)은 사고가 아니라 08-13 신설 **`Undo Finalize` 의 정상 사용**이다.

📌 ⚠️ **기각된 가설 2개도 남긴다**(같은 길을 다시 가지 않게):
(a) 「Cin7 브랜치 변경으로 창고가 바뀌었다」 → **불가능** — 현 코드에 `warehouse` 갱신 경로가 **없다**
(b) 「`normWarehouse` toronto 폴백이 오판했다」 → **무죄** — `location` 값이 `Asung Trading Inc.` 836 /
`Asung - Edmonton` 218 로 깨끗하게 갈린다.

📌 **조사 순서의 교훈**: 「이전에는 이렇지 않았다」는 증언에서 출발해 **git log -S 로 경계 커밋을 특정**하는
방식이 오늘 두 번(SO-13893 · Stats `~`) 다 통했다.

---

## 2. admin 탭별 창고 경계 (커밋 `d05ec51`)

| 탭 | 처리 |
|---|---|
| Status·Stats·Trace·Reports·Discrepancy | 전 창고 — 조회 전용이라 무해 |
| Finalized | 전 창고 + **창고 선택 세그**(All/Toronto/Edmonton, 기본 All) |
| **Receiving·Rollback** | **`warehouse_access` 로 제한** |

근거: picker(`inWh`)·packer·fulfillment 가 이미 같은 방식이다. **파괴적 동작이 있는 탭만 제한**하고
조회 전용 탭은 전 창고를 유지한다(사용자 결정).

- ⚠️ **void 박스는 목록 필터를 우회한다** — 오더 번호를 직접 입력하는 경로라 `voidGo` 에 창고 확인을
  따로 넣었다(조회 select 에 `warehouse` 병기 → 차단 toast). **목록만 막으면 구멍이 남는다.**
- ⚠️ **Finalized 는 목록·카드·카운트에 필터를 함께** 걸었다 — 한쪽만 걸면 「32 of 703」이 서로 다른
  모수를 가리킨다(08-14 잘린-모수 계열의 재발).
- ⚠️ **Receiving 의 `pend`(off-PO 승인)만 클라이언트 필터** — 라인 최상위 + receipts 임베드 구조라
  `!inner` 없는 서버 임베드 필터는 **부모 잔존 + 임베드 null** 로 조용히 틀린다.
- 📌 히스토리에 **「Showing Edmonton receipts only (your warehouse access)」** 안내 —
  **숨긴 것을 「없다」로 위장하지 않는다**(PO-01083 계열).
- ⚠️⚠️ **프론트 필터는 표시일 뿐이다** — RLS 가 `auth_all` 이라 진짜 경계가 아니다. 실질 대상은
  단일 창고 접근자 한 명(Jan Ko = edmonton)이고 나머지는 `both`. RLS 창고 스코프는 기존 백로그(규칙 27).
- 📌 검증은 **단일 창고 접근 테스트 계정**을 만들어 했다 — 권한·경계 작업의 표준 절차로 삼는다.

---

## 3. 롤백 벨트 확장 + 배치 pick 벨트 신설 (같은 커밋)

모든 벨트 4곳(배치 pack·배치 pick·오더 fulfillment·오더 pack)이 `status==="closed"` **하나만** 보고
있었다 → **`status==="closed" || finalized_at`**.

**잔재가 0건이어도 조건 자체가 불완전했다** — SO-13893 형(활성 status + `finalized_at` 잔존)을 못 잡는다.
「지금 그 데이터가 없으니 괜찮다」와 「조건이 맞다」는 다른 말이다.

**배치 pick 벨트 신설** — 백로그 「`doBatchRollback` pick 에 closed 벨트 없음」(08-13) 해소.
08-13 의 「`doBatchRollback` 무접촉」 지시를 해제하고 pack 분기와 동형으로 넣었다.

📌 `finalize` 벨트는 무변 — **반대 방향**(finalized 여야 진행)이라 확장 대상이 아니다.
(전수 확인: `select("status,finalized_at")` 5곳 중 4곳이 차단 조건, 1곳이 진행 조건.)

---

## 4. Finalized 탭 실 출고량 병기 (커밋 `9e61285`)

`222 (218)`·`1066 (1042)` — **차이가 있을 때만** 빨간 괄호. 기준은 **팩 수량**, 분할 주문은 배치 합산.

- 괄호 안 = `wms_pack_task_lines.verified_base`. ⚠️ **`expected_base` 를 쓰면 안 된다** — 그건 픽 실적이라
  픽이 덜 가져온 라인이 「채워진 것처럼」 보인다. 비교 기준은 `wms_orders.total_required_base`.
- ⚠️ **초과 스캔은 `verified_base` 에 안 남는다**(packer 가 캡) → `Σverified ≤ Σrequired` 구조적 보장.
- LINES 의 「실제」 = **`verified_base > 0` 인 라인 수**(배치 펼침과 같은 자). 부분 부족은 UNITS 에서만.
- **팩 태스크가 배치 수보다 적으면 회색 `(—)`** — [실측] closed 896건 중 SO-13893 1건뿐.
- ⚠️ **미완 배치 분기를 만들지 않았다** — `wms_pack_tasks.status` 는 `completed` 하나뿐(1,428건)이라
  status 를 검사하면 **죽은 조건**이 된다. 📌 처음엔 넣었다가 DB 실측으로 뺐다.
- 상위 행과 배치 펼침이 **`tallyVerified` 헬퍼 하나를 공유**한다.

---

## 5. Stats byWorker `~` 폴백 병합 (커밋 `e75ef52`)

같은 사람이 `Ian Lee` / `~Ian Lee` 두 줄로 나오던 것 → **실명 행에 접어 넣되
`(+N lines · N units attributed)` 로 분리 표기**. 막대는 정확분 진하게 + 추정분 연하게.

⚠️ **한 숫자로 합치지 않는다** — 「실제 작업자」와 「추정(시작자)」이 섞이면 PO-01131 형 거짓
(들어갔다 나온 사람이 62라인·3,570유닛의 작업자로 표시)이 부활한다.

- 병합은 **렌더 단계**에서 — 집계는 두 키로 분리 유지.
- `RECV_FB_PREFIX="~"` 상수로 생성부·파싱부를 묶었다(한쪽만 바뀌면 갈라진다).
- ⚠️ **`putaway%` 분모는 정확분만** — 추정 라인을 섞으면 비율이 거짓. `avg work time` 은 폴백 그룹에서
  애초에 계산되지 않는다(「없는 값을 만들지 않는다」).
- 실명 실적이 0인 사람도 **행은 하나**(`0 (+51 attributed)`).
- 📌 히스토리 열의 `~이름 (started by)` 는 그대로 — receipt 단위라 거기선 분리 표기가 옳다.

📌 **원인은 08-17 커밋 `64afcb2`**(리시빙 작업자를 라인 단위로). 결함이 아니라 **의도된 구분**이었고,
문제는 **화면에 그 의미를 설명하는 곳이 없었다**는 것이다.

---

## 6. 원장 — 하루치 회귀 검증 통과

발주 `candidates 20→29 · ledger_rows 829→1,164` · 판매 `145→219`(캡 40 고정이라 `ledger_rows 563` 동일) ·
반품 `4→5 · rows 2→6`. **경고 0 · 미지 어휘 0 · `UNMAPPED` 0 · `merged_lines` 0 — 이틀 연속.**

📌 발주의 `sr AUTHORISED` 가 1→0 이 된 것은 **PO-01112 가 Advanced 로 Convert 됐기 때문**이다
(전날 Simple 로 Apply → 다음 날 Convert). PA 카운트 15→25 와 짝이다 — 축을 갈아탄 것이지 유실이 아니다.

⚠️ **판매 캡이 계속 벌어진다** — 145→219. dry 는 커서를 안 쓰므로 늘 앞 40건만 본다. ⑤에서 커서가
돌기 시작하면 **초기에 밀린 것을 소화하는 구간**이 생긴다(5분 주기면 결국 따라잡지만, 그동안 원장이
현실보다 뒤처져 보인다 — 대조를 그 구간에 돌리면 오해한다).

~~⑤ 게이트 현황은 어제와 동일(오늘 안 바뀌었다)~~ → ⚠️ **이 문서를 쓴 뒤 같은 날 저녁~밤에
⑤ 가동까지 갔다** — 게이트 6개 중 5개가 그날 바뀌었다. 원장 쪽 진행은 이 문서가 아니라
**`docs/sessions/2026-08-20-ledger-go-live.md`** 가 정본이다(주제가 달라 문서를 나눴다).
현행 표기(정본은 설계 4부):
☑1 · 👁2 · ☑3 · 🟡4 ON CONFLICT(코드 완료 — 실동작 미검증) · ☑5 · **☑6 커서 seed** ⇒ **전부 통과.**

---

## 7. 브랜치 변경 대응 — 조사만, 미착수

⚠️ **실사고 근거가 없어졌다**(§1 기각 가설 (a)) — 「일어날 수 있는 일」에 대한 대비다.

- **현재**: `hello` 는 창고 변경을 **아예 안 본다**. Updated 트리거 재조회가 상세를 통째로 받아놓고도
  `d.Location` 을 버린다 ⇒ 「조용히 덮어쓴다」가 아니라 **「바뀌어도 영원히 모른다」**.
- ⚠️ 라인 쪽이 더 나쁘다 — `assembleLine` 이 `bin_location`·`zone` 을 유입 창고 기준으로 **동결**한다.
- **방향(확정)**: **hold 패턴 재사용**(`hold_state='on_hold'` + 사유 컬럼) — 화면 6지점이 무수정으로 작동.
- ⚠️ hold 와 다른 점: **시간이 지나도 안 풀린다** — 사람이 「옛 창고에서 픽한 물건」을 판단해야 한다.
- ⚠️⚠️ **`warehouse` 자동 덮어쓰기는 금지가 결론** — 진행 중 배치가 화면에서 소리 없이 증발한다.
- ⬜ **착수 전 관문(미실측)**: **브랜치 변경이 `saleList` 의 `Updated` 를 올리는가.**

상세는 `asung-wms` 스킬 규칙 43 아래 「Cin7 브랜치(창고) 변경 대응」이 정본.

---

## 8. 다음에 할 것

1. ~~**④ 기초 스냅샷 (8/22 예정)**~~ → ✅ **같은 날 저녁 완료**(8/20 로 앞당김 — 창고가 멈춘
   시각이면 조건이 같다)
2. ~~**⑤ `commit=1` + pg_cron**~~ → ✅ **같은 날 밤 완료**(커서 seed → 수동 1회 → pg_cron 6잡)
   ⇒ 1·2 의 실측·판단은 **`docs/sessions/2026-08-20-ledger-go-live.md`** 가 정본. 다음은 **⑥ shadow 대조**.
3. **Advanced PO 기본값** — PO 1건 수동 `Convert` 후 Apply 로 경로 검증 (⬜ 그대로 열려 있다)

**별건**: 브랜치 변경 대응(관문 프로브 먼저) · `product-images` cron 결과물 확인 ·
`hello` `DETAIL_DELAY_MS=250ms`(분당 240콜) · `asung-ops` 이관 · `Applied` 배지 검증 대기 ·
되돌림 트랜스퍼(자리 단위 때) · 조직 이전(버스팩터)

⬜ **오늘 배포분 현장 확인**: Jan Ko 계정으로 Rollback·Receiving 이 Edmonton 만 보이는지(+ void 박스에
toronto 번호 입력 시 차단) · Finalized 창고 세그 전환 시 카드·「N of M」이 함께 움직이는지 ·
Stats 에서 `~이름` 이 사라지고 `(+N attributed)` 로 보이는지.
