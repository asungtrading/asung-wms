# 재고 원장 설계 — 이벤트 목록과 스키마

작성 2026-08-16 · 프로브 1~22차 + WMS SQL 실측 기반
갱신 2026-08-17 — ②-b 세션 반영(정본 기록: `docs/sessions/2026-08-17-ledger-02b.md`).
갱신 2026-08-18 — **발주 축 재설계 반영**(Advanced=`PutAway` · 목록 게이트 제거 · line_ref=`CardID` —
정본 기록: `docs/sessions/2026-08-18-purchase-putaway-axis.md` · `2026-08-18-purchase-source-redesign.md`).
⚠️ 어제의 「−90행 = 유령 재고 차단」 판정은 무효가 됐다(1부 발주 절).
**명세 전제 4개 중 3개가 실측에서 깨졌다**(발주 블록 상태 어휘 · 반품 목록 행 단위 · 빈 Restock).
해당 절에 정정으로 반영했다 — 공통 교훈: **표본 하나에서 만든 규칙은 깨진다. 블랙리스트는
처음 보는 값을 조용히 통과시킨다 → 화이트리스트 + 미지값 경고가 정답.**

갱신 2026-08-20 — **④ 기초 스냅샷 완료 · ⑤ 쓰기 가동 · pg_cron 6잡 등록**
(정본 기록: `docs/sessions/2026-08-20-ledger-go-live.md`).
⑤ 진입 게이트 6개가 **전부 통과**했다 → 단계는 **⑥ shadow 대조**로 넘어간다.

갱신 2026-08-24 — **✅ ⑥ shadow 대조 가동**(RPC + cron 잡 12·13) · **첫 회차에서 결함 1건 검출**
(`FINAL-SALE` 비재고 SKU — 게이트로 처방 + 상쇄 행) · ⚠️ **스냅샷이 조용히 불완전할 수 있음을 발견
(급감 검사 미구현 — 최우선 미해결)**. 정본 기록: `docs/sessions/2026-08-24-shadow-compare-live.md`.

갱신 2026-08-25 — ⚠️ **⑥ 대조가 원장의 구조적 결함을 잡았다**: `TR-04175` 가 수집 후 Cin7 에서
138줄이 삭제됐는데 원장은 append-only 라 알 방법이 없어 토론토가 이중 차감(unknown 138).
⇒ 상쇄 276행 정정 + **라인 소멸 감지**(`inv_missing_lines` · `20260825142042`) 신설.
⚠️ **`inv_conflicts` 는 「변경」 감지지 「소멸」 감지가 아니다** — 삭제된 라인은 재수집 세트에
오지 않으므로 비교 대상 자체가 없다. 정본 기록: `docs/sessions/2026-08-25-transfer-line-deletion.md`.

갱신 2026-08-26 — ✅ **어제 건 완결**(`unknown 0` — 30일 관찰 1일차) ·
✅ **스냅샷 회차 로그 신설**(`inv_snapshot_runs` · `20260826130408`) — §4-c 처방의 **선행 조건 충족**
(판정은 아직 없다 · 아래 (c) 절). · 📌 **방향 문서 신설: `docs/design/ims-principles.md`**
— 모든 모듈의 상위 문서(Cin7 독립 · 모듈 레고화). ⚠️ 이 문서와 충돌하면 그쪽이 이긴다.

갱신 2026-08-31 — ⚠️ **커서 결함 3연쇄**(동결 C · 이중 차감 · 캡 누락 D)를 겪고 고쳤다.
정본 기록: `docs/sessions/2026-08-31-cursor-defects.md`.
⚠️ 그중 **`ShipmentDate` 는 재고가 빠진 시각이 아니다**([실측 `SO-15041`] 출하 승인 8/20 16:29
vs API 8/21) — 기준선 스냅샷 경계에서 **이중 차감**이 났다. 재기준선을 잡을 때마다 걸린다.

갱신 2026-08-31(오후 후반) — 트랜스퍼 출발 bin 을 WMS 픽에서 해결.
⚠️ **Cin7 API 에는 없다는 것을 전수로 확인**했다(라인 세 경로 · 공식 문서 · 엔드포인트 102개 ·
VOID 된 SO). 정본: `docs/sessions/2026-08-31-transfer-departure-bin.md`.
⭐ 소멸 감지에 **bin 변경 예외**를 넣었다 — 키 변경 시 원장 전체가 「삭제됨」으로 잡히는
알려진 위험의 처방이다.

갱신 2026-09-01 — 아침 점검이 셋을 잡았다: ⚠️ 기초 경계 오더 재유입(3문서 157행) ·
⚠️ **「수집 후 취소」 구조적 구멍**(`FG-00131` — ⚠️ VOID 다수는 Cin7 의 기계적 동작이고
관측 빈도는 8월 12건 중 1건이다) ·
트랜스퍼 출발 bin 을 **잔고 규칙 우선**으로 전환(`SKL01861` — WMS 값이 유입 시점이라 낡았다).
정본: `docs/sessions/2026-09-01-morning-check.md`.
⭐ 그리고 08-30 의 tie-breaker 가 8/31 오후 대량 갱신 때 **실제로 작동**했다(커서에 `|SO-…`).

갱신 2026-09-01(오후) — **재고 마스터의 원장측**을 만들었다(RPC · 어긋남 일지 · 코멘트 ·
「확인됨」 축). ⭐ 방향 전환: **화면은 검증의 보상이 아니라 검증 도구**다 —
「깨끗해지면 연다」에는 끝이 없고, 그날 찾은 것 중 둘은 사람이 이미 아는 일이었다.
⚠️ **대조의 시점 컷오프를 세 번 고쳐서 맞췄다**(§대조의 시점 컷오프). 정본:
`docs/sessions/2026-09-01-stock-master.md`.

갱신 2026-09-02 — ⭐ **결함 E**(커서 아래 문서의 편집을 못 본다)를 잡고 고쳤다.
실사고 `ST-01283` — 완제품 3행이 재료 12행으로 통째로 바뀌었는데 원장이 이틀간 몰랐다.
⚠️ 그리고 **소멸 감지가 못 보는 축이 둘**(`adjustment`·`assembly`)이라는 것이 드러났다 —
목록에 last-modified 가 없다. 정본: `docs/sessions/2026-09-02-defect-e.md`.
📌 ⭐ **어제 만든 `bin` 단위 대조가 이 둘을 다 잡았다** — 창고 단위로는 안 보였다.

갱신 2026-09-03 — ⭐ **처음으로 `new_today 0` · `unack 0`** 인 아침(8/20 기준선 이후 처음).
소멸 감지 사각지대 **1단계**(판정은 하되 표에 안 넣는다) · `ledger_collected_at` 을 화면
계약에 올렸다. ⚠️ **「G4 조건만 완화」가 안 되는 이유**(유니크 키)를 §B-1 에 박았다.
정본: `docs/sessions/2026-09-03-clean-morning.md`.

갱신 2026-09-05 — 재고 마스터 RPC 둘. ① `inv_stock_master` 성능 수리(`union all` 두 갈래를
`full outer join` 하나로 — 뷰가 인라인되어 각 2회 실행되던 것을 1회로 · **3,891 → 496 ms**)
+ `p_sku_exact` 추가(옛 6-인자 `drop`). ② **`inv_stock_master_sku` 신설**(SKU 층 · 537 ms) —
⚠️ 필터를 집계 전/후로 나눠 **거울 칸**을 지킨다(`diff` 합이 0이어도 `diff_bins > 0`).

갱신 2026-09-06 — **이력 RPC `inv_bin_history`**(칸·SKU 의 사건 목록 · 실측 5 ms).
⭐ `reason` 이 모든 사건에 붙는다(`cin7` 은 `raw.rule` 100% · `manual` 은 `raw.detail`) ·
`running_qty` 는 `baseline` 에서 **누적 후 페이징** · `fix_kind` 는 `line_ref` 접미어.
⚠️ 계약에 박은 것 둘: **`reason` 이 null 일 수 있다**(`:binfix` 592행 — 정상값) ·
**`occurred_on ≠ created_at`**(두 축이 며칠씩 갈린다 — 그 차이가 진단 정보다).

갱신 2026-09-07 — **재기준선 절차**를 한 자리에 모았다(§기준선 「재기준선 절차」).
조각은 `cron.sql` 주석 · `inv-snapshot` 코드 · `inv_snap_balance_diffs` 본문에 흩어져 있었고
**순서로는 어디에도 없었다.** ⚠️ 새로 드러난 것 둘: **촬영 시각은 그날 마지막 사건 이후**
여야 한다(`since` 가 엄격히 큼이라 촬영일 전체가 제외된다 — 8/20 이 안전했던 것은 19:42
촬영이라는 **그날의 조건**이었다) · **커서 seed 는 재기준선에 불필요**하다(첫 가동에만).
⬜ 아직 한 번도 실행하지 않은 절차다.

갱신 2026-09-07(오후) — **테스트 DB 방식 결정**(§4단계 「테스트 DB」) ·
**플립 이관 항목 등록**(§4부). ⭐ **별도 Supabase 프로젝트 · Cin7 미연결 + 운영 데이터
복사** — 착수는 **PO + 원가 시점**이다(그전엔 테스트 DB 가 놀고 복사본은 즉시 낡는다).
⚠️ 그리고 이 논의가 드러낸 것: **테스트 DB 는 버리는 물건이 아니라 내일의 운영**이고,
⇒ **진짜 시험은 로직이 아니라 이관**이다(미출고 오더 · 열린 PO · 백오더 · IN_TRANSIT ·
❌ 원가 레이어는 넘길 수 없다).

갱신 2026-09-07(저녁) — **가용(Available) 판단**(§재고 마스터 계약) ·
**SO 모듈이 받아야 할 것**(§4부 「플립 — 이관」). ❌ **`inv_stock_master` 에 `available` 을
내지 않는다** — `Allocated` 는 **주문 서밋**에서 생기는데 원장은 안 보고(Ship 에서 차감한다)
WMS 는 일부만 안다. ⇒ WMS 기준 계산은 **과소집계**다. ⭐ 그 원천은 **SO 모듈**이 서야 생긴다.
⚠️ **오버셀은 정책이다**(프리오더 · 수요 파악) — 「팔 수 있는」이 아니라 **「보낼 수 있는」**
으로 쓴다. ⚠️ 용어: **「로드맵 N단계」로 부르지 않는다** — 이 문서의 「N단계」는 원장 안의
순서이고 IMS 전체 로드맵은 이 문서에 없다(⬜ 어디에 둘지 별도로 정한다).

갱신 2026-09-08 — ✅ **테스트 DB 구축 완료**(`Asung-IMS` · ref `fazgmyvzzhqybtvtktyg`) ·
**절차 확정**(§4단계 「테스트 DB」) · ⚠️⚠️ **프로젝트 구분 규칙 신설**.
⭐ 세 단계로 만든다: 운영에서 `db dump --data-only --schema public`(로그성 12개 제외) →
테스트에 `db push --db-url` → `psql -f` 로 복원. **cron 잡 0 · EF 미배포**가
「Cin7 미연결」의 실체다. ⚠️ 함정 넷: **IPv6 불가**(Session pooler 5432) · **Docker 필요** ·
**URL 은 파일에 담는다** · **`inv_config` 중복 키**(재복사 시 먼저 비운다).
⚠️⚠️ 그리고 **이제 프로젝트가 둘이다** — `--db-url` 이 테스트, 없으면 운영이다.
**헷갈리면 실제 재고가 틀어진다.**

갱신 2026-09-08(오후) — **원가 레이어 절 신설** — `inv_layer` 3표 + 기초 적재 RPC · IN_TRANSIT 음수 예외.

갱신 2026-09-09 — **`inv-cost` 수집 계약 절 신설**(§4단계 「inv-cost 수집 계약」) — Simple Purchase 경로
(`StockReceived.Lines` 출처 · `raw.axis` · (b′)(c′) · `skip_not_received` · 환율 우선/폴백/null ·
`?recheck_since`). 표본 `PO-01215` 47행 · 정본 `docs/sessions/2026-09-09-simple-purchase-cost.md`.

갱신 2026-09-09(저녁) — **상쇄(정정) 계약 절 신설**(§4단계 「상쇄(정정) 계약」) — 방식 둘의 선택 기준 ·
`source` 필터 금지 · `line_ref` 키 금지 · `(doc_number, sku, warehouse)` 순액. 오진 둘(`source='cin7'` · `line_ref` 키)의
경위와 상쇄 전수 실측은 정본 `docs/sessions/2026-09-09-reversal-conventions.md`.

갱신 2026-09-09(밤) — ⭐⭐ **사건 9종 완료 · 잔량 대조 첫 통과**(§원가 레이어 12번 신설 · 미결 → 13번 · 실무 흐름 → 14번) ·
IN_TRANSIT 결함 둘(문서 범위 · 날짜 범위) · 테스트 DB 재복사 절차(비우는 단계 ②′) ·
⚠️ **정정: Cin7 도 FIFO 다**(13번 미결 · 시드 마이그레이션 헤더 · 스킬) · §9 에 「전제가 풀렸다」 한 줄.

레포 경로: `docs/design/ledger-design.md`
(마이그레이션 `20260816000000_inv_ledger_tables.sql` 이 이 문서를 참조한다)

---

## 이 문서의 전제

원장은 **재고 수량의 변동을 사건 단위로 쌓는 장부**다. "지금 몇 개"를 저장하지 않고 "무슨 일이 있었다"만 쌓아서, 현재 재고는 그것들을 더해 구한다.

1단계 범위는 **수량만·창고 단위**다. 원가는 값을 받아 저장하되 계산은 하지 않는다. 자리(빈)는 기록하되 잔고 계산에는 쓰지 않는다. **둘 다 나중에 소급이 불가능하기 때문에 지금부터 담는다.**

기준은 **Cin7 문서**다. WMS 는 보조다 — WMS 를 안 거치는 재고 이동이 한 달에 수백 건 있어서, WMS 를 기준으로 삼으면 처음부터 구멍이 있는 원장이 된다.

📌 **자체 IMS 설계 원칙 (2026-08-18 승격) — Cin7 의 목록/상세 2단 구조를 자체 API 에 복제하지
말 것.** 이날 캡·페이싱·커서·429 로 씨름한 것의 상당 부분은 **목록이 요약만 주고 라인을 보려면
문서마다 상세를 또 불러야 하는 구조** 때문이다(발주 40건 = 41콜). 이것은 **남의 제약이지
우리 설계가 아니다.** 자체 IMS 의 API 는 호출자가 필요한 데이터를 한 번에 받게 설계한다.
같은 이유로, 변경 감지는 **폴링이 아니라 이벤트(webhook)** 를 기본으로 둔다 — 지금 `hello` 가
5분마다 묻는 것은 Cin7 이 알려주지 않기 때문이지 그것이 옳은 설계여서가 아니다.
⚠️ 다만 **자체 API 에도 한도는 둔다** — 잘못 짠 클라이언트 하나가 시스템 전체를 죽인다.
차이는 그 선을 우리가 정한다는 것뿐이다(내부는 넉넉히, 외부 파트너는 보수적으로).

---

## 1부. 이벤트 목록

### 재고를 움직이는 사건 (8종)

| # | 사건 | 어느 문서에서 | 부호 | 날짜 |
|---|------|-------------|------|------|
| 1 | 판매 출고 | 판매 문서의 픽 라인 | 빼기 | 배송일 |
| 2 | 반품 입고 | 판매 문서의 크레딧 노트 안 재입고 목록 | 더하기 | 크레딧 노트 날짜 |
| 3 | 발주 입고 | 발주 문서의 입고 기록 | 더하기 | 입고 라인의 날짜 |
| 4 | 창고이동 출발 | 이동 문서 | 빼기 (출발 창고) | 출발일 |
| 5 | 창고이동 도착 | 같은 문서 | 더하기 (도착 창고) | 완료일 |
| 6 | 조정 — 기존 재고 | 조정 문서의 기존 재고 목록 | 목표수량 − 당시수량 | 실효일 |
| 7 | 조정 — 신규 재고 | 조정 문서의 신규 재고 목록 | 더하기 (수량 전량) | 실효일 |
| 8 | 조립 | 조립 문서 | 구성품 빼기 + 완제품 더하기 | 완료일 |

### 각 사건의 주의사항

**1. 판매 출고** — 픽·팩·Finalize 단계에서는 재고가 안 빠진다. 배송 처리된 것만 기록한다. 배송일이 문서에 있고, 그 날짜가 Cin7 이 실제로 재고를 움직인 날과 정확히 일치하는 것을 세 건 전수로 확인했다.

⚠️ **수량은 `Pick.Lines` 기준인데, 이것은 검증된 선택이 아니라 선택의 여지가 없었던 것이다**
(2026-08-17 성격 확정): **`Ship.Lines` 에는 SKU·수량 필드가 아예 없다**(3차 프로브 실측 —
`ID·ShipmentDate·Carrier·Boxes·TrackingNumber·TrackingURL·IsShipped` 뿐). 부분 선적이 실재하면
원장이 과다 차감되지만 **Cin7 API 로는 탐지 불가** — 발견 경로는 대조 단계의 Movements 검산
(SKU 단위)뿐이다. **감수하는 구조적 한계**로 둔다. 부수 근거(13차): SO-14532 Order 4·Pick 4·
Movements 4 / SO-14545 3·3·3 — 단 둘 다 단일 fulfilment·전량 출고라 부분 선적의 표본이 아니다.

**2. 반품 입고** — 크레딧 노트에 재입고 목록이 붙어 있을 때만 재고가 돌아온다. 목록이 비어 있으면 금액만 조정한 것이라 원장 대상이 아니다. 한 오더에 크레딧 노트가 여러 개 붙을 수 있으므로 전부 훑어야 한다.

⚠️ **정정 2건 (2026-08-17 실측)**:
- **"승인 상태면 재입고 목록이 차 있다"는 표본 하나(CR-00024)의 일반화였다** — 승인(AUTHORISED)
  인데 `Restock[]` 이 빈 크레딧 노트가 실재한다(2~4건 · 반품 누락인지 금액만 처리인지 실물 확인
  미완 — 검증 단계 항목). 판정은 상태값이 아니라 **`Restock[]` 실제 길이**가 안전하다.
- **크레딧 노트 목록(`saleCreditNoteList`)의 행은 CN 단위가 아니라 sale 단위다** — 행의
  `RestockStatus` 단일값으로 오더를 통째로 거르면 **같은 오더 안의 승인된 CN 이 함께 조용히
  유실된다**([실측] 목록 단계 필터 제거로 후보 60→124 · 상세 40건에서 CN 54개). 상태 판정
  권한은 상세의 `CreditNotes[]` 순회 **한 곳**에만 둔다. 원장 문서번호는 오더번호가 아니라
  **CreditNoteNumber**(다중 CN 이 유니크 키를 깬다).

두 정정의 직접 근거가 **`SO-00062`** 다 — CN 이 둘이고 `RestockStatus` 가 각각 달랐다:
`CR-00024` = DRAFT · Restock 0줄 / `CR-00007` = AUTHORISED · Restock 2줄.
⚠️ 그 오더의 **목록 행** `RestockStatus` 가 무엇이었는지는 미확인이다(첫 번째 CN 값인지
대표값인지 합성인지 모른다) — 목록 값으로 거르면 안 되는 이유가 하나 더 있는 셈이다.

**3. 발주 입고** — 날짜는 **입고 기록의 라인마다 붙어 있다.** 헤더가 아니라 라인이라 더 정확하다 — 한 발주를 여러 번 나눠 받으면 각각의 날짜가 따로 잡힌다. 네 건 전수로 확인했다.

⚠️ 주문일·인보이스 날짜·최종수정일은 전부 어긋난다. 그중 하나가 우연히 맞는 경우가 있어 **한 건만 보고 판단하면 안 된다.**

⚠️⚠️ **축 (2026-08-18 재설계 — `docs/sessions/2026-08-18-purchase-putaway-axis.md`·
`2026-08-18-purchase-source-redesign.md` 가 근거): Advanced = `PutAway` / Simple = `StockReceived`.**
목록 `Type` 으로 가른다(엔드포인트 분기와 동일 기준 — `purchase?ID=` 는 Advanced 에 400
deprecated 를 반환한다: `PO-00069`. Simple 근거: `PO-00874` — SR 객체·`LocationID` 있음.
**두 번째 실측 = PO-01117 (2026-08-18)**: Apply 직후 Simple SR 51줄 전부 `LocationID` 보유,
같은 빈에 여러 SKU 공존(`eedb7d88` 빈에 4개 SKU) — Simple=SR 축에서 bin 이 실제로 얻어진다).

**SR 을 쓰면 안 되는 이유 둘** (2026-08-17 까지의 SR 축 구현이 이래서 틀렸다):
① SR 라인의 `LocationID`·`Location` 은 null 이거나, 값이 있어도 **bin 이 아니라 창고 GUID** 다 —
  창고 판정이 `UNMAPPED(no-id)` 또는 bin 없음을 낸다. **bin 이 구조적으로 안 얻어진다.**
② SR 의 `Status` 는 **stock receiving 단계의 워크플로 상태**이지 재고 반영 여부가 아니다 —
  PO-00703 SR=DRAFT/PA=AUTHORISED 62줄 `FULLY RECEIVED` · PO-01131 SR=NOT AVAILABLE/
  PA=AUTHORISED 3,570u · PO-01128 SR=""/PA=AUTHORISED 84줄.

SR·PA 는 **같은 입고의 두 표현**이다(15건 표본 `srLines==paLines`) — **둘 다 읽으면 정확히 두 배.**

**`Convert` 는 라인을 재생성하지 않고 SR → PA 로 옮긴다** [실측 2026-08-18 PO-01117 51줄].
Convert 직후 SR 블록은 **빈 상태(`""`) · 0줄**로 남고 PA 블록에 51줄이 그대로 들어간다
(`TaskID` 는 양쪽 동일). 📌 이것이 PO-01128 에서 본 `SR=[""]` 패턴의 정체다 — 빈 상태 SR 은
"이상한 문서"가 아니라 **Convert 를 거친 문서의 정상 흔적**이다.
그리고 **PA 블록은 `DRAFT` 로 생성된 뒤 승인된다** — 진행 중 상태가 존재한다.

**블록 상태 화이트리스트 = 두 축 모두 `AUTHORISED` 만** + 어휘 밖 값 경고.
~~"빈 문자열도 통과"(2026-08-17)~~ 는 삭제됐다 — 근거였던 PO-01128 은 Advanced 였고 빈 상태는
**SR 블록**이었다(PA 는 AUTHORISED). PA 축에선 근거가 소멸한다.
~~"DRAFT 만 제외"~~ 도 ~~"AUTHORISED·빈 문자열 통과(SR 기준)"~~ 도 전부 **어느 배열의 상태를
보느냐**가 틀렸던 것이다 — 아래 「−90행 판정 무효」 참조.

**목록 게이트 없음**: `StockReceivedStatus` 로 문서를 거르지 않는다. 목록 값과 상세 블록 값은
**양방향으로 어긋난다**(PO-01131 목록 AUTHORISED/상세 NOT AVAILABLE ↔ PO-00848 목록
NOT AVAILABLE/상세 AUTHORISED) = 상관이 없다. 표본 6건 중 Advanced 4건(PO-00848·00931·01048·
01065, 12,552u)이 실재 입고인데 문서째 지워지고 있었다. **판정 권한은 상세 한 곳**(반품 .7 의
목록 필터 제거와 같은 계열). 목록 상태 2종은 분포만 센다(관측 전용).

**Advanced 인데 PA 블록 없음** = 선반 미배치 — 행을 만들지 않고 보고만 한다. **커서는 안
멈춘다**(증분 축엔 hold 기계가 없고, 세우면 TR-00012~76 형태로 발주 축이 영구 동결).
~~⚠️ "put-away 가 문서를 갱신해 재등장한다"는 전제는 **미확인**이다.~~ → ✅ **닫힘 (2026-08-19 실측
— 게이트 3번)**: PO-01117 이 PA 승인 후 같은 창에서 **+51행**으로 들어왔다(4부 체크리스트 ☑3).

⚠️ **`Type` 은 가변이지만 전환은 자동이 아니다** — PO 는 Simple 로 발행 → 입고 → WMS Apply →
**사람이 Cin7 화면에서 `Convert` 를 눌러야** Advanced 가 된다. 트리거는 시간이 아니라 사건이다.
[실측 2026-08-18 PO-01117] Apply 후 31분이 지나도 Simple 그대로였고(`LastUpdatedDate` 도 불변),
`Convert` 를 누르는 즉시 Advanced 가 됐다. ~~"Apply 후 10분 안팎 자동 전환(12/12)"~~ 은
**Convert 가 보통 10분 안에 이뤄졌던 것**을 자동으로 오인한 서술이다.
그래서 `simple_docs: 0` 은 이상이 아니고, 반대로 `simple_docs > 0` 도 이상이 아니다
(아직 Convert 안 된 PO). ⚠️ 전환이 `LastUpdatedDate` 를 갱신하므로 **모든 PO 가 최소 두 번
`UpdatedSince` 에 잡힌다**(중복 무시 정책의 수정 미반영 문제가 가설이 아니라 일상이라는 뜻).

📌 종전 서술 정정: ~~"`PO-00427` 은 두 엔드포인트로 형태가 갈린다"~~ 는 **위험 과장**이었다 —
목록 `Type` 이 확정이므로 수집은 항상 한쪽으로만 부른다.
📌 ~~"최근 40건 전부 Advanced — Simple 분기 미실행"~~ 도 닫혔다 — 목록 게이트 제거 후
`simple_docs: 7` 로 실행됐고 정상 동작(SR 블록 NOT AVAILABLE 을 올바르게 배제).

✅ **Simple 분기가 실제로 행을 만들었다 (2026-08-19 PO-01112).** 종전에는 `simple_docs > 0` 이어도
SR 블록이 전부 `NOT AVAILABLE` 이라 걸러졌을 뿐, **Simple 축이 원장 행을 만든 적은 없었다.**
PO-01112 가 13:34 토론토에 Simple 로 Apply 되면서 `sr_block_status_counts={AUTHORISED:1,
NOT AVAILABLE:5}` 가 되었고 **102행**이 들어왔다(같은 창 `ledger_rows` 727 → 829).
⇒ **Simple=SR 축 결정이 실동작으로 검증됐다.**
📌 이 +102 는 코드 변경이 아니라 **데이터 변화**였다 — `candidates`·`docs_processed` 가 20 으로
동일한데 행만 늘어 처음에 감지 로직을 의심했으나, `wms_receipts.applied_at` 조회로 확인됐다.

⚠️⚠️ **「−90행 = 유령 재고 차단」(2026-08-17 SR 화이트리스트 도입 효과) 판정은 무효다.**
SR 상태는 워크플로 중간 상태라 그 −90행은 **실재 입고를 지운 것**이었다(PO-00703·PO-01131).
📌 단 **화이트리스트 방향 자체는 옳았다** — 블랙리스트는 미지값을 조용히 통과시킨다. 틀린 것은
**어느 배열의 상태를 보느냐**였다. 이 구분을 지키지 않고 방향까지 뒤집으면 블랙리스트로
되돌아간다.

[실측 2026-08-18 배포 전/후 dry — 같은 창 `from_since=2026-08-17` · 캡 없음 15건 전량]
`ledger_rows` 383 → **643(+68%)** · `warehouse` UNMAPPED(no-id) 단일 → `Asung Trading Inc.` ·
`bin` "" 단일 → C040101·J01PALLET07 등 실제 선반 · 블록 상태 SR 4종 혼재 → **PA AUTHORISED 13
단일** · `merged_lines`·`card_id_fallback`·`adv_no_putaway` 전부 0.

**4·5. 창고 이동** — 한 문서가 원장 두 줄이 된다. 출발일과 도착일이 다르고(열흘 정도 벌어진다), 그 사이 물건은 어느 창고에도 없다. 아직 도착 안 한 이동은 출발 줄만 만든다.

⚠️ 이동의 97%는 **같은 창고 안에서 자리만 옮기는 것**이다. 창고 단위 잔고로는 더하고 빼서 0이 되지만, **두 줄 다 기록한다** — 나중에 자리 단위로 올릴 때 필요하고, 그때 가서는 복구할 수 없다.

⚠️⚠️ **트랜스퍼의 bin 은 라인이 아니라 문서 헤더에 있다 (2026-08-19 실측 · GAS 프로브).**
`FromLocation`/`ToLocation` 이 **`"창고: bin"` 문자열**이다(목록·상세 양쪽). [실측] TR-02645:
`FromLocation="Asung - Edmonton: EZ01Pallet03"` · `ToLocation="Asung Trading Inc.: F0300PALLET01"`.
`From`/`To` GUID 는 창고가 아니라 **그 bin 의 GUID 로 보인다**(관찰 — 확정 아님).
📌 **문서 하나가 bin 하나 → bin 하나** 이므로 **라인에 bin 이 없는 것이 정상**이다. 발주처럼
라인마다 자리가 갈리는 구조가 아니다 — **트랜스퍼는 문서 하나 = 자리 하나가 업무의 성격**이고
(2026-08-19 Caleb), WMS 도 같다: 「토론토 여러 bin → Asung - Edmonton」으로 받아 **Apply 시
출발 bin 별로 트랜스퍼 문서를 쪼개 생성**한다(리시빙 Apply 의 bin 그룹 분할과 같은 패턴 ·
도착지는 창고 단위 지정). ⇒ 자리 단위 원장에서도 **문서 단위로 자리를 해석**하는 것이 맞다.

⚠️⚠️ **현재 원장은 이 값을 읽지 않는다** — `From`/`To` GUID 만 `resolveLoc` 에 넘겨 창고만
해석하고 bin 은 빈 값이 된다(dry 전량 3,172행의 `bins` 가 `[""]` 하나).
**데이터가 없는 게 아니라 안 읽고 있는 것이다.** 발주에서 「SR 이 아니라 PA」를 찾은 것과 같은 계열.
⇒ **자리 단위 원장으로 올릴 때 여기부터 고친다.** ⚠️ 다만 `FromLocation` 은 GUID 가 아니라
**문자열이라 `": "` 파싱 + 매핑 대조**가 필요하다(발주의 `LocationID` 처럼 깔끔하지 않다).
⚠️ `Lines[].ProductCustomField1/2` 에 비슷한 값이 보이나 **상품 마스터 필드이므로 쓰지 말 것**
(TR-02644 는 `ProductCustomField1=null`).
📌 **창고 단위 원장에서는 지금 그대로도 정확하다** — 3창고(토론토·에드먼튼·`IN_TRANSIT`)로
정확히 갈리고 4행 구조(출발 − / IN_TRANSIT + / IN_TRANSIT − / 도착 +)도 정상이다.

#### 되돌림 트랜스퍼 (2026-08-19 Caleb + Cin7 실물)

잘못 간 물건은 **WMS 를 거치지 않고 Cin7 에서 직접** 트랜스퍼 백으로 해결한다 —
에드먼튼 → 토론토로 `From`/`To` 를 반대로 설정한 새 트랜스퍼를 만든다.
✅ **bin 은 제대로 지정된다** — 실물 7건(`Search=error`, 전부 COMPLETED) 전부 `창고: bin` 형태로
출발·도착이 명시돼 있고, `Reference` 에 사유도 남긴다(「EDM Transfer Error」·
「Revised Transfer Error - TR-01126」). 📌 **사람이 `ToLocation` 에도 bin 을 찍는다.**
⇒ **실무 절차 자체는 정확하다.** 원장이 이 데이터를 안 읽고 있는 것이 문제였다(위 항목).
⚠️ 남는 질문: 이 되돌림이 **실물 이동이 없는 장부 조정**인 경우, 원장은 실제 이동과 구분하지
못한다. 창고 총량에는 영향이 없으나(왕복 상쇄) **자리 단위에서는 두 번의 이동으로 기록**된다.
그것이 옳은지는 자리 단위 설계 때 판단한다.

**6. 조정 — 기존 재고** — 문서에 적힌 값은 증감분이 아니라 **조정 후 목표 수량**이다. 실제 변동은 목표수량에서 당시수량을 뺀 값이다. Cin7 화면의 차이 값과 일치하는 것을 확인했다.

당시수량이 조정 시점 값으로 박제돼 있어서, 몇 달 전 조정도 정확히 계산할 수 있다.

**7. 조정 — 신규 재고** — 구조가 다르다. 당시수량이 없고 수량 하나만 있으며, 그게 그대로 증가분이다. **6번 규칙을 여기 적용하면 틀린다.** 단가가 함께 들어 있다.

**8. 조립** — 구성품 여러 줄이 빠지고 완제품 한 줄이 들어온다. 번들 판매를 하면 **오더가 만들어지는 시점에** Cin7 이 자동으로 조립 문서를 만들어 구성품을 차감한다. **그래서 우리가 번들 구성을 알 필요가 없다** — 조립 문서에 이미 다 들어 있다.

조립일과 출고일은 다르다. 조립은 오더 생성 때, 출고는 배송 때다. 그 사이 **번들이 재고로 존재하는 구간**이 생긴다(실측 0~15일, 대부분 0~3일).

⚠️ **조립은 다른 사건과 성격이 다르다.** 물건이 물리적으로 움직이는 게 아니라 창고 안에서 구성품 여러 개가 완제품 하나로 **재분류**되는 것이다. 잔고 계산에는 문제가 없지만, 실사할 때 장부에는 완제품 1개인데 선반에는 구성품 수십 개가 있어 보인다.

⚠️ **번들이 조립 없이 팔리는 경우도 있다.** 조정으로 재고를 잡아두고 파는 경우다(세 건 확인). 원장은 조정도 판매도 다 기록하므로 놓치지 않는다.

### ⭐ 미달·초과 입고의 실무 흐름 (2026-08-19 Caleb 확인 + 실물 ST-01220)

**원장이 발주·조정 두 소스를 함께 읽어야 하는 이유가 여기 있다.**

Cin7 은 **인보이스 수량과 입고 수량이 정확히 같아야** PO 를 닫을 수 있다. 그런데 실제 입고는
자주 어긋난다. Asung 은 이렇게 처리한다:

1. **WMS 는 실제 받은 수량으로 받는다** — 부족/초과를 `discrepancy` 로 걸러 기록한다.
   ⚠️ **"어차피 조정할 거니 인보이스 수량대로 받자"로 하지 않는 이유**: 그러면 **무엇이 부족했는지
   확인할 수 없다.** 사실을 사실대로 남기는 것이 우선이다.
2. **Cin7 에는 실제 수량을 쓴다** — 초과분은 인보이스 수량으로 클램프(`recv_over` 는 discrepancy 큐).
3. **PO 를 닫기 위해 PO 수량을 인보이스 수량으로 채운다** — 미달분이 남으면 I&R 이 계속 열려
   **PO 관리가 불가능**해지기 때문이다. ⚠️ 이것은 재고를 늘리려는 것이 아니라 **문서를 닫기 위한 형식**이다.
4. **차액을 stock adjustment 로 되돌린다** — 실물 예: `ST-01220`
   (`Reference: Initial Warehousing - Parfums de Coeur, Ltd. (PO-01117)`),
   `CAN01620` 201→177(−24, 미달) · `CAN01547` 503→527(+24, 초과)를 **한 문서에 함께** 담았다.

**⇒ 원장 관점**: 한 번의 입고가 **두 소스에 걸친 사건**을 만든다 — `po_in`(발주) + `adjustment`(조정).
⚠️ **Adjustment 는 증감분이 아니라 조정 후 수량(절대값)** 이므로 앞선 오차를 흡수한다.
그래서 **창고 총량은 자동으로 맞는다.**
⚠️ **그러나 bin 단위는 어긋난 채 남는다** — 조정이 걸린 bin(`E030501`)과 입고 bin(PA 의
`3d3c6295`)이 다를 수 있다. 자리 단위 원장으로 올릴 때 이 차이가 드러난다.

### 재고를 안 움직이는 것 (기록하지 않음)

원가만 바뀌는 사건이 여섯 종류 있다. 발주 원가 변경, 이동 원가 변경(들어오는 쪽·나가는 쪽), 조정 원가 변경, 조립 원가 변경, 판매 원가 변경. **수량이 전부 0이다.**

수량 원장에서는 무시한다. 원가 단계로 가면 그때 처리한다.

### 원장 대상이 아닌 것

- **아직 배송 안 된 판매** — 재고를 안 건드린다. WMS 에 없는 것도 정상이다
- **발주 잔량** — 주문했지만 안 온 물건
- **Production Facility 창고** — 삭제만 안 될 뿐 안 쓰는 창고다. 재고가 0인 것을 전수로 확인했다. ⚠️ 다만 여기 재고가 생기면 **경고**해야 한다
- **묶음 단위 상품** — 자체 재고를 갖지 않는다. 낱개 상품 재고에서 나눠 표시하는 값일 뿐이다. 원장은 낱개 상품만 기록한다

---

## 2부. 스키마 설계

### 테이블 다섯 개

#### ① 원장 본체

**한 줄이 재고 변동 하나**를 뜻한다. 한 번 쓰면 고치거나 지우지 않는다.

담는 것:

- **언제** — 사건이 일어난 날. 문서의 배송일·출발일·실효일 등을 쓴다. 우리가 처리한 시각이 아니다
- **순서** — 같은 날 같은 상품에 여러 사건이 겹칠 수 있다. 유입인지 유출인지를 값으로 저장해 정렬에 쓴다 (아래 「순서를 어떻게 보장하나」 참조)
- **무엇** — 상품 코드. 묶음 단위가 아니라 낱개 기준
- **어디** — 창고. 그리고 자리(빈)도 함께. 자리는 지금은 안 쓰지만 나중에 필요하다
- **얼마나** — 부호가 붙은 수량. 나가면 음수, 들어오면 양수
- **왜** — 사건 종류 (위 여덟 가지 중 하나)
- **근거** — 문서 종류, 문서 번호, 문서 내부 식별자, 라인 위치
- **금액** — 그 사건의 재고 가치 변동. 계산에는 안 쓰지만 저장한다
- **출처** — Cin7 문서에서 왔는지 WMS 에서 왔는지

**중복 방지**: 문서 종류 + 문서 번호 + 라인 위치 + 사건 종류를 묶어 유일하게 만든다. 같은 문서를 두 번 읽어도 두 번 쌓이지 않는다. 수집 배치가 실패해서 다시 돌려도 안전해야 하기 때문이다.

⚠️⚠️ **라인 위치(line_ref) 규칙 (2026-08-18 승격): 소스마다 그 문서가 제공하는 가장 안정적인
라인 식별자를 쓴다.** ~~"ProductID 로 소스 간 통일"~~ 은 폐기됐다 — 통일보다 정확성이 우선이다.
공통 원인은 하나: 유니크 키에 **날짜(occurred_on)가 없어서**, 같은 상품·같은 자리의 두 사건이
날짜만 다르면 키가 완전히 같아져 **두 번째가 조용히 사라진다.**

| 소스 | line_ref | 근거 |
|---|---|---|
| 판매 | `<fulfilment TaskID>:<ProductID>` | 분할 출하(8/10 5개 · 8/15 3개)가 뭉개진다. TaskID 는 GUID — 배열 인덱스는 void·재정렬에 흔들림([실측] 폴백 0회) |
| 발주 | **`CardID`** (2026-08-18) | PA 는 같은 SKU 를 **빈 단위로 쪼개** 여러 줄로 담고, **같은 빈·같은 SKU 가 날짜만 달라** 두 줄인 사례 실재(PO-00944 KUZ77036: 7/15 894 · 7/16 6). 유일성 실측 ProductID 94/97·109/110 vs CardID 97/97·110/110·62/62. [실측] `.7` 의 8/01 창 dry 에서 `merged_lines: 5` — **가설이 아니라 실제로 뭉개지고 있었다**(`.1` 에서 0) |
| 조정·이동·조립·반품 | `ProductID` | 뭉개짐 사례 미발견 — 나타나면 merged_lines 가 시끄럽게 잡는다(문서 내 합산 경보) |

✅ **[실측 2026-08-18 · 체크리스트 1번 닫힘] `CardID` 는 Convert 를 통과해도 유지된다.**
PO-01117 51줄 전부가 Apply 직후(SR) → Convert 후(PA) → 승인 후까지 동일한 `CardID` 를 유지했다
(`LocationID`·순서까지 동일). Convert 는 **재생성이 아니라 이동**이므로 이중 계상 위험은 없다.
⚠️ 남는 미확인: 수량 **수정** 시의 거동(4부 3번 `ON CONFLICT` 항목과 같은 축).
**이유 없이 ProductID 단독으로 되돌리면 분할 출하·빈 분할이 다시 뭉개진다 — 되돌리려는 사람은
이 문단을 먼저 반박할 것.**

⚠️ **원본 응답을 통째로 한 칸에 함께 저장한다.** 나중에 "이 줄이 왜 이렇게 됐지" 할 때 원본이 없으면 추적이 안 된다. 조사 과정에서 여러 번 겪은 일이다.

#### ② 기초 스냅샷

시작하는 날의 재고를 한 번 찍어 둔다. 약 1만 4천 줄이고 1분이면 받는다.

담는 것: 상품, 창고, 자리, 수량, 평가액, 찍은 시각.

⚠️ 조건이 단순하지 않다. **자리가 지정된 것은 자리별로, 자리 없이 떠 있는 재고는 그것대로** 담아야 한다. 자리 없는 재고가 11건 있는데, 자리 있는 것만 담으면 이 물량이 통째로 빠진다.

✅ **④ 기초 스냅샷 완료 (2026-08-20)** — ⚠️ **8/22 예정이었으나 8/20 저녁으로 앞당겼다**:
창고가 멈춘 시각이면 조건이 같기 때문이다(토요일을 기다릴 이유가 없었다).

```
snapshot_key : 2026-08-20-initial
taken_at     : 2026-08-20T23:42:47.855Z  (19:42:47 토론토)
13,844행 · 토론토 7,523 / 에드먼튼 6,321
소요 44초 · truncated false · rate_limited false · unexpected_warehouses []
```

**리허설을 먼저 했다** — 같은 날 낮 dry 로 41초·13,871행 확인. 실촬영과 **27행 차이**는 그사이
5시간의 실제 재고 변동이다(정상 — 리허설은 같은 값을 내야 하는 것이 아니라 **경로가 도는지**를 본다).

**DB 검증 일치** — `inv_snapshot` 의 행·SKU·수량·금액이 EF 응답과 동일.
📌 `bins` 만 SQL 쪽이 1씩 많다(506→507 · 1467→1468) — **빈 문자열 bin 이 `distinct` 에 세어진 것**이지
불일치가 아니다.

⚠️ **자리 미상 재고가 기준선에 포함된다** — `null_bin_nonzero` **15건**(수량은 있는데 bin 이 없음).
토론토 1,421개 · 에드먼튼 6개. 최대는 **`AS00879BLA` 1,200개 · $12,065**.
`AIA*` 7종은 전부 6개·$0.0831 로 같은 패턴(샘플·디스플레이 추정) · `SUN31504` 159개는 원가 $0.
⇒ 원장은 이를 **그대로 기록하고** ⑥ 대조에서 드러난다. **알고 시작한다.**
📌 ~~`null_bin_nonzero` 에 샘플 상한이 있는지 미확인~~ → **상한 없음 (코드 확인 2026-08-20)**:
`inv-snapshot/index.ts` 의 `nullBinNonzero` 는 조건 없이 `push` 되고 `slice` 되지 않는다
(응답 주석 "전체 목록"과 일치). **15건이 전량**이다.

⚠️ **`inv-snapshot` 은 `key` 파라미터가 필수다** — 자동 생성을 하지 않으며 **dry 에도 요구**한다.
인증은 `inv-collect` 와 같은 `WMS_CRON_SECRET`.

#### ③ 현재 잔고

기초 스냅샷에 원장을 더해서 나오는 값이다. **따로 저장하지 않고 필요할 때 계산**하는 것을 기본으로 한다.

⚠️ 다만 원장이 수만 줄을 넘어가면 매번 계산하는 게 느려진다. 그때는 "어느 시점까지 더한 결과"를 중간 저장하는 방식으로 바꾼다. **처음부터 그렇게 만들지는 않는다** — 없어도 되는 복잡도를 미리 넣으면 틀렸을 때 찾기 어려워진다.

#### ④ 대조 결과

매일 우리 원장과 Cin7 재고를 비교한 기록이다.

담는 것: 비교한 날, 상품, 창고, 우리 수량, Cin7 수량, 차이, 그리고 **차이의 분류**.

분류가 이 테이블의 핵심이다. 차이가 나는 이유는 셋 중 하나다 — 우리가 놓친 사건, 우리 계산 오류, 설명되는 정상 차이(운송 중이라든지). **설명되는 것을 자동으로 걸러내야** 사람이 봐야 할 것만 남는다.

#### ⑤ 수집 상태

어느 문서 목록을 어디까지 읽었는지 기록한다.

판매와 발주는 "언제 이후 갱신된 것"으로 증분 조회가 되는데, 조정·이동·조립은 그게 안 돼서 **매번 전량을 받아 우리 쪽에서 날짜로 걸러야** 한다. 어디까지 처리했는지 알아야 중복 없이 이어갈 수 있다.

**커서는 두 축이다 (2026-08-17 구현 확정)**:
- **문서번호 커서** (조정·이동·조립 — 전량 축): 비종결 문서(운송 중·미승인) 앞에서 멈춘다 —
  도착 후 회차가 나머지 두 줄을 만들려면 재방문이 필요해서다. 시야 하한(`from_cursor`)이
  스냅샷 이전 문서를 제외한다([실사고] 날짜 없는 초기 트랜스퍼 40건이 상세 캡을 정확히 소진해
  뒤 3,000건이 안 보였다 — 하한은 옛 데이터를 안 보는 것이지 이상 감지를 끄는 것이 아니다).
- **변경 없는 문서는 상세를 부르지 않는다** (2026-08-31 결함 D · `inv_doc_state`).
  문서별 「마지막으로 본 수정 시각」을 기록하고, 목록의 값이 같으면 skip 한다.
  [실측] 트랜스퍼 후보 159건 중 상세 조회가 **39 → 5** 로 줄었다.
  ⚠️ **비종결 문서는 절대 skip 하지 않는다**(도착 시 그 값이 바뀌는지 미확인).
  ⚠️ **skip 한 문서도 커서 전진에 포함**한다.
  📌 이것이 없으면 ②-a 는 **비종결 홀드 + 캡** 조합에서 뒤쪽 문서를 영영 못 읽는다
  (`TR-04330` −144 누락 실사고).
- **최근 7일 재조회 창** (2026-09-02 · 결함 E 처방).
  목록 행의 사건 날짜가 창 안이면 **커서 아래라도 후보에 남긴다.**
  ⚠️ 없으면 커서가 지나간 문서의 편집을 **영영 못 본다**(실사고 `ST-01283` — 완제품 3행이
  재료 12행으로 통째로 바뀌었는데 원장은 이틀간 옛 3행을 들고 있었다).
  ⚠️ **창 문서는 커서에 관여하지 않는다** — 전진값으로 쓰면 커서가 **후퇴**하고
  hold 로 세우면 위쪽 전진을 **막는다.**
  ⚠️ 날짜는 **목록에서** 얻는다(상세 전에 판정해야 한다). 못 얻으면 종전 동작 + 경고.
  📌 비용은 `inv_doc_state` 가 지운다. ②-b 는 해당 없다(`UpdatedSince` 라 편집되면
  목록에 다시 나온다).
- **날짜 커서** (판매·발주·반품 — 증분 축): `UpdatedSince = 커서 − 1일`(겹치게 받는다 — 경계
  유실 방지, 중복은 유니크 키가 흡수). 문서 상태로 멈추지 않는다 — 미완료 문서는 갱신되면
  다시 잡힌다. ⚠️ 커서는 시각 전체 정밀도로 저장하고 **거르기도
  우리 쪽에서 전체 정밀도로** 한다 — 날짜로 절단해 다시 요청만 하면 매 회차 같은 앞 40건만
  반복 처리하는 **완전히 조용한 정체**가 된다(삽입 0행 · 정상 응답 · 커서 동일값 갱신).
  갱신 시각 없는 문서가 캡을 채우면 커서가 동결되므로 그 회차는 쓰기를 차단한다
  (판매는 하루 40~90 오더라 캡 초과가 일상).
- **②-b 커서 = `<Updated>|<문서식별자>`** (2026-08-30 결함 C 이후). 비캡 회차는 회차 시작
  시각(맨 ISO), 캡 회차는 **마지막 처리 문서의 키**.
  ⚠️ **`Updated` 단독으로는 안 된다** — 동률 그룹이 캡보다 크면 커서가 그 안에서 끝나
  **영구 동결**된다([실사고] 밀리초까지 같은 판매 238건 · 하루 반 동결 · cron 은 매번
  succeeded). 캡을 올려도 안 된다(238×1200ms=286초 > 예산 120초).
  📌 **마이그레이션 불필요** — `Updated` 가 고정 길이 ISO 라 문자열 비교가 성립하고,
  기존 맨 ISO 커서는 「그 시각 동률 그룹 맨 앞」으로 해석된다(재처리 = 안전 방향).
  ⚠️ 정렬은 **코드유닛 비교**다 — `localeCompare` 는 `|` 취급이 ICU 에 좌우돼 필터의 `<` 와
  순서가 어긋날 수 있다.
  ⚠️ **증상 가드** `cursorStalled = detailCapped && wouldBe <= before` → 경고 + commit 차단.
  결함 A·B·C 가 전부 같은 증상이었고 원인별 가드는 매번 사촌을 놓쳤다.

### 날짜 커서 축과 이벤트 날짜는 분리돼 있다 (2026-08-17 실측 확정 — 스냅샷 경계와 직결)

`UpdatedSince` 는 **어떤 문서를 가져올까**의 축이고, 원장 행의 날짜는 **이벤트 날짜**(입고 라인
날짜 · 크레딧 노트 날짜 · 배송일)다. 8월에 갱신된 문서가 4월·6월 이벤트를 품고 있으면 **과거
날짜의 원장 행이 생긴다.**

[실측 2026-08-17 dry]

| 소스 | 커서 시작점 | 원장 행 날짜 분포 |
|---|---|---|
| 발주 | 2026-08-01 | 06-18 ~ 08-13 (6·7월이 대부분) |
| 반품 | 2026-07-01 | **2026-04-27 포함** |
| 판매 | 2026-08-14 | **90% 이상이 7월** · 08-14 이후 0 |

⚠️⚠️ **기초 스냅샷과 정면으로 부딪힌다.** 스냅샷은 그 시점 잔고를 담고 과거 이벤트는 이미 그
안에 녹아 있다 — 수집이 또 더하면 **원장이 조용히 부푼다.** 방어는 이벤트 날짜 필터(`since=`)다.
[실측 검증] 반품에 `since=2026-08-01` 을 걸자 원장 행 0 · 걸러진 행 15 · 날짜 분포 빈 것 =
정확히 상보적. ⇒ **쓰기를 켤 때(cron 등록) `since=<스냅샷 날짜>` 는 선택이 아니라 필수다.**
✅ **실행됨 (2026-08-20)** — cron 6잡 URL 전부에 `since=2026-08-20` 이 박혀 있다
(아래 「⑤ 쓰기 가동」 · `supabase/ops/cron.sql`). 📌 **스냅샷을 다시 찍으면 그 6줄을 손으로 갱신할 것.**

⚠️ **이름 혼동 위험**: `since` = 이벤트 날짜 필터(스냅샷 경계 방어 — 영구 필요) /
`from_since` = 갱신 커서의 시작점(저장된 커서가 없을 때만 쓰는 씨앗). cron URL 에서 하나를
빠뜨리거나 바꿔 쓰면 조용히 잘못 돈다.

### 운송 중 물량을 어떻게 다루나

창고 이동은 출발할 때 빠지고 도착할 때 들어온다. 그 사이는 어느 창고에도 없다.

**"운송 중"을 창고처럼 하나 더 둔다.** 출발 줄은 원래 창고에서 빼서 운송 중으로 넣고, 도착 줄은 운송 중에서 빼서 도착 창고로 넣는다. 그러면 전체 합이 항상 맞고, 지금 떠 있는 물량이 얼마인지도 바로 보인다.

⚠️ 이러면 이동 하나가 원장 **네 줄**이 된다. 대신 총 재고가 사라지는 구간이 없어진다.

### 순서를 어떻게 보장하나

같은 날 같은 상품이 여러 번 조정된 사례가 있었다. 하루에 1개에서 82개로, 다시 161개로 바뀌었다.

날짜만으로는 순서를 못 정한다. **문서 번호로도 안 된다** — 문서 종류마다 접두어가 달라서 판매 번호와 조정 번호를 나란히 비교하는 것은 의미가 없다.

⚠️ 실제로 겪었다. 같은 날 조정으로 3개가 들어오고 1개가 팔린 사례에서, 문서 번호로 정렬하니 판매가 먼저 와서 잔고가 잠깐 음수가 됐다. 실제로는 조정이 먼저였다.

**규칙: 같은 날짜면 들어오는 것(+)을 먼저, 나가는 것(−)을 나중에 계산한다.** 재고가 없으면 팔 수 없으니 물리적으로도 그게 맞다. 이 값을 따로 저장해 정렬에 쓴다.

---

## 3부. 만드는 순서

### 1단계 — 원장 쌓기

기초 스냅샷을 찍고, 매일 문서 목록을 받아 원장에 쌓는다. **어디에도 쓰지 않는다.** 화면에 안 보여주고 판단에도 안 쓴다.

이 단계의 목표는 "수집이 빠짐없이 되는가" 하나다.

✅ **2026-08-20 가동** — 스냅샷 촬영 → 커서 seed → `commit=1` 수동 1회 → pg_cron 6잡 등록까지
같은 밤에 넘어갔다. 실측·판단은 아래 4부 「⑤ 쓰기 가동」이 정본.

**쓰기 게이트 (2026-08-17 구현 확정)** — 소스별 all-or-nothing. 다섯 중 하나라도 걸리면
**그 소스는 한 행도 쓰지 않고 커서도 안 옮긴다**:

1. 목록 불완전 (429 · 수신 부족 · 페이지 오류)
2. 창고 맵에 없는 위치(UNMAPPED) 1건 이상 — 원장에 들어가면 영구히 남는다
3. 빈 상품 코드 라인 1건 이상 — 파싱이 틀렸다는 뜻이라 나머지 행도 못 믿는다
4. 날짜 결손
5. 캡 회차인데 처리 문서에 갱신 시각이 없음 — 커서가 동결된다

⚠️ **상세 캡에 걸린 것 자체는 쓰기 허용** — 커서가 캡 앞에서 멈춰 다음 회차가 이어받는다.

### 2단계 — 대조

매일 우리 원장과 Cin7 재고를 비교한다. 차이가 나면 왜 나는지 분류한다.

**이 단계가 가장 오래 걸린다.** 처음에는 차이가 많이 날 것이고, 하나씩 원인을 찾아 수집 로직을 고쳐야 한다.

**차이가 0이 되고 그 상태가 한 달 유지되면** 신뢰할 만하다고 본다.

⚠️ **WMS discrepancy 큐는 최종 진실이 아니다 (2026-08-19).** 작업자 보고와 매니저 실사가 다를 수 있고,
Cin7 조정은 **매니저 실사 기준**으로 이뤄진다. [실물] PO-01117 `CAN01547` — WMS apply_note 는
초과 16 을 클램프했다고 기록했으나, 매니저 실사는 24 였고 `ST-01220` 도 +24 로 조정됐다.
⇒ 대조에서 **「WMS 수량 ≠ 원장 수량」** 이 나와도 오류가 아니다. **구조적 차이**로 분류한다 —
두 시스템이 **다른 시점의 사실**을 기록하는 것이지 어느 쪽이 틀린 게 아니다.
📌 최종 재고의 기준은 **매니저 실사가 반영된 Cin7** 이고, 원장은 그것을 읽으므로 원장이 맞다.

**대조 단계용으로 미리 확보된 자산 (2026-08-17 정리)**:

- **회계 분개(/transactions)를 탐지 축에서 배제한 정량 근거** — 최근 60일 트랜스퍼:
  창고내 분개 있음 123 / 없음 1,882(94% 미포착) · 창고간 23 / 4. 빈 이동은 금액 변동이 없어
  분개가 안 생긴다. **다시 시도하지 않도록 이 숫자를 남긴다.**
- **Movements 검산 (SKU 단위 유일한 검산 축)** — 누적 = 현재 OnHand 3건 전수 일치:
  ANN00098 100 · BS2411 291 · AS92080 2,695. 날짜 필터가 없고 SKU 단위라 수집엔 못 쓴다.
- **WMS 창고 매핑** — `wms_orders.location` 은 Cin7 표기 그대로(매핑 불필요) ·
  `wms_orders/wms_receipts/wms_waves` 의 `warehouse` 는 toronto/edmonton(2줄 매핑 필요).
- **영업일 임계값은 요일별로** — 재고는 주 5일만 움직이고 **온타리오와 앨버타의 공휴일이
  다르다.** 8/03 Civic Holiday → 첫 영업일 8/04 유입 94건으로 튐 — 요일별로 안 잡으면 매주
  월요일 경보가 울린다.
- **`Release to WMS`** — `sale` 상세의 `AdditionalAttributes.AdditionalAttribute1`(목록엔 없어
  상세 필수). 값 어휘 16종에 `Invoiced`·`Fulfilled` 가 번호 있는 것/없는 것으로 중복 ·
  `Credit Note` 는 잔재로 실제는 `Credit Issued` 만 쓴다(Caleb 확인). 원장 1단계엔 불필요 —
  대조에서 "이 오더가 왜 WMS 에 없는가"의 설명에 쓴다.
- **`ProductCustomField` 매핑**(Caleb 확인) — 1·2(Edmonton/Toronto Bin Location)는 실제와
  달라 **무시** · 3 = Project Name · 4 = Registered On. raw 에 라인 원본을 담으면 1~10 이 전부
  들어가는데 쓰는 건 3·4 뿐(raw 저장 단위 최적화와 연결).

### 3단계 — 자리 단위로

창고 단위 대조가 안정되면 자리 단위로 정밀도를 올린다. 1단계부터 자리를 기록해 뒀으므로 소급이 된다.

### bin 축의 현재 상태 (2026-08-31)

| | |
|---|---|
| 같은 창고 안 bin 이동 | ✅ 헤더 `"창고: bin"` 에서 읽는다 |
| 창고 간 이동 — 출발 | ✅ **WMS 픽에서 얻는다**(`Reference` → 픽용 SO → `wms_order_lines.bin_location`) · ⚠️ Cin7 API 는 주지 않는다 |
| 창고 간 이동 — 도착 | ✅ **비어 있는 것이 정상**이다 — Cin7 도 풋어웨이 전에는 bin 없이 들고 있다(에드먼튼 bin 대조 6,478칸 중 12칸만 어긋남) |
| 내부 풋어웨이 | ✅ 우리 WMS 가 만든 문서(`Reference "WMS putaway …"`) · 도착 bin 은 헤더 `To` 에 있다 |
| IN_TRANSIT 다리 | ✅ bin 없음 — 합성 창고다 |

⚠️ **결합 명시**: 원장이 `wms_orders`/`wms_order_lines` 를 읽는다. **잠정 조치다** —
목표는 **WMS 픽이 원장에 사건을 남기는 것**이고, 그때 이 조회도 `Reference` 라는 다리도,
「트랜스퍼 → 가짜 SO → 픽 → VOID」 우회도 사라진다.

📌 **bin 단위 대조가 가능하다** — 기초 스냅샷도 당일 스냅샷도 bin 단위이기 때문이다
(`ref/productavailability` 의 `Bin`). 창고 단위 대조(⑥)는 bin 오차를 못 잡으므로
**bin 축은 따로 재야 한다.**

### ⭐ 대조의 시점 컷오프 (2026-09-01)

`inv_balance_vs_cin7` 은 **언제 조회해도 같은 값**을 준다. 세 조건을 **함께** 쓴다.

```sql
where l.source = 'manual'                                            -- 정정 = 뒤늦게 적은 과거 사실
   or l.occurred_on < (snapshot_taken_at at time zone 'America/Toronto')::date
   or l.created_at <= snapshot_taken_at
```

⚠️⚠️ **하나만 쓰면 반드시 틀린다.** [실측] 같은 날 같은 쿼리가
**9 · 49 · 128 · 475 · 161 · 7 · 9** 를 냈다.

| 조건 | 없으면 |
|---|---|
| `manual` 면제 | 우리 정정이 잘려 **상쇄 전 상태로 되돌아간다**(`ANN01111` diff −24) |
| `occurred_on` 과거 | 과거 사건을 늦게 수집한 것이 잘려 **짝의 반쪽만** 남는다(`WTA00219` diff +60) |
| `created_at` | 낮 사건이 전부 「원장이 과하게 뺐다」로 잡힌다(오후 475칸) |

⚠️ **남는 한계**: 스냅샷 당일 새벽에 일어났는데 우리가 그 뒤에 수집한 사건은 여전히 오탐이다
([실측] 5칸 · 각 −1). 다만 **창이 일정해 값이 시각마다 뛰지 않는다.**

📌 **`inv_balance` 는 컷오프가 없다** — 그건 **지금 재고**를 보여주는 것이 맞다.
컷오프는 **대조 축에만** 필요하다.

📌 **「원장이 말하는 재고」의 정의는 `inv_balance` 뷰 하나다**(2026-09-01 신설).
기준선은 `inv_config.baseline_snapshot_key` 에 있다 —
⚠️ **재기준선을 잡으면 이 값과 cron URL 의 `since=` 를 함께 바꿔야 한다.**

#### 재기준선 절차 (2026-09-07 정리 · ⬜ 아직 한 번도 실행하지 않았다)

⚠️⚠️ **이 절차는 조각을 모은 것이지 실행 기록이 아니다.** 8/20 기초는 **첫 가동**이었고
재기준선이 아니었다 — 둘은 다르다(아래 ⑦). ⬜ 표시는 **검증되지 않은 것**이다.

**0. 선행 — 사각지대가 메워졌는가**
⚠️⚠️ **사각지대를 메우기 전에 재기준선을 잡으면 같은 부류가 다시 쌓인다.**
재기준선은 **누적된 미상을 지우지만 원인을 고치지 않는다.**
📌 [2026-09-07 기준] 소멸 감지 2단계 ✅ · 「수집 후 취소」 `adjustment`·`assembly` ✅ ·
`transfer` 관측만 · ⬜ `sale`·`purchase` 미착수.

**1. ⭐ 경계 오더 목록을 미리 뽑는다**
⚠️ **8/20 때 이것을 안 해서 나중에 522행 + 157행을 상쇄했다.**
`ship_date` 가 새 기초를 가로지르는 오더 — 준비는 끝났는데 아직 안 나간 것들 —
을 촬영 **전에** 목록으로 남긴다. 그것이 없으면 나중에 「왜 안 맞지」를 처음부터 판다.
📌 §함정 표 `ShipmentDate` 행이 그 계열의 정본이다.

**2. ⭐ 촬영 시각을 정한다 — 그날 마지막 사건 이후**
⚠️⚠️ `since` 는 **엄격히 큼**(`occurred_on > since`)이라 **촬영일 당일 전체가 제외된다.**
⇒ **촬영 후에 그날 사건이 더 생기면 그것은 영영 누락된다.**
📌 8/20 이 안전했던 이유는 **19:42 촬영이었고 그 뒤 창고가 멈춰 있었기** 때문이다 —
그것은 그날의 조건이지 규칙이 아니었다. **영업일 낮에 찍으면 안 된다.**
⬜ **일지 cron(01:21)과의 관계는 미검증이다** — `inv_snap_balance_diffs` 는
`baseline_snapshot_key` 가 아니라 **`inv_snapshot` 의 최신 `taken_at`** 을 쓴다.
낮에 새 `-initial` 을 찍으면 그날 일지가 **그 기준으로** 만들어질 수 있다
(`on conflict (checked_on, …) do nothing` 이라 그날 첫 실행 값이 남는다).
⇒ ⚠️ **저녁 촬영이면 다음 날 01:21 회차부터 정상 궤도**일 것으로 보이나 **해보지 않았다.**

**3. 스냅샷 촬영**
```
inv-snapshot?key=<YYYY-MM-DD>-initial     ⚠️ dry=1 로 먼저 볼 것
```
⚠️ **`-initial` 은 자동 생성이 없다** — 사람이 키를 정해 실행한다(`auto-compare` 는
`-compare` 전용이다).
⚠️ **같은 키 재실행은 덮지 않는다**(`ignore-duplicates` — 첫 실행 값이 남는다).
다시 찍으려면 **그 키의 행을 지우고** 재실행한다.
📌 `-initial` 은 **14일 보존 정리에서 불가침**이다(`-compare` 만 지운다).

**4. `inv_config.baseline_snapshot_key` 갱신**
```sql
update inv_config set value = '<YYYY-MM-DD>-initial' where key = 'baseline_snapshot_key';
```
📌 이 값을 읽는 것: `inv_balance` · `inv_balance_vs_cin7`(컷오프) · `inv_bin_history`
(`baseline`). ⇒ 갱신하면 **그 셋이 함께 따라온다.**

**5. ⚠️⚠️ cron URL 의 `since=` 를 6줄 모두 갱신**
```
inv-collect?commit=1&only=<축>&since=<YYYY-MM-DD>
```
⚠️ **하드코딩이다.** `transfer`·`sale`·`purchase`·`adjustment`·`assembly`·`creditnote`
여섯 잡을 **손으로** 고친다. 📌 「테이블 영속화 대신 cron URL 을 고른 대가」이며
설계 4부에 그 결정이 있다.
⚠️⚠️ **빠뜨리면 조용히 부푼다** — 스냅샷에 이미 녹아 있는 과거 사건이 원장에 또 더해진다.
⚠️ `since` 와 `from_since` 는 다르다 — 후자는 커서 씨앗이고 `inv_sync_state` 가 우선한다.

**6. 아침 점검 ⑨ 의 날짜 갱신**
⑨(기초 스냅샷 경계 오더 재유입)는 `occurred_on = '2026-08-21'` 이 **박혀 있다.**
새 기초의 **다음 날**로 바꾼다.

**7. ⭐ 커서 seed 는 하지 않는다**
⚠️ 8/20 에는 했지만 그것은 **첫 가동**이었기 때문이다. 커서(`inv_sync_state`)는
**「어디까지 읽었나」**이고 기준선은 **「어디서부터 세나」**다 — **서로 무관하다.**
⇒ 재기준선에 seed 를 다시 하면 **이미 읽은 문서를 다시 읽는다.**

**8. 검산**
- ⑧ bin 대조가 **0칸**이 되는가 (누적된 미상이 지워졌다)
- ⓪ `new_today 0` · `total 0`
- ⚠️ **다음 날 아침 점검을 끝까지 돌 것** — 특히 ⑨ 와 ⑦(캡·동결).
- 📌 ⑨ 가 뜨면 1번(경계 오더 목록)이 부족했던 것이다.

⬜ **이 절차는 실행되지 않았다.** 처음 할 때는 **한 단계씩 확인하며** 진행하고,
끝나면 이 소절을 **실행 기록으로 갱신**할 것.

Cin7 대조는 `inv_balance_vs_cin7` 이고 ⭐ **시점 컷오프가 들어가 언제 조회해도 같은 값**이다(위 절).

### ⚠️ 재고 마스터 — WMS 화면이 읽는 계약

`wms.asung.ca` 의 재고 마스터 화면이 아래를 읽는다.
⚠️⚠️ **컬럼 이름을 바꾸면 화면이 조용히 빈 표가 된다 — 에러가 안 난다.**
바꿀 때는 **WMS 세션에 알린다.**

| | |
|---|---|
| `inv_stock_master(...)` RPC | 전체 재고 + Cin7 열 · 검색·필터·페이징 |
| `inv_stock_master_sku(...)` RPC | ⭐ **SKU 층**(2026-09-05) — 한 SKU 를 한 줄로 접는다. 캐럿 펼침은 칸 층을 `p_sku_exact` 로 부른다 |
| `inv_bin_history(...)` RPC | ⭐ **이력**(2026-09-06) — 칸(또는 SKU)의 사건 목록. **왜 그 숫자인지**를 보여준다 |
| `inv_balance_diffs` | 어긋남 일지 · ⭐ `first_seen_on` 이 시간 축 |
| `inv_bin_notes` | 코멘트 (insert·select 만) |
| `inv_ack_diff(...)` | ⭐ 「확인됨」 누르기 — **창고·매니저가 원인을 안다는 표시**다 |
| `inv_diff_summary()` | 카드용 — `unack`(카드 숫자) · `new_today`(오늘 처음) · `prev_total`(어제 대비) |

⚠️ **「확인됨」과 「해결됨」은 다르다.** 확인됨은 사람이 원인을 안다는 뜻이고 숫자는 아직 틀려
있다. 해결됨은 원장이 상쇄를 넣어 실제로 맞춘 것이고, 그러면 **다음 회차에 목록에서 저절로
사라진다** — 별도 상태가 없다.
📌 화면이 누르는 것은 **「확인됨」 하나**다.

#### 두 RPC — 시그니처와 반환 (2026-09-05)

⚠️⚠️ **파라미터 순서가 두 함수에서 다르다 — 반드시 명명 인자로 부를 것.**
위치 인자로 부르면 **조용히 틀린다.**

```
inv_stock_master(      p_search, p_warehouse, p_only_diff, p_nonzero,
                       p_limit, p_offset, p_sku_exact )      ← p_sku_exact 가 맨 끝
inv_stock_master_sku(  p_search, p_warehouse, p_only_diff, p_nonzero,
                       p_sku_exact, p_limit, p_offset )      ← p_nonzero 다음
```

📌 칸 층에서 `p_sku_exact` 를 **맨 끝**에 붙인 이유: 기존 6-인자 호출(위치·명명 모두)을
깨지 않기 위해서다.
⚠️ **옛 6-인자 시그니처는 `drop` 했다.** 7-인자가 default 라 6-인자 명명 호출에도
매칭되어, 둘이 남으면 PostgREST 가 두 후보를 모두 맞다고 보고 **후보 선택 오류**를 낸다
(화면이 통째로 깨진다). ⇒ **시그니처를 바꿀 때는 옛것을 반드시 지운다.**

**SKU 층 `rows[]` — 13개**

| 컬럼 | 뜻 |
|---|---|
| `sku` | 접기 키 |
| `qty` | 창고 재고 — ⚠️ **`IN_TRANSIT` 제외** |
| `in_transit_qty` | 운송 중 — 분리해서 낸다 |
| `qty_at_snapshot` · `cin7_qty` | 컷오프 시점 원장 · Cin7 |
| `diff` | ⚠️ **이것만 보면 안 된다** — 거울 칸이 상쇄된다 |
| `diff_bins` | ⭐ `diff ≠ 0` 인 칸 수 |
| `abs_diff` | ⭐ 각 칸 `abs(diff)` 의 합 |
| `in_transit_bins` | 운송 중이라 대조 상대가 없는 칸 수 — **상시** |
| `new_since_snapshot_bins` | 스냅샷 이후 생긴 칸 수 — ⚠️ **정상값**(아래) |
| `bins` · `warehouses` | 걸쳐 있는 칸·창고 수 (⚠️ `IN_TRANSIT` 은 창고로 세지 않는다) |
| `unack_bins` | `diff ≠ 0` 이고 미확인인 칸 수 |

봉투는 칸 층과 동형: `total` · `snapshot_key` · `snapshot_at` ·
`ledger_collected_at` · `ledger_lag_source` · `rows[]`.
❌ `first_seen_on` 은 SKU 층에 내지 않는다 — 칸 단위 의미다. 가장 오래된 것을 대표로 내면
**새로 생긴 칸이 오래된 것처럼 보인다.**

⚠️⚠️ **`diff` 합만 보면 거울 칸이 사라진다.**
[실물 `PRO00124`] 에드먼튼 `EB010302` **+3** · `EB010304` **−3** →
SKU 층에서 `diff 0` 인데 `diff_bins 2` · `abs_diff 6` 이다.
⇒ **화면은 `diff` 가 0이어도 `diff_bins > 0` 이면 경고를 띄운다.**
그것이 없으면 SKU 층은 **어긋남을 감추는 화면**이 된다 — 칸 층보다 나빠진다.

📌 **필터는 집계 전/후로 나뉜다** — 칸 단위로 먼저 걸면 `p_only_diff` 가 `diff = 0` 칸을
지워 `qty` 가 「어긋난 칸의 재고만 더한 값」이 되고, **거울 칸 경고를 넣어놓고 필터가
그것을 걸러내는** 모양이 된다.
· 집계 전 — `p_sku_exact`/`p_search` · `p_warehouse`
· 집계 후 — `p_only_diff` → `diff_bins > 0` · `p_nonzero`
⚠️ **`p_only_diff` 의 뜻이 층마다 다르다**: 칸 층 「이 칸이 어긋났다」 ·
SKU 층 **「이 SKU 에 어긋난 칸이 있다」**.

📌 **`p_warehouse` 는 부분합이다**(WMS 확정) — 창고를 고르면 `qty`·`bins`·`warehouses`·
`diff_bins` 전부 그 창고 것만. 총합이 필요하면 필터를 푼다(기본이 전체).
⚠️ **캐럿 펼침에는 `p_warehouse` 를 주지 않는 것이 화면의 약속**이다(전 창고 칸을 보여주려고).
**함수는 그 규칙을 넣지 않았다 — 받은 대로 처리한다.** 주면 그 창고 칸만 나온다.

⚠️ **`unack_bins` 는 낮에 부풀어 보인다.** 「확인됨」을 `inv_balance_diffs` 의 **최신 회차**
에서 읽는데 그 일지는 **새벽에 굳는다** ⇒ 낮에 새로 어긋난 칸은 일지에 없어 미확인으로
세인다. 칸 층 `Ack` 열이 그런 칸을 「—」로 그리는 것과 **같은 동작**이다.

📌 [실측 2026-09-05] 칸 층 **496 ms** · SKU 층 **537 ms** · 칸 14,272 → SKU 7,938.
⚠️ `total` 이 `count(*)` 라 **페이징은 비용을 줄이지 않는다**(두 층 모두).

#### 이력 — `inv_bin_history` (2026-09-06)

```
inv_bin_history(
  p_sku        text,                  -- ⚠️ 필수 · 정확 일치(ilike 아님 — 지목해 여는 것이다)
  p_warehouse  text default null,     -- null = 전 창고
  p_bin        text default null,     -- null = 전 칸
  p_limit      int  default 50,
  p_offset     int  default 0
)
```

**봉투**: `total` · `rows[]` + ⭐ **`baseline`** 별도 키
```
baseline: { snapshot_key, taken_at, qty }
```
⚠️⚠️ **`baseline` 을 `rows[]` 에 섞지 않는다** — 기초는 **사건이 아니라 출발점**이다.
섞으면 「사건 수」가 하나 늘어 보인다.
📌 원장은 8/20 기초 스냅샷 **위에** 사건을 쌓으므로, **사건을 다 훑어도 기초 없이는 잔고에
닿지 않는다.** 화면은 목록 맨 아래(오름차순이므로 시작점)에 그 행을 그린다.
⚠️ **재기준선을 잡으면 값이 바뀐다** — `inv_config.baseline_snapshot_key` 를 읽는다.

**`rows[]` — 12개**

| 컬럼 | 뜻 |
|---|---|
| `occurred_on` | ⭐ **사건 날짜** |
| `doc_type` · `doc_number` | ⭐ 문서번호는 **전 축 100%** 존재(실측) — `SO-15440`·`TR-04504`·`ST-01283` |
| `event_type` | `sale_out`·`transfer_in`·`assemble_out` … |
| `warehouse` · `bin` | SKU 단위로 볼 때 필요하다 |
| `qty_delta` · `running_qty` | 증감 · 그 사건 뒤 잔고(`baseline.qty` 에서 누적) |
| `is_manual` | `source = 'manual'` — **우리가 손으로 맞춘 것** |
| `fix_kind` | `line_ref` 접미어 — 상쇄 종류 |
| `reason` | 사람이 읽을 한 줄 — ⚠️ **null 일 수 있다**(아래) |
| `created_at` | ⚠️ **수집 시각** — 사건 날짜와 다르다(아래) |

⭐ **정렬은 오름차순(기초 → 현재)이다.** `running_qty` 가 더해가며 읽히고,
**「마지막 `running_qty` = 현재 잔고」**가 성립한다. 최신을 위로 보이려면 화면이 뒤집는다.

⚠️ **`running_qty` 는 전 사건을 누적한 뒤 페이징한 값**이다. 페이징을 먼저 하면
2페이지부터 누적이 틀린다. 누적 순서는 `occurred_on` · `seq_hint` · `id` 이며,
`seq_hint` 는 원장이 **같은 날 유입(1)을 유출(2)보다 먼저** 적용하려고 둔 축이다.

⭐ **`reason` 은 모든 사건에 붙는다** — `coalesce(raw->>'detail', raw->>'rule')`.
[실측] `cin7` 19,668행에 `raw.rule` **100%**(예: `"sale_out: -Quantity(48) shipped pick line"`) ·
`manual` 1,579행에 `raw.detail` 987행.
⇒ `cin7` 행은 **「이 증감이 어느 규칙으로 나왔나」**, `manual` 행은 **「왜 손으로 맞췄나」**가 보인다.
⚠️ **원장은 자르지 않고 그대로 낸다** — `detail` 은 영문 장문이다. 어디서 자를지는 화면이
정하고(목록엔 짧게 · 전문은 hover/펼침), 잘라서 내면 **전문을 볼 방법이 없어진다.**

⚠️⚠️ **`reason` 이 `null` 인 것은 정상이다 — 에러가 아니다.**
[실측] `transfer`/`manual` 881행 중 **592행**에 `detail` 이 없다. 8/31
`scripts/fix-transfer-bins.mjs` 가 넣은 행이고 접미어가 `:binfix` 다(실물 `TR-04173`) —
스크립트가 `detail` 을 안 담았다. ⇒ **`fix_kind` 가 종류를 말해준다.**
📌 화면은 이 경우를 **정상으로 그린다**(경고 스타일 금지).

⚠️ **`is_manual` 을 경고 스타일로 그리지 않는다** — 수동 정정은 **문제가 아니라 해결의
흔적**이다. 숨기면 오히려 나쁘다: 사건의 증감을 더했는데 잔고와 안 맞으면 사람이 화면을
못 믿게 된다.

**`fix_kind` — 상쇄 종류** (`line_ref` 의 마지막 `:` 뒤 · `manual` 행만)

| 값 | 무엇 |
|---|---|
| `voided` | 문서 전체가 Cin7 에서 취소됨 |
| `deleted` | 라인 하나가 삭제됨 |
| `qtyfix` | 라인 수량이 바뀜 — 차액만 |
| `latepick` | 기초 이전 `ship_date` 의 홀드 오더가 기초 이후 출고 |
| `binfix` · `binfixed` | bin 규칙 변경 상쇄(스크립트 · `detail` 없음) |
| `superseded` · `reversal` | bin 규칙 변경으로 자리 비움 · 되돌림 |

⚠️ **판정은 화이트리스트가 아니다** — `source='manual'` 이고 마지막 `:` 뒤가
**영문 소문자만**(`^[a-z]+$`)일 때다. 목록으로 박으면 **앞으로 생길 접미어가 전부 null** 이 된다.
⚠️⚠️ 판매의 `line_ref` 는 `<Fulfilment TaskID GUID>:<ProductID GUID>` **복합키**이고
`cin7` 폴백에 `no-product-id:<SKU>` 도 있다 — 「마지막 `:` 뒤」만으로는 **GUID·SKU 가 샌다.**
GUID 는 숫자·하이픈을, SKU 는 숫자를 포함해 `^[a-z]+$` 에 걸리지 않고, `cin7` 행은
`source` 조건으로 애초에 null 이다. **이 규칙을 단순화하지 말 것.**

⚠️⚠️ **두 날짜 축이 며칠씩 갈린다 — 둘 다 낸다.**
```
occurred_on   사건 날짜   — Cin7 문서의 날짜(⚠️ 사용자 입력일 수 있다 · §ShipmentDate)
created_at    수집 시각   — 우리가 그것을 안 시각
```
[실물 `SO-14897`] `occurred_on` **8/25** · 수집 **9/4**.
⇒ ⭐ **그 차이 자체가 진단 정보다.** 「왜 이 사건이 이제야 보이나」가 그 자리에서 읽힌다 —
결함 E·`SO-14734` 규명 때 결정적이었던 축이 정확히 이것이다.
📌 화면은 `occurred_on` 으로 정렬하되 `created_at` 도 보여주는 편이 낫다.

📌 [실측 2026-09-06] **5 ms**(`p_sku := 'PRO00124'`). 인덱스
`inv_ledger_sku_wh_on_idx (sku, warehouse, occurred_on)` 가 필터와 정렬을 함께 처리한다 —
**인덱스를 추가하지 말 것.** 칸당 사건 수는 최대 39 · 평균 3.0 · p95 9 다.

⭐ **이력은 편의 기능이 아니라 진단 도구다.** [실물 `BNAT48173`/`C120102`]
`baseline 836` 에서 시작해 마지막 `running_qty 747` 로 ⑧ bin 대조의 `ledger_qty` 와
정확히 맞는데, 그 사이에 `SO-15440 −1` 과 `qtyfix −11` 이 나란히 보인다 —
**09-04 에 반나절 걸린 규명이 두 줄로 읽힌다.**
⚠️ 그러므로 **경고 스타일을 쓰지 않는다.** 이력은 사실 기록이지 문제 표시가 아니다.

#### 가용(Available) — ❌ 원장이 내지 않는다 (2026-09-07 결정)

⚠️ **`inv_stock_master` 에 `available` 컬럼을 넣지 않는다.** 원장 축이 아닌 값을
원장 RPC 가 내면 **계약이 흐려지고**, 화면은 그것을 **원장이 보증하는 값**으로 읽는다.

**원장은 `Allocated` 를 「안 보는」 것이지 「못 보는」 것이 아니다**

원장은 **Ship 에서 차감**한다(`raw.header.ship_date` · `sale_out`). **픽·팩은 사건이 아니다.**
⇒ ⭐ 원장은 **「무엇이 실제로 움직였나」**의 기록이고, `Allocated` 는 **「약속」**이지
움직임이 아니다.

⚠️ **`inv_snapshot` 에 `Allocated` 를 추가하지 않는다.** 스냅샷은 「그 순간 창고에 몇 개
있었나」이고 `Allocated` 는 **시시각각 변하는 상태**다. 섞으면 **⑧ bin 대조의 컷오프 축이
무엇을 뜻하는지 모호해진다.**
📌 [실측 2026-09-07] `inv_snapshot` 컬럼은
`snapshot_key · taken_at · sku · warehouse · bin · qty · value` 뿐이다.

**❌ 지금 만들지 않는 이유 셋 — 되살리지 말 것**

1. ⚠️ **원천이 없다.** `Allocated` 는 **주문 서밋**에서 생기는데 원장은 안 보고
   **WMS 는 일부만 안다** — 서밋~WMS 구간이 비고 · **백오더가 중간에 스플릿**되고 ·
   WMS 미경유가 월 수백 건이다. ⇒ **WMS 기준으로 계산하면 과소집계**이고,
   **틀린 숫자를 정답처럼 보여주게 된다.**
2. **Cin7 이 이미 그 일을 한다** — `Inventory → Products → Stock` 의 `ALLOCATED` 열.
3. ⚠️ **지금 만들면 Cin7 을 끄는 날 버린다** — **SO 모듈**이 그 원천이 된다
   (§4부 「플립 — 이관」).

**⚠️ 용어 — 「팔 수 있는」이 아니라 「보낼 수 있는」이다**

**오버셀은 정책이다.** 재고가 0이어도 주문을 받는다 — **프리오더를 받기 위해서**이고
**손님 수요를 정확히 잡아 예측에 쓰기 위해서**다. **백오더 스플릿이 그 정책의 처리 장치**다.
⇒ ⭐ 갈라야 하는 것은 **「창고에 있는 것」과 「이미 임자가 정해진 것」**이다.

📌 화면에 한 줄을 두는 것은 유용하다 — 「이 숫자는 **창고에 있는 것**이고,
**지금 보낼 수 있는 수량은 Cin7 을 보라**」. 그것이 없으면 매니저가 재고 마스터 숫자로
출고 가능량을 판단한다.
⚠️ **「팔 수 있는 수량」이라고 쓰지 말 것** — 주문은 재고와 무관하게 받는다.

📌 **세 화면의 경계** — 겹치지 않는다:
· WMS **Discrepancy** = 오더 단위 픽·팩 수량 불일치
· WMS **Health** = WMS 내부 정합 12검사
· **재고 마스터** = 원장 vs Cin7 재고 정합

⚠️ **화면에 「○시 기준」을 반드시 띄운다** — Cin7 스냅샷은 01:21 의 한순간이고
낮에는 실시간이 아니다. 없으면 「방금 조정한 것이 왜 안 보이냐」가 된다.

⭐ `inv_diff_summary()` · `inv_stock_master(...)` 둘 다 **`ledger_collected_at`** ·
**`ledger_lag_source`** 를 낸다 — `ledger_qty` 가 **어디까지 반영된 값인지**다.
값은 `inv_sync_state.last_ok_at` 의 **재고 축 여섯**(⚠️ `cost` 제외) 중 가장 뒤처진 것이다.
⚠️ **`cost` 를 빼는 이유**: 하루 1회라 늘 「하루 뒤처짐」으로 보이고 재고 수량과 무관하다.
📌 **화면은 이 값을 반드시 표시한다** — [실사고 2026-09-01] WMS 의 `⟳ Stock` 체인이
**12일간 죽어 있었는데 어느 화면에도 드러나지 않았다.**

⚠️ **`diff` 가 `null` 인 행**: `inv_balance_vs_cin7` 은 `IN_TRANSIT` 을 제외한다.
`inv_stock_master` 는 두 뷰를 **`full outer join`** 하므로(2026-09-05 성능 수리 —
그전에는 `union all` + `not exists` 두 갈래였다) **`inv_balance` 에만 있는 행**에서
`null` 이 생긴다.
⇒ **뷰를 `group by` 로 세면 `null` 이 0건인 것이 정상**이다 — 「비교 불가」는 **뷰에 없다는
사실 자체**로 표현된다. 화면은 그것을 `n/a` 로 그리고 **0 과 구분**한다.

⚠️⚠️ **그 `n/a` 는 두 부류이고 성격이 다르다 — 하나로 합치면 뜻을 잃는다.**

| | 무엇 | 성격 |
|---|---|---|
| `in_transit_bins` | `IN_TRANSIT` — Cin7 쪽 대응이 원래 없다 | ⭐ **상시**(실측 512칸) |
| `new_since_snapshot_bins` | 스냅샷 **이후** 생긴 자리 — 대조 상대가 아직 없다 | ⚠️ **하루살이** |

⚠️⚠️ **`new_since_snapshot_bins` 는 정상값이다.** 낮에 자리 이동이 있으면 늘 생기고
**다음 스냅샷에 사라진다.** [실측] 09-04 저녁 **10칸**(에드먼튼 `TR-04501`~`04504` bin 이동)
→ 09-05 아침 **0칸**.
⇒ 📌 **화면이 이것을 경고로 띄우면 매일 오탐이 난다** — 이 프로젝트가 계속 경계해 온
「매일 빨간불」이 하나 더 생긴다. 표시한다면 `in_transit_bins` 와 **나란히, 중립적으로**.

📌 하나로 합쳐 세면 이런 것을 못 읽는다 — 09-04 `n/a` 522칸 → 09-05 **512칸**의 차이 10 이
무슨 뜻인지 알 수 없게 된다.

⚠️ **`full outer join` 이 성립하는 전제**: 두 뷰 모두 `(sku, warehouse, bin)` 이 group by
키라 **유일**하고, `bin` 이 양쪽 `coalesce(bin,'')` 라 **not null** 이다
(`null = null` 은 false 라, 이것이 아니었으면 매칭이 조용히 깨졌다).
⇒ **뷰의 group by 키·bin coalesce 를 바꿀 때 이 조인을 함께 볼 것.**
📌 `IN_TRANSIT` 512칸은 그 조인에서 **`inv_balance` 전용 행**으로 살아남는다 —
**잃어도 에러가 안 난다. 조용히 줄어든다.** RPC 를 고칠 때 그 수를 반드시 검산한다.

### 4단계 — 원가

FIFO 레이어를 붙인다. ⚠️ **Cin7 이 원가 레이어를 안 주기 때문에**, 우리가 처음부터 쌓은 것만 정확하다. 기초 스냅샷의 평가액은 출발선일 뿐 레이어 구조가 아니다.

여기부터는 회계와 맞물려서 사실상 별개 프로젝트다.

#### 테스트 DB — 여기서부터 필요하다 (2026-09-07 결정)

⭐ **「Cin7 이 채점해 준다」가 여기서 끝난다.** 지금까지 원장은 **받아적고 대조**했고,
맞는지는 Cin7 이 말해 줬다. **원가는 그것이 없다** — Cin7 이 레이어를 안 주므로
**대조할 상대가 없다.**
⇒ ⭐ **시나리오를 만들어 검증하는 수밖에 없고, 그러려면 실험할 자리가 필요하다.**

⚠️ 그리고 **원가는 되돌리기 어렵다.** 재고는 틀리면 상쇄하면 되지만 원가는
**COGS 로 회계에 들어간다.** ⇒ **실제 데이터에서 실험할 수 없다.**

**방식 — 별도 Supabase 프로젝트 · Cin7 미연결 + 운영 데이터 복사**

📌 Supabase **Pro** 라 추가 프로젝트가 가능하다.
⭐ **격리가 구조적이다 — 운영 코드에 `if (test)` 가 한 줄도 안 생긴다.**

⚠️ **버린 안 셋과 그 이유** (되살리지 말 것):
- ❌ **`source='test'` 플래그** — 대조가 전수라 테스트 행 하나만 넣어도 ⑧ 에 뜨고
  ⓪ `new_today` 가 오르고 **아침 점검이 매일 그것을 묻는다.** 걸러내려면 뷰·RPC·점검
  열두 줄·감지 표 넷을 전부 고쳐야 하고, ⚠️ **그중 한 곳만 빠뜨려도 조용히 틀린다.**
  ⇒ **테스트를 위해 운영 코드가 복잡해진다.**
- ❌ **Cin7 에 테스트 회사·창고** — 그 재고도 **Cin7 정본에 들어간다.** 격리가 아니다.
- ❌ **Cin7 테스트 조직** — ⭐ **불필요하다.** 검증하려는 것은 **「우리 로직이 맞나」**이지
  「Cin7 과 같나」가 아니고, 후자는 **이미 매일 하고 있다.**
  ⚠️ 게다가 **원가는 Cin7 이 채점해 줄 수 없다.**
  📌 참고: 현재 Cin7 API 키 2개는 **같은 조직의 접근 키 둘**(GAS 용 · WMS 용)이지
  테스트 조직이 아니다.

**⚠️⚠️ 반드시 끄는 것 — 이것이 「Cin7 미연결」의 실체다**
```
pg_cron 잡 전부       ← Cin7 수집 · 스냅샷 · 대조 · 헬스
Edge Function 배포    ← inv-collect · inv-snapshot 등
```
⚠️ 안 끄면 **테스트 DB 가 운영과 같은 것을 긁는다.** 그러면 격리가 깨지고,
Cin7 API 호출도 두 배가 된다.

**착수 조건 — 날짜가 아니라 조건이다**

⭐ **PO + 원가에 착수하는 시점**에 복사한다. ⚠️ **그전에 옮기면 테스트 DB 가 논다** —
남은 원장 작업(커서 근본 해결 · `sale`·`purchase` VOID 감지)은 전부 **관측·감지**라
**실제 데이터가 오히려 낫다.** 그리고 **복사본은 즉시 낡는다** — 옮긴 순간부터 운영은
쌓이고 테스트는 멈춘다.

⚠️ **예외 하나**: **복사 절차는 미리 한 번 시험할 값이 있다.**
「과연 복사가 되나」를 모르는 채로 착수하면 **그 자리에서 막힌다.**
⇒ ⭐ **리허설로 한 번, 실제 착수 때 최신으로 다시.**

**✅ 구축 완료 (2026-09-08) — 절차 확정**

📌 프로젝트: **`Asung-IMS`** · ref `fazgmyvzzhqybtvtktyg` · `ca-central-1` ·
운영과 **같은 조직**(Pro). ⭐ **마이그레이션은 레포에서 재생하고 데이터만 옮긴다.**

**세 단계**

```bash
# ① 운영에서 데이터만 덤프 (--linked = 운영)
supabase db dump --linked --data-only --use-copy --schema public \
  -x public.inv_collect_runs   -x public.inv_snapshot_runs \
  -x public.inv_compare_runs   -x public.inv_compare \
  -x public.inv_missing_lines  -x public.inv_conflicts \
  -x public.inv_missing_docs   -x public.inv_voided_docs \
  -x public.wms_health_runs    -x public.wms_refresh_requests \
  -x public.wms_polled_sales   -x public.wms_image_sync_runs \
  -f ~/asung-testdb-data.sql

# ② 테스트에 스키마 재생
supabase db push --db-url "$(cat ~/.asung-testdb-url)"

# ③ 테스트에 데이터 복원
psql "$(cat ~/.asung-testdb-url)" -f ~/asung-testdb-data.sql > ~/restore.log 2>&1
```

⚠️ **`--schema public` 이 필수다.** 없으면 `--schema "*"` 가 되어 **`auth`·`storage` 가
덤프에 들어간다**(`refresh_tokens` · 사용자 계정). 📌 [실측] 붙이면 `COPY "auth"` 0건이다.

📌 **제외 12개는 로그성·감지 표다** — 테스트에선 수집·대조를 안 돌리므로 **화석**이 되고,
낡은 채로 남으면 헷갈린다. ⭐ `inv_balance_diffs` 는 **복사한다**(「확인됨」 이력이라 재현 가치가 있다).

**⚠️ 오늘 겪은 함정 넷**

1. ⚠️⚠️ **IPv6 로는 연결이 안 된다.** `db.<ref>.supabase.co` 직접 연결은 **IPv6 전용**이라
   WSL 에서 `network is unreachable` 이 난다.
   ⇒ ⭐ **Session pooler(포트 5432)** 를 쓴다 — 대시보드 `Connect → Direct → Session pooler`.
   ```
   postgresql://postgres.<ref>:<비번>@aws-0-ca-central-1.pooler.supabase.com:5432/postgres
   ```
   ⚠️ **Transaction pooler(6543)는 마이그레이션에 맞지 않는다**(세션이 필요하다).
2. ⚠️ **`supabase db dump` 는 Docker 를 쓴다** — 버전을 맞추려고 컨테이너로 `pg_dump` 를
   돌린다. **Docker Desktop 이 떠 있어야** 한다.
3. ⚠️ **URL 을 셸 변수에 넣으면 잃어버린다.** 비밀번호에 `!`·`@` 가 있으면 붙여넣기가
   쪼개지고, 변수는 **셸 세션이 바뀌면 사라진다.**
   ⇒ ⭐ **파일에 담는다**: `cat > ~/.asung-testdb-url` → 붙여넣기 → `Ctrl+D` →
   `tr -d '\n'` 로 개행 제거 → `chmod 600`.
   ⚠️ 비밀번호가 **평문으로 남는다** — 그것을 알고 쓴다.
4. ⚠️ **`inv_config` 는 중복 키로 실패한다.** 마이그레이션이 기본값을 이미 넣어 두므로
   덤프의 `COPY` 가 `duplicate key value violates unique constraint "inv_config_pkey"` 로
   막힌다. 📌 [실측 09-08] 값이 같아서(`2026-08-20-initial`) **무해했다.**
   ⚠️ 그러나 **재복사 시 운영에 새 키가 추가돼 있으면 그 `COPY` 블록 전체가 안 들어간다.**
   ⇒ ⭐ **재복사 절차: `truncate inv_config;` 를 먼저 하고 복원한다.**

**⭐ 검산값 (2026-09-08 구축 직후 · 두 DB 일치)**

| | 값 |
|---|---|
| 테이블 수 | **43** |
| 마이그레이션 | **53** (전부 적용) |
| ⚠️ **cron 잡** | ⭐ **0** — 이것이 「Cin7 미연결」의 실체다 |
| `inv_ledger` | 22,285 |
| `inv_snapshot` | 206,489 |
| `wms_orders` | 1,613 |
| `wms_order_lines` | 40,364 |
| `inv_cost` | 922 |
| `inv_config.baseline_snapshot_key` | `2026-08-20-initial` |

⭐ **cron 잡이 0인 이유**: 마이그레이션에 `cron.schedule` 이 **한 줄도 없다**
([실측] `grep -rln "cron.schedule" supabase/migrations/` 0건). 잡은 `supabase/ops/cron.sql`
에만 있고 그것은 **손으로 돌리는 파일**이다. ⇒ `db push` 만으로는 잡이 생기지 않는다.
⚠️ **그 파일을 테스트에 실행하지 말 것.**

⚠️ **EF 도 배포하지 않는다** — 아무것도 하지 않으면 된다.
`supabase functions deploy` 는 **link 가 걸린 운영으로** 가므로 실수할 여지가 적다.

**⬜ 남은 판단**
- ⬜ `inv_snapshot` 의 `-compare` 약 192,000행(44MB)은 **테스트에서 화석**이다
  (대조를 안 돌린다). 지울지 미정 — **급하지 않다.**
- ⬜ 재복사 주기 미정. ⭐ **「필요할 때 다시 만든다」**가 맞아 보인다 —
  최신으로 유지하려 들면 **그 자체가 일이 된다.**

⚠️ **원칙**: **테스트를 위한 시스템이 되어서는 안 된다.**
운영 코드가 테스트를 알아야 한다면 그 설계는 틀린 것이다.

#### ⚠️⚠️ 어느 프로젝트에서 실행하는가 (2026-09-08 · 프로젝트가 둘이 됐다)

⚠️ **레포는 하나다.** `~/asung/asung-wms` 에서 두 DB 를 다 쏜다 —
**구분은 폴더가 아니라 옵션이다.**

| | 프로젝트 | ref |
|---|---|---|
| **운영** | `asung-WMS` | `gftpcnkxbdjzzfvzwcfl` |
| **테스트** | `Asung-IMS` | `fazgmyvzzhqybtvtktyg` |

⚠️ 이름이 헷갈린다 — **새로 만든 `Asung-IMS` 가 테스트**이고, 원래 쓰던
**`asung-WMS` 가 운영**이다.

| 명령에 보이는 것 | 어디로 |
|---|---|
| `--db-url "$(cat ~/.asung-testdb-url)"` | ⭐ **테스트** |
| `psql "$(cat ~/.asung-testdb-url)"` | ⭐ **테스트** |
| `supabase db push` (옵션 없음) | ⚠️ **운영** |
| `supabase functions deploy` | ⚠️ **운영** |
| `supabase db dump --linked` | ⚠️ **운영에서 읽기** |
| 대시보드 SQL Editor | ⚠️ **화면에서 고른 것** — URL 의 ref 를 확인한다 |

⇒ ⭐ **`~/.asung-testdb-url` 이 보이면 테스트다.** 그것이 가장 기억하기 쉬운 규칙이다.
⚠️ **`--linked` 는 테스트가 아니라 운영이다** — link 는 운영에 걸려 있다.

⚠️⚠️ **`supabase link` 를 테스트로 바꾸지 말 것.** 그러면 `db push`·`functions deploy` 가
**전부 테스트로** 가고, 나중에 운영에 배포하려 할 때 「왜 안 바뀌지」가 된다.
⇒ ⭐ **link 는 운영에 둔다.**

**⚠️ 틀리면 무슨 일이 나나**
- 테스트에서 돌릴 것을 운영에서 → ⚠️⚠️ **실제 재고가 틀어진다**
- 운영에서 볼 것을 테스트에서 → 📌 **덤프 시점에 고정된 낡은 값**을 보고 판단한다

**⚠️ 마이그레이션 이력이 갈린다 — 정상이다**
PO + 원가 작업은 **테스트에 먼저** 쏘므로 두 DB 의 적용 수가 다르다.
⇒ ⭐ 「어느 쪽이 앞서 있나」를 헷갈리지 않도록 세어 본다.
```bash
# 테스트
psql "$(cat ~/.asung-testdb-url)" -c "select count(*) from supabase_migrations.schema_migrations;"
```
```sql
-- 운영 · asung-WMS 의 SQL Editor
select count(*) from supabase_migrations.schema_migrations;
```
📌 [실측 09-08 구축 직후] 둘 다 **53**.

#### inv-cost 수집 계약 — Advanced·Simple (2026-09-09)

📌 실측 근거는 `docs/sessions/2026-09-09-simple-purchase-cost.md` · Advanced 는 `docs/sessions/2026-08-27-landed-cost-investigation.md`.
여기는 **계약**만 적는다(스킬은 요약·함정).

**라인 출처** — Advanced 는 `PutAway.Lines`, **Simple 은 `StockReceived.Lines`**(PutAway 블록이 없다).
`inv_cost.raw.axis` 가 `"putaway"` / `"stock_received"` 로 출처를 남긴다.

**5키 계약**

| `inv_cost` 컬럼 | Advanced (`PutAway.Lines`) | Simple (`StockReceived.Lines`) |
|---|---|---|
| `line_ref` | `CardID` | `CardID` |
| `bin` | `LocationID` → `resolveLoc` | `LocationID` → `resolveLoc`(= `Location`) |
| `warehouse` | `resolveLoc` | `resolveLoc` |
| `sku` | `SKU` | `SKU` |
| `occurred_on` | 라인 `Date` | 라인 `Date` |

⭐ **Simple·Advanced 공통으로 `line_ref` = `CardID`** 이므로 하류(레이어 조인)에 타입 분기가 필요 없다.

**자기검증** — 불일치 = **문서 단위 격리**(`skip_check_failed`) · 부분 저장 없음.

| | Advanced | Simple |
|---|---|---|
| (b) 수량 | PA↔SR `ProductID` 별 수량 합 | **(b′)** `Invoice.Lines`↔SR `ProductID` 별 수량 합 — 근거: Simple 은 인보이스=입고 수량을 구조적으로 강제(`cin7-api` 주의 14) |
| (c) 키 | PA↔SR `(ProductID, Date)` 키 양방향 존재 | **(c′)** **IM 키 ⊇ SR 키** — PA↔SR 을 그대로 두면 SR↔자기 자신 항등식이 되어 검증이 아니게 된다. ⭐ 부수 효과로 「입고됐는데 COGS 없는 라인」을 잡는다 |

**`skip_not_received`** — SR 수량 합 0 **그리고** IM 0 인 Simple 문서. 경고 없이 `dispositions` 에서만 센다.
⚠️ **IM 이 있는데 SR 이 0 이면 빠지지 않는다** — 입고 전에 COGS 가 생긴 이상 상태이므로 (b′) 가 격리한다.
📌 Advanced 는 같은 상황에서 `processed`(행 0)로 조용히 끝난다(현행 유지 · `scripts/test-invcost.mjs` ㉗ 이 가드).

**환율** — `Invoice[].CurrencyRate` **우선** → 최상위 `CurrencyRate` **폴백** → 둘 다 없으면 **null**(⚠️ **0 금지**)
+ `fx_rate_missing` 카운터 + 경고. null 이면 `currency_orig`·`amount_orig`·`fx_rate` 셋 다 null 로 두고 **행은
저장**한다(`amount`·`unit_cost` 는 `COGS` 에서 나와 정확하므로 문서를 버리지 않는다).
⚠️⚠️ **`COGS` 에 환율을 곱하지 않는다** — 이미 CAD 확정값이다.
📌 `amount_orig` 는 `Invoice.Lines` 원문 합(**할인 전**)이고 `amount` 는 할인·환율이 반영된 CAD 확정값이다 —
[실측 `PO-01215`] `amount_orig` 27,160.20 / `amount` 35,957.06. **두 축은 다른 것을 담는다**(재계산 항등식
`amount = (amount_orig ÷ lines_total_all) × net_total × fx_rate` 로 연결된다).

**`?recheck_since=YYYY-MM-DD`** — 목록 `UpdatedSince` 를 그 날짜로 쓰고 정밀도 필터를 그 회차만 끈다.
**커서는 뒤로 가지 않는다**(캡 회차에는 유지 · 비캡이면 회차 시작 시각으로 전진). `summary.recheck_since` 에 값이
남는다. ⚠️ **수동 커서 되감기 금지** — 결함 C·D 계열 사고 지점이고 **되감은 사실이 회차 로그에 남지 않는다**
(「사건을 남긴다」 위반).

#### 상쇄(정정) 계약 (2026-09-09)

📌 실측 근거는 `docs/sessions/2026-09-09-reversal-conventions.md`. 여기는 **판정 기준**만 적는다.

- **물리 삭제 금지**(append-only). 정정은 항상 **행 추가**다.
- **두 방식과 선택 기준**
  · 같은 사건을 **다른 키로 재기표**해 옛 벌을 무효화 → `event_type='manual_reversal'` · 접미어 **없음**(타입이 구분한다)
  · 사건 자체를 **취소·정정** → **원래 `event_type` 유지** · `line_ref` 에 **`:reversal` 접미어** · 부호 반대
- **`source='manual'`** 은 두 방식 공통이다.
- ⚠️⚠️ **원장 잔고·소진량을 계산할 때 `source` 로 필터하지 않는다.** 정본은 `inv_balance` 뷰이고 `source` 를 구분하지 않는다.
- ⚠️⚠️ **`line_ref` 를 집계 키에 넣지 않는다.** 접미어 때문에 원본과 상쇄가 다른 키가 된다. 판매 소진량은
  **`(doc_number, sku, warehouse)` 키 순액**으로 구한다(순액이 0 이상이면 소진하지 않는다 = 완전 상쇄된 판매).
  · [근거] 그 키 수 13,183 = `cin7 sale_out` 행 수 ⇒ 판매에서 이미 유일하다.
  · ⚠️ `replace(line_ref,':reversal','')` 금지 — 문자열을 만지는 방식이다.
- ⭐ **트랜스퍼도 같은 키 순액 방식이 성립한다**(방식·접미어·타입이 섞여 있어도 `doc_net` 0). ⚠️ 단 **`warehouse` 를 키에서
  빼지 말 것** — 출발과 도착이 상쇄돼 이동 자체가 사라진다(leg 는 `event_type` 으로 한 번 더 가른다 · §원가 레이어 10번).

#### 원가 레이어 (2026-09-08 · 그릇 + 기초 적재까지)

**1. 무엇을 만드는가**
FIFO 원가 레이어. 「지금 재고 100개의 원가가 얼마」에 답한다.
`inv_cost` 는 입고 라인마다 단가를 붙이는 데까지 돼 있고(922행), **레이어가 없다.**

**2. 테이블** (마이그레이션 `20260908195949` · 커밋 `edf1393`)
- `inv_layer` — SKU×창고 · PO 라인 단위 · `parent_layer_id` 로 트랜스퍼 사슬
- `inv_layer_cost_add` — 뒤늦게 붙는 원가 (`kind`: `landed` | `transfer_freight`)
- `inv_layer_consume` — 소비 기록 (소비 시점 `unit_cost` 를 굳힌다)
- `inv_layer_open` — 잔량 뷰 (`remaining_qty` · `total_cost` · `remaining_cost`)
⚠️ **`total_cost` 는 레이어 전체 원가다. 재고 평가액에 쓸 값은 `remaining_cost`.**

**3. ⚠️⚠️ 불변 조건**
레이어는 **`inv_ledger` + `inv_cost` + `inv_snapshot` 으로 전량 재생성 가능해야 한다.**
손으로 만든 값을 넣지 않는다. 이것이 나중에 bin 층을 추가할 수 있는 **유일한 근거**다
(지금은 창고 단위).

**4. 확정된 설계 판단 (2026-09-08)**

| 판단 | 근거 |
|---|---|
| **레이어 단위 = SKU×창고** (bin 아님) | 트랜스퍼 운송비가 재고 단가의 **16%** 규모라 창고를 갈라야 한다. 반대로 bin 까지 쪼개면 bin 트랜스퍼(784건 중 772건)마다 레이어를 쪼개야 하는데 그쪽은 운송비도 나이 변화도 없다 |
| **bin 은 나중에 추가 가능** | 원장이 bin 단위이므로 재구축할 수 있다. ⚠️ 단 **§3 불변 조건이 지켜지는 동안만** |
| **IN_TRANSIT 도 창고로 취급** | 원장과 축이 같아야 대조가 된다. 건너뛰면 미도착 재고(597행)의 원가가 어디에도 없어 평가액 총액이 빈다 |
| **`inv_layer` 에 유니크 제약 없음** | 자연키가 성립하지 않는다(부분입고 · 트랜스퍼 분할 · baseline 의 null `doc_number`). 이중 처리는 **`inv_layer_open` 잔량 vs `inv_balance` 대조**로 잡는다 |
| **`inv_layer_cost_add` 는 유니크 필수** | 수집기가 같은 문서를 매일 다시 읽는다 |
| **소비 기록을 남긴다** | 반품 원가 되짚기와 뒤늦은 원가 차액 계산에 필요하다 |

**5. 사건별 규칙** (원장 `event_type` 기준 · 실측 행수)

| 사건 | 행수 | 처리 | 원가 출처 |
|---|---|---|---|
| `baseline` | 13,830칸 | 생성 1회 | `inv_snapshot.value` |
| `po_in` | 536 | 생성 | `inv_cost` (goods + landed) |
| `sale_out` | 13,183 | FIFO 소진 | 레이어 → COGS |
| `transfer_out` · `transfer_in` | 3,680 · 3,083 | 이동(나이·원가 유지) | 원본 레이어 |
| `adjust_new` | 28 | 생성 | Cin7 UnitCost (`raw.line.UnitCost` · 실측 $0.24~$15.67) |
| `adjust_existing` + | 41 | 생성 | 그 SKU×창고 남은 레이어 가중평균. ⚠️ **ExistingStockLines 에는 UnitCost 가 없다**(157행 전부) |
| `adjust_existing` − | 116 | FIFO 소진 | 레이어 (⚠️ 손실 계정) |
| `assemble_out` → `assemble_in` | 16 → 5 | 부품 소진 → 완제품 생성 | 부품 레이어 합 |
| `credit_in` | 18 | 생성 | ⭐ **두 갈래** — 원 판매가 원장에 있으면 그 소비 기록을 되짚어 복원(`raw.header.order_number` → `sale_out` 조인) · 없으면 남은 레이어 가중평균. [실측] 18행 중 **8행만 되짚기 가능**(나머지는 기초 이전 판매의 반품) |

**6. ⚠️ 예외 — IN_TRANSIT 음수 잔고 215칸 (−1,999개)**
[실측 2026-09-08] `inv_balance` 에서 **IN_TRANSIT 축만** 음수 칸이 있다(215칸 · −1,999).
실제 창고(real 축 15,362칸)에는 음수가 **0**이다.
원인: 8/20 기초 이전에 출발해 이후 도착한 트랜스퍼는 leg 1·2 가 since 필터에 걸려
원장에 없고 leg 3·4 만 기표됐다 — **운송중에서 빼기만 한 셈**이다.
⚠️⚠️ **버그가 아니다. 재기준선이 지운다.**
⇒ 레이어 처리 규칙 (사건 적재 함수에 구현할 것):
- IN_TRANSIT 에서 소진하려는데 레이어가 없거나 부족하면 **있는 만큼만 소진**하고
  부족분은 소비 기록을 만들지 않는다
- ~~그 트랜스퍼의 도착 레이어는 `parent_layer_id = null` · **원가 미상**으로 만든다
  (기초와 같은 취급)~~
  ~~⚠️⚠️ **[정정 2026-09-09 · 구현 실측] 부족분에 대해 도착 레이어를 만들지 않는다
  — 원가 미상으로 채우지 않는다.**
  만들면 **없는 재고를 만드는 것**이다 — 원장이 IN_TRANSIT 잔고를 영구히 음수로
  남겨두고 채우지 않으므로, 레이어가 채우면 두 축이 어긋나 대조가 깨진다.
  ⭐ 「상쇄하면 없던 사건을 만드는 셈이다」(2026-08-28 IN_TRANSIT 기초 경계 결정)와
  같은 판단이다.
  ⇒ **있는 만큼만 소진하고 그만큼만 도착 레이어를 만든다. 부족분은 소비 기록도
  도착 레이어도 만들지 않고 `short_events` 로 센다.**
  ⚠️ 대가는 에드먼튼 재고가 그만큼 레이어에 없는 것이고, ⭐ 그것이 `short_events` 에
  세지므로 **얼마나 비었는지는 보인다**(「모르면 비워둔다」). 재기준선이 지운다.
  📌 「도착 레이어를 원가 미상으로 만드는」 경우는 **대응 out 행 자체가 없을 때**로
  좁혀졌다(`transfer_orphan_in`) — [실측] 이번 데이터에 **0건** · 방어 경로다.~~
  ⚠️⚠️ **[재정정 2026-09-09 저녁 · 실측] 위 「만들지 않는다」가 오판이었다.
  부족한 만큼 도착 창고에 원가 미상 레이어를 만든다 — 1차 판정이 맞았다.**
  (마이그레이션 `20260909190145`)

  ⚠️ **왜 두 번 뒤집었나 — 두 축을 섞었다.**
  2차 판정의 근거는 「없는 재고를 만들면 안 된다」였는데, 「없는 재고」는
  **IN_TRANSIT 축**의 음수 얘기다. **에드먼튼(real 축)에는 재고가 실재한다** —
  원장이 인정하고 Cin7 Movements 도 인정한다.

  ⭐ **결정적 실측 `RIN01018`(에드먼튼)**: `inv_balance` 는 34개를 인정하는데
  레이어는 **0개**였다. 원인은 `TR-03975` 가 도착 leg 만 있고(8/14 출발 · `since` 로
  정상 제외) IN_TRANSIT 에 소진할 레이어가 없어 short 로 떨어진 것이다.
  ⚠️⚠️ **그러면 연쇄가 난다** — 그 SKU 의 이후 bin 이동·판매가 전부 부족이 된다.
  [실측] `same_day` 부족 207건·1,639개가 전부 이 연쇄이고, 에드먼튼 레이어가
  원장보다 **−2,449** 모자랐다(IN_TRANSIT 은 정확히 짝인 **+2,450** 초과).

  ⇒ ⭐ **판정 기준: 「원장(real 축)이 그 재고를 인정하는가」.**
  인정하면 레이어를 만든다. **원가를 모르는 것과 재고가 없는 것은 다르다** —
  「모르면 비워둔다」는 원가를 비우는 것이고 재고를 없애는 것이 아니다.

  📌 카운터는 `transfer_unknown_layers` 로 따로 센다
  (`transfer_orphan_in` = out leg 행 자체가 없는 방어 경로 · 실측 0건과 구분).
  ⚠️ `short_events` 는 올리지 않는다 — 도착 레이어를 만들었으므로 부족이 아니다.
  ⚠️ **단 IN_TRANSIT 출발 부족만이다**(정확히는 그 문서의 IN_TRANSIT in leg 가 원장에 없는
  기초 이전 출발). **창고 출발 부족은 미처리 사건이 원인이라 `short_events` 로 남긴다** —
  「영구히 모르는 것」과 「아직 안 붙인 것」을 섞지 않는다. 미처리 사건 넷을 다 붙인 뒤
  창고 출발 부족이 남으면 그때 다시 판단한다(Caleb 판정 2026-09-09 저녁).
  ⚠️ `sale_out` 은 다르다 — 판매가 부족하면 그대로 short 다(재고가 나가는 사건이므로
  원가 미상 레이어를 만들 이유가 없다).
- ⚠️ **조용히 넘어가지 않는다** — 함수 반환 jsonb 에 건수를 담는다
  (창구가 없어 일주일 묵은 `SO-15440` 의 교훈)
- ⭐ **real 축에는 이 경로가 없다** — 이 예외의 유일한 감시 구역이 IN_TRANSIT 이다

**7. 기초 적재** (마이그레이션 `20260908202555`)
`inv_layer_seed_baseline(p_snapshot_key, p_force)` · security definer
가드 넷: 기존 baseline / 소비·cost_add 참조 / 빈 스냅샷 키 / 음수 평가액
[실측 테스트 DB 적재] **13,830 레이어 · 1,909,097개 · $3,055,493.67** ·
`received_on` 2026-08-20 · `age_known` false
⚠️ `total_value` 가 스냅샷 실측($3,055,493.66)보다 **1센트 크다** — `unit_cost` 를
소수 6자리로 반올림한 뒤 다시 곱해 합산한 누적 오차다. 칸별 절대값 합계 $18.70 ·
최악 $0.0074 · 1센트 넘게 어긋난 칸 0개. 오차율 0.0006%.
📌 원인을 아는 오차이므로 크기와 이유를 여기 적어둔다.

**8. ⭐ 첫 대조 결과 — 원장이 채점자로 작동한다**
[실측 2026-09-08] 레이어 잔량 vs `inv_balance.baseline_qty`:
칸 13,830 · `baseline_mismatch` **0** · 총량 양쪽 1,909,097 일치 ·
`layer_only` 0 · `ledger_only` 1,561(8/20 이후 새로 생긴 칸)
⇒ **유니크 제약 대신 대조로 가기로 한 §4 판단이 실물로 뒷받침됐다.**
⚠️ **대조할 때 잔고 0 인 칸을 빼지 말 것** — real 축 delta −38,501 중 −22,804 가
「다 팔려서 0이 된 칸」이고, 그 레이어도 **완전히 소진**돼야 한다.
qty>0 만 걸면 소진이 덜 됐는지 확인할 수 없다.

**9. ⚠️ 부분 처리 상태에서는 잔량 대조가 성립하지 않는다 (실측 2026-09-09)**

`inv_layer_apply` 로 `po_in`(536) + `sale_out`(13,183)만 처리한 상태에서
「레이어 잔량 = 원장 잔고 − 미처리 사건 합」을 검산했으나 **맞지 않았다**:
real 축 잔차 −11,449 · IN_TRANSIT +1,247.

⚠️ 원인은 계산 오류가 아니라 **원리**다 — 미처리 사건(트랜스퍼·조정 등)으로 들어온
재고는 레이어가 없으므로 그 SKU×창고의 판매가 **기초 레이어를 대신 소진한다.**
⭐ 수량은 그럴듯하게 남고 `short_events` 도 안 늘지만 **어느 레이어에서 빠졌는지가
틀린다.** 그 어긋남은 미처리 사건 합으로 환산되지 않는다.

⇒ ⭐ **잔량 대조(§8의 안전망)가 의미를 갖는 것은 사건 9종을 다 처리한 뒤**다.
⚠️ 부분 처리 중에는 그 대조로 판정하지 말 것.

📌 **다만 이 사실이 §4 판단을 흔들지는 않는다** — 기초만 있던 상태에서는
`baseline_mismatch 0` 으로 대조가 정확히 작동했다(§8). 대조는 유효하고,
**전제가 「전량 처리」라는 것이 이번에 드러났다.**

**⭐ 그 대신 이번 단계에서 확인된 것**

| | |
|---|---|
| 성능 | **2,708 ms** (13,719 사건) — 전량 재생성이 감당된다. 최적화 불필요 |
| 재현성 | 두 번 불러도 반환·행 수 동일 (⭐ 전량 재생성의 핵심) |
| `layers_created` | **536** = 원장 `po_in` 행 수. ⚠️ 합쳐진 것이 0 — 발주는 `(doc_number, line_ref, sku, warehouse)` 로 이미 유일하다 (「원장은 bin 단위」가 이 축에는 해당하지 않는다) |
| `cost_unknown_layers` | **22** = `PO-01227`(21) + `PO-01237`(1) · ⚠️ 결손이 아니라 **테스트 DB 시점 문제**(원장 09-08 07:38 기입 · `cost` 커서 00:33) |
| `short_events` | **206** — ⭐ 전부 규명됨(아래) |

**⭐ `short_events 206` 의 정체 — 둘 다 미처리 사건이 원인이다**
- 토론토 126건 · 636개 — ⭐ **소분 제품군**(`AS91...`). 벌크를 소분해 파는 제품으로
  재고가 `adjust_existing` 증가로 생긴다([실측] `AS91463` 기초 26 + `ST-01240` +188 ·
  `ST-01298` +15 · `ST-01305` +15 · 판매 −224 ⇒ 부족 198).
  ⚠️ **벌크는 non-inventory 로 입고**되므로(Caleb 확인) 원장·원가 어느 쪽에도 없다
  ⇒ ⭐ 조립처럼 「부품에서 원가를 가져오는」 경로가 **성립하지 않는다.**
  `adjust_existing` 증가의 「남은 레이어 가중평균」이 유일한 근거이고 그것이 맞다.
  ⚠️ 다만 ⬜ **남은 레이어가 0 일 때**(다 팔린 뒤 조정으로 재충전)는 미정 —
  마지막 소비 단가 · `unknown` 두 갈래
- 에드먼튼 80건 · 451개 — 트랜스퍼가 미처리라 에드먼튼 레이어가 아예 없다.
  ⚠️ 에드먼튼은 PO 가 없고 **트랜스퍼가 유일한 유입 경로**다
- 📌 ⭐ 스냅샷 결손은 아니다 — `AS91463` 기초 26개가 레이어에 정확히 들어가 있다

⇒ ⬜ **다음은 `transfer`** (6,763행 · 가장 크고 에드먼튼의 유일한 유입 경로)

⭐ **[2026-09-09 저녁] 전제가 풀렸다** — 사건 9종이 전부 붙어
`skipped_by_type` 이 `{}` 가 됐고, **잔량 대조가 처음으로 통과했다**(§12).

**10. ⚠️ 원장을 읽을 때 `source` 를 필터하지 말 것 (사고 2026-09-09)**

`inv_layer_apply` 세 함수가 `source = 'cin7'` 으로 필터해 `manual` 상쇄 행을
읽지 않고 있었다. [실측] 그래서 레이어가 원장보다 **+3,619** 남아 있었다
(`manual sale_out` 684행 +3,733 을 안 읽은 몫 등).

⚠️⚠️ **원장 잔고의 정본은 `inv_balance` 뷰이고 그 정의에 `source` 조건이 없다.**
상쇄는 `source='manual'` 로만 들어오므로 `cin7` 만 읽으면 **상쇄 전 세계**를 본다.

⚠️ **같은 함정으로 오진을 하나 냈다** — 트랜스퍼 5문서를 「이중 기표 · 순액
−7,537」으로 보고했으나 실제로는 08-31~09-01 에 정상 상쇄돼 `doc_net 0` 이었다.
⭐ 그리고 「⑧ 대조가 깨끗한데 원장이 −7,537」이라는 **모순을 스스로 적어놓고도
내 쿼리를 의심하지 않았다.**

⇒ ⭐ **「⑧·② 대조가 깨끗한데 내 계산이 안 맞으면 감지 창구를 의심하기 전에
① 내 쿼리의 `source` 필터 ② `line_ref` 를 키에 넣었는지를 먼저 볼 것.」**

**⭐ 상쇄 관례 (원장 실측 2026-09-09)**

| 방식 | 쓰이는 상황 | 접미어 |
|---|---|---|
| `event_type` **유지** (`sale_out`·`transfer_in`·`transfer_out` 등) | 사건 자체를 취소·정정 | ⭐ `line_ref` 에 `:reversal` (같은 `doc_number` 안이라 키를 갈라야 한다) |
| `event_type = 'manual_reversal'` | 같은 사건을 **다른 키로 다시 기표**해 옛 벌을 무효화 | 없음 (타입으로 갈린다) |

📌 실측 분포: `manual_reversal` 592행(전부 `transfer` · 08-31 20:11 의 **사건 하나**) ·
타입 유지 987행(`sale_out` 684 · `transfer_out` 151 · `transfer_in` 138 ·
`assemble_out` 8 · `adjust_new` 3 · `assemble_in` 3) · `manual po_in` **0건**.

⚠️ 스킬 §진행 상태의 「재생성 짝을 상쇄할 때 접미어를 바꿀 필요가 없다」는
**`doc_number` 가 다른 경우**(조립 `FG-00133`/`FG-00134`)다 — 같은 문서 안 상쇄와
다른 상황이다.

**⭐ 처방 — 키 순액 방식이면 관례를 알 필요가 없다**

소진량을 원장 **행**이 아니라 `(doc_number, sku, warehouse)` **순액**으로 구한다.
⚠️ `line_ref` 를 키에 넣지 않는다 — 접미어 때문에 상쇄가 원본과 다른 키가 된다.
⚠️ `replace(line_ref, ':reversal', '')` 는 **금지**다(문자열을 만지는 방식 ·
규칙이 바뀌면 조용히 깨진다 · 「SKU 접미사 파싱 금지」와 같은 정신).

[실측 검증 · 같은 데이터를 세 키로 묶은 결과]

| 키 | 키 수 | 완전 상쇄 | 부분 | ⚠️ 순액 양수 |
|---|---|---|---|---|
| `doc_number, line_ref, sku, warehouse` | 13,865 | 0 | 3 | **681** |
| `+ replace(line_ref,…)` | 13,187 | 680 | 3 | 1 |
| ⭐ **`doc_number, sku, warehouse`** | **13,183** | 681 | 3 | **0** |

⇒ 세 번째만 정합하다. 📌 키 13,183 = `cin7 sale_out` 행 수와 일치.

⚠️ **트랜스퍼는 `event_type` 으로 leg 를 한 번 더 갈라야 한다** — IN_TRANSIT 은
한 문서에 `in`·`out` 이 함께 있어 `(doc, sku, warehouse)` 만으로는 순액이 0 이 된다.
out leg = `transfer_out` + `manual_reversal` · in leg = `transfer_in`.

**⚠️ 같은 날 두 `in` 의 순서 (구현 2026-09-09)**

쌍1·쌍2 가 같은 날이면 두 `in` 행이 **모두 `seq_hint 1`** 이라 `id` 로만 갈리고,
쌍2(창고 in)가 쌍1(IN_TRANSIT in)보다 먼저 올 수 있다 — 그때 IN_TRANSIT 레이어가
아직 없어 부족으로 오처리된다.
⇒ ⭐ **창고 `in` 을 처리할 때 같은 `(doc, sku)` 의 미처리 IN_TRANSIT `in` 이 있으면
그것을 먼저 처리한다**(중첩 호출). 순서에 기대지 않는다.

**⚠️ 오늘 낸 쿼리 실수 셋 (2026-09-09) — 전부 「데이터가 틀렸다」로 오진했다**

| 실수 | 잘못된 결론 | 실제 |
|---|---|---|
| `source='cin7'` 필터 | 트랜스퍼 5문서 「이중 기표 · 순액 −7,537」 | 08-31~09-01 에 정상 상쇄돼 `doc_net 0` |
| `line_ref` 를 키에 포함 | 「상쇄가 원본과 짝이 안 맞는다 · 순액 양수 681」 | `:reversal` 접미어 때문 · 키에서 빼면 짝이 맞는다 |
| `inv_balance` 를 bin 단위 행과 조인 | 「레이어가 원장보다 149,028 초과」 | 레이어가 여러 번 세어진 것 · SKU×창고로 먼저 합치면 +1,015 |

⇒ ⭐ **공통점: 셋 다 「내 계산이 이상하다」를 「데이터가 이상하다」로 읽었다.**
⚠️ 원장은 **합계만** 맞으면 되고 원가 레이어는 **개별 추적**이 필요하다 —
그래서 원장이 통과한 데이터에서도 레이어는 계속 걸린다. ⭐ 그때 의심할 순서는
**① 내 쿼리 ② 미처리 사건 ③ 데이터**다.

**11. ⭐ `transfer` 적재 완료 — 실측 (2026-09-09)**

마이그레이션 `20260909174046`(이동 신규 · `source` 필터 제거 · sale 키 순액) ·
`20260909183253`(카운터 거짓 신호 수정) ·
`20260909190145`(기초 이전 출발 → 원가 미상 도착 레이어).

**`select inv_layer_apply()` 반환 [실측 테스트 DB]**

| | |
|---|---|
| `elapsed_ms` | **6,584** (전량 재생성 · 사건 21,000여 건) |
| `layers_created` (po_in) | 536 · 그중 `cost_unknown_layers` **22** (수집 지연 · 결손 아님) |
| `sale_keys_processed` | 13,183 · `sale_keys_fully_reversed` **681** (상쇄 흡수) |
| `transfer_layers_created` | **3,300** ⭐ 에드먼튼 레이어가 처음 생겼다 |
| `transfer_unknown_layers` / `_qty` | **245 / 2,286** — 기초 이전 출발분 |
| `transfer_keys_net_zero` | 138 (상쇄로 소멸한 in 키 · 정보 카운터) |
| `transfer_orphan_in` · `transfer_out_unpaired` | **0 · 0** ⭐ 둘 다 「0 이 아니면 신호」 |
| `short_events` | ⭐ **796 → 81** |
| `consume_rows` | 15,572 |

⭐ **`transfer_unknown_layers 245` · `_qty 2,286` 이 사전 실측
`no_IT_in 245건 · 2,286개`와 정확히 일치한다** — 기초 이전 출발분의 전량이다.

**⭐ `short_events` 가 796 → 81 로 떨어진 것이 이번 작업의 핵심 성과**다.
⚠️ 원인은 하나였다 — 기초 이전 출발 트랜스퍼가 도착 레이어를 못 만들어
**그 SKU 의 이후 모든 사건이 부족으로 연쇄**됐다(§6 재정정 참조).
⇒ 뿌리를 막으니 파생 부족이 함께 사라졌다.
📌 남은 **81 은 「아직 안 붙인 사건 넷이 만든 부족」**이고 ⭐ 그 크기를 말해주는
신호다. `adjust_existing` 등을 붙이면 줄어야 한다.

**⚠️ 잔량 gap 의 정체 — 셋으로 갈린다**

⚠️ **잔량 대조는 사건 9종 전량 처리 뒤에 의미를 갖는다**(§9). 아래는 「지금
gap 이 무엇으로 설명되는가」이고 **합격 판정이 아니다.**

[실측] `inv_layer` 잔량 − `inv_balance` (SKU×창고로 합쳐서 비교):

| 축 | 칸 | 원장 | 레이어 | gap |
|---|---|---|---|---|
| TOR | 7,615 | 1,780,984 | 1,781,999 | **+1,015** |
| EDM | 6,565 | 89,612 | 89,151 | **−461** |
| IN_TRANSIT | 1,211 | 5,944 | 8,394 | **+2,450** |

📌 미처리 사건 넷의 순액은 **−321** 이다
(`adjust_existing` −786 · `adjust_new` +364 · `assemble` −46 · `credit_in` +147).

**① ⭐ IN_TRANSIT `+2,450` — 구조적이다. 고칠 것이 없다**

[실측] IN_TRANSIT 을 원장 부호로 갈라보면:

| | 칸 | 원장 | 레이어 | gap |
|---|---|---|---|---|
| 원장이 음수인 칸 | **215** | −1,999 | 106 | **+2,105** |
| 그 외 | 996 | 7,943 | 8,288 | +345 |

⚠️⚠️ **원장은 음수를 허용하고 레이어는 못 한다**(`inv_layer_qty_ck check (qty > 0)`).
못 뺀 만큼 0 에서 멈추므로 gap 이 생긴다.
📌 그 215칸은 스킬에 이미 판정돼 있다 — 「**IN_TRANSIT 잔고는 영구히 음수로
남는다 · 버그가 아니다**」(2026-08-28 기초 경계 결정). 8/20 이전 출발분의 leg 1·2 가
`since` 로 정상 제외된 결과다.
⇒ ⭐ **두 축이 구조적으로 다른 것이고 재기준선이 지운다.**
⚠️ 다음에 잔량 대조를 볼 때 「IN_TRANSIT 이 왜 +2,450 인가」를 다시 파지 말 것.

**② TOR `+1,015` · EDM `−461` — 미처리 사건 넷 몫으로 보인다**

순액 −321 과 자리수가 맞고, IN_TRANSIT 의 `+345`(양수 칸)도 같은 계열로 보인다.
⚠️ **확정은 아니다** — 넷을 다 붙인 뒤 gap 이 0 에 수렴하는지가 판정이다.

⚠️ **위 gap 표는 `transfer` 까지만 붙인 시점의 값이다.**
`adjust` 를 붙인 뒤에는 미처리가 `assemble`(32행 · net −46) ·
`credit_in`(18행 · net +147) 둘로 줄었다 ⇒ 순액 **+101**.
⬜ 그 둘까지 붙이고 나서 gap 이 0 에 수렴하는지가 **최종 판정**이다.

**⭐ 확정된 설계 판단 (이번 작업에서 추가)**

| 판단 | 근거 |
|---|---|
| 원장을 읽을 때 `source` 를 필터하지 않는다 | §10 |
| 소진량은 `(doc_number, sku, warehouse)` 키 순액 | `:reversal` 접미어를 파싱하지 않아도 상쇄가 자동 흡수된다 (§10) |
| ⭐ 트랜스퍼는 `event_type` 으로 leg 를 한 번 더 가른다 | IN_TRANSIT 은 한 문서에 `in`·`out` 이 함께 있어 창고 키만으로는 순액이 0 이 된다. out leg = `transfer_out` + `manual_reversal` · in leg = `transfer_in` |
| ⭐ 같은 날 두 `in` 은 순서에 기대지 않는다 | 쌍1·쌍2 가 같은 날이면 두 `in` 이 **모두 `seq_hint 1`** 이라 `id` 로만 갈린다 ⇒ 창고 `in` 처리 시 같은 `(doc, sku)` 의 미처리 IN_TRANSIT `in` 을 먼저 처리한다(중첩 호출) |
| ⭐ 기초 이전 출발분만 원가 미상 도착 레이어 | 조건 **둘** — IN_TRANSIT 출발 부족 **AND** 쌍1(`transfer_in@IN_TRANSIT`)이 원장에 없음. ⚠️ 한 줄이면 창고 출발 부족이 쌍2 로 전파돼 원가 미상으로 덮인다 (§6 재정정) |
| ⚠️ `sale_out` 부족은 그대로 `short` | 판매는 재고가 나가는 사건이므로 원가 미상 레이어를 만들 이유가 없다 |
| ⚠️ 카운터는 실제로 그 일이 일어났을 때만 센다 | `transfer_orphan_in` 이 레이어를 안 만들어도 올라가 평상시 138 을 냈다 ⇒ 「0 이 아니면 신호」가 죽는다. 스킬 「기준선 3을 외운다」와 같은 실패 모양 |

**⚠️ 상쇄 관례의 정본은 §상쇄(정정) 계약이다**

원장 쪽에서 `ledger-design.md` 에 **`#### 상쇄(정정) 계약` 절을 신설**했다
(2026-09-09 · 커밋 `f7bfc19` · 수집 계약 뒤 · 원가 레이어 앞).
⇒ ⭐ **상쇄 방식·접미어·`manual_reversal` 의 성격은 그 절이 정본이다.**
§10 의 상쇄 관례 표는 **원가 레이어가 그것을 어떻게 흡수하는지**를 적은 것이고,
계약 자체가 바뀌면 그 절을 따른다.

📌 실측 근거 정본은 `docs/sessions/2026-09-09-reversal-conventions.md` 다.
📌 트랜스퍼 `manual` 상쇄는 **세 묶음**이다(① `TR-04175` 라인 삭제 정정 08-25 ·
② 출발 bin 해결 옛 벌 상쇄 08-31 · ③ 09-01 후속 정정 13행).
📌 세 묶음이 확정값이다 — 스킬·세션 기록에도 처음부터 「셋」으로 들어가 있다.
⭐ **키 순액 방식이면 세 묶음을 구분할 필요가 없다** — 함수가 관례를 알 필요가 없다.

**⭐ `adjust_existing` + `adjust_new` 적재 완료 (2026-09-09 저녁)**

마이그레이션 `20260909201223`.

| | |
|---|---|
| `short_events` | ⭐ **81 → 22** (트랜스퍼 전 796 → 81 → **22**) |
| `adjust_layers_created` | **66** = 증가 41 + 신규 25 (사전 실측과 정확히 일치) |
| `adjust_consume_rows` | 119 (감소 116키가 119행에 걸쳐 소진) |
| `adjust_new_net_zero` | **3** — `manual` 3행이 3키를 완전 상쇄 |
| `adjust_unknown_layers` | **0** ⭐ 「남은 레이어가 0」 경우가 없다 |
| `processed_adjust` | 188 |
| `elapsed_ms` | 6,712 |

⚠️ `transfer_layers_created` 가 3,300 → **3,299** 로 하나 줄었다 —
조정 증가가 IN_TRANSIT 부족을 채워 레이어 하나가 `unknown` 대신 정상 경로를
탔다. ⭐ 좋은 방향이다.

**⚠️ 남은 `short_events 22` 의 성격**

⚠️ **함수가 센 22 가 맞는 값이다.** 밖에서 `event_type` 으로 갈라 세면 부풀어
보인다 — [사고 2026-09-09] `manual_reversal` 592행을 `transfer_out` 에서 빼지 않아
「토론토 출발 7,439 부족」으로 읽었고, `transfer_unknown` 으로 처리된 2,286 도
「소진 안 됨」으로 읽혔다.
⇒ ⭐ **`short_events` 는 함수 반환값으로 읽는다.** 재계산하려면 함수와 **같은 방식**
(키 순액 · `event_type` 으로 leg 구분 · `unknown` 처리분 제외)이어야 한다.

📌 [실측] 밖에서 다시 센 결과 실제 남은 것은 `sale_out` TOR **8건 · 19개**이고,
⬜ `assemble`·`credit_in` 미처리 몫으로 보인다(확정은 그 둘을 붙인 뒤).

**⭐ 확정된 규칙**

| | |
|---|---|
| 분기 | ⚠️⚠️ **`event_type` 이 아니라 `qty_delta` 부호로.** `adjust_existing` 은 `seq_hint` 1(41행 양수)·2(116행 음수) **양쪽에 있다.** [실측] 키 순액으로 묶으면 `net_pos 41` · `net_neg 116` · ⭐ **`net_zero 0`** — 한 키에 증가와 감소가 섞이지 않는다 |
| 증가 41키 (+1,290) | 새 레이어 · `cost_source = 'layer_avg'` · **그 SKU×창고 남은 레이어 가중평균** |
| 감소 116키 (−2,076) | FIFO 소진 · `reason = 'adjust_out'` · 부족하면 `short_events`(⚠️ 원가 미상 레이어를 만들지 않는다 — 재고가 나가는 사건이다) |
| `adjust_new` 25키 | 새 레이어 · `cost_source = 'cin7_unitcost'` · **Cin7 `raw.line.UnitCost`** 수량 가중평균. [실측] 28행 전부 존재 · $0.24~$15.67 · 0원 없음 · ⭐ `net_neg 0` 이라 생성만으로 된다 |
| 키 | ⚠️ `(doc_number, sku, warehouse, **event_type**)` — 같은 `ST-` 문서가 `Existing`·`New` 라인을 함께 가지면 두 규칙이 한 순액에 섞인다. **분기는 부호로, 키는 타입별로** |

**⚠️ 가중평균이 유일한 근거인 이유**

Cin7 이 `UnitCost` 를 주지 않는다 — [실측] `ExistingStockLines` **157행 전부**
`raw.line.UnitCost` 가 없고, `adjust_new`(`NewStockLines`) 28행은 전부 있다.
📌 스킬 「`NewStockLines` 는 규칙이 다르다」의 실체가 이것이다.

⭐ **그리고 실무 근거가 있다** — 증가의 실체는 **소분 제품군**(`AS91...`)이고
벌크가 **non-inventory 로 입고**된다(Caleb 확인 2026-09-09).
⇒ ⚠️ **조립처럼 「부품에서 원가를 가져오는」 경로가 성립하지 않는다** —
벌크는 원장·원가 어디에도 없다.
⇒ ⭐ **같은 자리에 있던 물건의 평균이 유일한 근거이고 그것이 맞다.**
📌 [실측 `AS91463`] 기초 26 + `ST-01240` +188 + `ST-01298` +15 + `ST-01305` +15 ·
판매 −224. 검증에서 조정 레이어 셋의 단가가 **전부 기초 단가와 같게** 나왔다
(남은 레이어가 기초뿐이므로 가중평균 = 기초 단가).

**⚠️⚠️ 조정 사유는 판단하지 않는다**

[실측] `raw.line.Comments` 가 감소 **116행 전부 빈 문자열**이고 `raw.header` 에도
사유 필드가 없다(`doc_number` · `effective_date` · `header_location_id` ·
`status` · `task_id`).
⭐ 그리고 **조정 사유는 정말 다양하다**(Caleb 확인) — WMS 실사만이 아니다.

⇒ ⭐ **레이어는 「조정으로 빠졌다」까지만 기록한다.** 파손인지 실사 차이인지
재분류인지는 **회계 판단**이고, `reason='adjust_out'` 이 판매(`sale`)와 구분되므로
나중에 갈라 쓸 수 있다. 되짚을 근거는 `doc_number`(`ST-` 번호)다.
⚠️ **사유를 추측해 계정을 배정하면 나중에 고칠 수 없다**(원가는 소급 불가).

**⏸ 표본 대기 — 「남은 레이어가 0」일 때의 원가**

[실측 2026-09-09] `adjust_existing` **157건 전부 조정 직전 재고가 양수**였다
(`prior_zero_or_neg 0`) · 실행에서도 `adjust_unknown_layers 0`.
⇒ **평균낼 대상이 없는 경우가 아직 없다.**

⚠️ 방어만 해뒀다 — 남은 레이어 합이 0 이하면 `cost_source='unknown'` ·
`unit_cost 0` 으로 만들고 `adjust_unknown_layers` 로 센다.
⭐ **그 카운터가 0 이 아니게 되는 것이 표본이 나타났다는 신호다.**
⬜ 규칙 후보: 그 SKU 의 **마지막 소비 단가** · `unknown` 유지. **지금 정하지 않는다.**

📌 **원가 0 은 정상이다** — 서플라이어 프로모션 무상 재고가 실재한다
(기초 스냅샷에 51칸). ⚠️ 예외 처리하지 않는다.

**⚠️ 가드 — `UnitCost` 음수는 거부한다**

`inv_layer_cost_ck(unit_cost >= 0)` 에 걸려 어차피 실패하는데, 그때는 제약 위반
메시지만 나오고 **어느 문서 때문인지 알 수 없다.** ⇒ 키와 값을 담아 예외를 던진다.
📌 세 경로가 갈린다 — **음수는 예외 · 0 은 통과 · 읽을 수 없으면 `unknown`.**
📌 `po_in` 의 같은 가드(`20260909155320`)와 같은 취지다.

**⬜ 다음 — `assemble`(32행) · `credit_in`(18행)**

사건 9종 중 **7종이 붙었다.** 남은 둘:

- **`assemble`** — `assemble_out`(24행 · −46) → `assemble_in`(8행 · +2).
  ⭐ 부품 소진 → 완제품 생성이고 **원가는 부품 레이어 합**이다(추정이 아니라 계산).
  ⚠️ `doc_number`(`FG-` 번호)로 짝을 찾아야 한다. ⚠️ `manual` 이 양쪽에 있다
  (`assemble_out` 8행 +28 · `assemble_in` 3행 −3).
- **`credit_in`** — 18행 · +147. ⭐ **두 갈래** — 원 판매가 원장에 있으면 그
  소비 기록을 되짚어 복원(`raw.header.order_number` → `sale_out` 조인) · 없으면
  남은 레이어 가중평균. [실측] 18행 중 **8행만 되짚기 가능**(나머지는 기초 이전
  판매의 반품).
  ⚠️ restock 라인만 원장에 온다 — 파손·미수령 크레딧노트는 재고를 안 움직이므로
  다룰 것이 없다.

⚠️ **그 전에 테스트 DB 재복사를 한다**(§테스트 DB) — 뒤처진 규모
`inv_ledger` `cin7` +1,402 · `inv_cost` +84.
⚠️ 재복사 후 위 숫자(`short_events 22` 등)를 기준선으로 쓰지 말 것.

**⚠️ 테스트 DB 재복사 — 다음 착수 전에 한다**

⭐ Caleb 판정(2026-09-09): **레이어 적재 함수 착수 시점**에 재복사한다.
[실측] 지금 뒤처진 규모 — `inv_ledger` `cin7` **+1,402** · `inv_cost` **+84** ·
⭐ `manual`(1,579)과 `inv_layer`(13,830)는 **양쪽 동일**하다.
📌 절차 정본은 §테스트 DB(세 단계 · 함정 넷 · ⚠️ `truncate inv_config;` 먼저).

⚠️ **재복사하면 레이어가 날아간다** — 불변 조건(§3)대로 함수를 다시 돌려 복원한다:
`inv_layer_seed_baseline('2026-08-20-initial')` → `inv_layer_apply()`.
⭐ 두 함수가 RPC 인 것이 이 때문이다.

⚠️ 재복사 후 숫자가 달라진다 — 위 실측(§11)은 **09-08 08:59 복사본 기준**이다.
`short_events 81` 등을 재복사 후 값과 직접 비교하지 말 것.

✅ **[2026-09-09 저녁] 완료** — 실측과 절차의 차이(비우는 단계)는 §원가 레이어
12번에 적었다. ⚠️ 이 블록의 뒤처진 규모(+1,402 · +84)는 **그때 값**이고
재복사 후 실측은 12번을 볼 것.

**12. ⭐⭐ 사건 9종 완료 · 잔량 대조 첫 통과 (2026-09-09 저녁)**

⚠️ **이 절의 숫자는 「그 시점 실측」이다** — 사건이 매일 쌓이므로 최신 값은 `select inv_layer_apply();` 와 `select sum(remaining_cost) from inv_layer_open;` 을 직접 돌려 본다. ⭐ 문서 값은 **당시 판단의 근거**이고 현재 상태가 아니다. 📌 §11 은 재복사 전(09-08 08:59 복사본) · §12 는 재복사 후 기준이다.

마이그레이션 `20260909231134`(assemble + credit_in) ·
`20260909233729`(IN_TRANSIT 문서 범위) · `20260909235347`(트랜스퍼 날짜 범위).

**⚠️ 먼저 — 테스트 DB 를 재복사했다**

09-08 08:59 복사본이 낡아 절차(§테스트 DB)대로 다시 만들었다.

⚠️ **문서의 「세 단계」는 최초 구축 절차이고 재복사와 다르다** — 이미 데이터가
들어 있는 DB 라 `COPY` 가 전부 중복 키로 막힌다. ⇒ **비우는 단계가 하나 더 필요하다:**

```
① 운영에서 덤프 (--linked · 읽기만)         ← 문서 그대로
②′ ⭐ 테스트에서 덤프 대상 표를 truncate ... cascade
    · 덤프에 들어온 31개 표 + inv_layer 계열 셋
    · ⚠️ 목록은 덤프 파일에서 뽑는다: grep -oP '^COPY "public"\."\K[^"]+'
    · ⭐ cascade 를 쓰면 FK 순서(wms_pallets 순환 · inv_layer)를 따질 필요가 없다
    · 📌 inv_config 도 여기 포함된다(함정 4 의 「truncate inv_config 먼저」)
③ 테스트에 복원                              ← 문서 그대로
④ ⭐ inv_layer_seed_baseline → inv_layer_apply 로 레이어 복원
```

📌 `inv_layer` 계열은 **운영에 없으므로 덤프에 안 들어온다** — 비우기만 하고
함수로 복원한다. ⭐ 두 함수가 RPC 인 것이 이 때문이다(§3 불변 조건).

**[실측] 재복사 전후**

| | 전(09-08 08:59) | 후(09-09 저녁) |
|---|---|---|
| `inv_ledger` cin7 | 20,706 | **23,680** |
| `inv_ledger` manual | 1,579 | 1,579 |
| `inv_cost` | 922 | **1,164** |
| `inv_snapshot` | 206,489 | 206,501 |
| `wms_orders` | 1,613 | 1,741 |
| `inv_config.baseline_snapshot_key` | `2026-08-20-initial` | ⭐ 그대로 |

⭐ **기초 레이어는 재복사 후에도 같다** — 13,830 · 1,909,097개 · $3,055,493.67.
8/20 스냅샷은 과거라 안 변한다.

⚠️ **`cost_unknown_layers` 가 22 → 73 으로 늘었다** — 결손이 아니라 **수집 지연**이다.
[실측] `PO-01214`(09-09 12:23 기입) · `PO-01234`(15:38)가 `cost` 커서(10:37)보다
나중에 원장에 들어왔다. ⭐ 반대로 어제의 22(`PO-01227`·`PO-01237`)는 **사라졌다** —
오늘 `inv-cost` 가 돌면서 붙었고, 그것이 소급 구조가 작동한다는 증거다.

**⭐⭐ 잔량 대조 — 실제 창고 14,185칸 전부 일치**

| 축 | 칸 | 원장 | 레이어 | gap | 어긋난 칸 |
|---|---|---|---|---|---|
| **TOR** | 7,620 | 1,811,533 | 1,811,533 | ⭐ **0** | ⭐ **0** |
| **EDM** | 6,565 | 86,661 | 86,661 | ⭐ **0** | ⭐ **0** |
| IN_TRANSIT | 1,227 | 5,944 | 8,503 | +2,559 | 259 |

⭐ **이것이 §4 「유니크 제약 대신 대조로 잡는다」의 최종 증명**이다.
⚠️ IN_TRANSIT 만 남고 그것은 **구조적**이다 — 원장은 음수를 허용하고 레이어는
`inv_layer_qty_ck(qty > 0)` 때문에 못 한다(§11 ① 참조 · 재기준선이 지운다).

**⭐ 재고 평가액 — 처음 나온 숫자**

**$3,073,830.45** (`select sum(remaining_cost) from inv_layer_open`)
⇒ ⭐ **[2026-09-10] PO landed 적재 후 `$3,075,916.90`** (+$2,086.45).
⇒ ⭐ **[2026-09-10] 트랜스퍼 운송비 배분 후 `$3,076,166.23`** (+$249.33).
⚠️ `freight_no_basis` $398.75 는 반영되지 않았다(아래 「트랜스퍼 운송비」 소절 D).
⚠️ 아래 대조 얘기는 **landed 적재 전 값 기준**이다.

📌 기초 스냅샷 Cin7 평가액 $3,055,493 대비 **+0.6%**. 그 사이 3주간 매입·판매가
있었으므로 자리수가 맞는 것이 확인 사항이다.
⚠️ Cin7 평가액과의 본격 대조는 별건이다(§13 미결).

**`inv_layer_apply()` 반환 [실측]**

| | |
|---|---|
| ⭐ `skipped_by_type` | **`{}`** — 사건 9종 전부 처리 |
| ⭐ `short_events` | **0** (796 → 81 → 24 → 14 → **0**) |
| `layers_created`(po_in) | 803 · `cost_unknown_layers` 73 |
| `transfer_layers_created` | 3,341 · `transfer_unknown_layers` 259 · `_qty` **2,559** |
| `adjust_layers_created` | 73 · `adjust_unknown_layers` **0** |
| `assembly_layers_created` | **2** (`FG-00130`·`FG-00132`) · `assembly_net_zero` **3** |
| `credit_layers_created` | 20 · `credit_traced` 8 · `credit_avg` 12 · `credit_unknown_layers` 1 |
| `transfer_orphan_in` · `transfer_out_unpaired` · `assembly_out_unpaired` | ⭐ 전부 **0** |
| `consume_rows` | 18,432 · `elapsed_ms` 7,843 |

**⚠️⚠️ 결함 둘 — 둘 다 IN_TRANSIT 축이었다**

**① FIFO 가 남의 문서 레이어를 먹었다 (`20260909233729`)**

[실측] IN_TRANSIT consume 1,164행 중 **88행 · 1,345개**가 다른 문서 레이어를 소진.

```
[VNA57973]
08-21  TR-04174  토론토 기초에서 −24 → IN_TRANSIT 레이어 생성
08-26  TR-03975  ⚠️ 그 레이어를 먹었다   ← 자기 IN_TRANSIT 레이어가 없어서
                                          (기초 이전 출발 · leg 1·2 가 since 로 제외)
09-01  TR-04174  IN_TRANSIT 에서 빼려는데 ⚠️ 이미 없다 → short
```

⭐ **창고와 IN_TRANSIT 은 다르게 다뤄야 한다** — 창고에서는 물건이 섞이는 것이
맞지만 **운송 중 재고는 문서별로 묶여 있다**(다른 트럭에 실려 있다).
⚠️ FIFO 의 전제(같은 자리 = 같은 뭉치)가 IN_TRANSIT 에는 성립하지 않는다.

⇒ `inv_layer_fifo_take` 에 `p_doc_scope text default null` 추가. IN_TRANSIT
출발일 때만 `doc_number` 를 넘긴다. ⚠️ **판매·조정·조립·창고 출발은 인자를 안
넘겨 종전대로 문서 경계 없이 돈다** — 공통 함수라 무조건 넣으면 판매가 깨진다.

⭐ **진단의 근거는 「TOR 은 이미 gap 0」이었다는 것** — 창고 축은 정상이고
IN_TRANSIT 만 틀렸으니 원인이 「FIFO 자체」가 아니라 **「IN_TRANSIT 에 FIFO 를
쓴 것」**으로 좁혀졌다.
📌 이 하나가 `EDM gap −461` · `short_events 14` 의 뿌리였다 —
남의 레이어를 먹혀서 주인 문서가 빈손이 되고 그 SKU 의 이후 사건까지 번졌다.

**② ⚠️⚠️ 감지되지 않던 결함 — 순액과 중복 방지의 범위가 어긋났다 (`20260909235347`)**

`tr_in` 중복 방지는 **날짜별**인데 `v_in_net`·`v_out_net` 순액은 **문서 전체**로
계산했다. ⇒ 쌍1 이 여러 날에 걸치면 각 날짜가 문서 전체 순액을 소진한다.

```
[실증] TOR 기초 40 · TR-M 08-21 −10 · 08-23 −10
        원장 소진 20  vs  레이어 40 (consume 2행 · 각 20)
        ⚠️ short_events 0 — 조용히 틀린다
```

재고가 충분하면 **이중 소진**, 부족하면 **거짓 short** 다.
⚠️⚠️ **어느 카운터에도 안 잡힌다** ⇒ 문서로 남기는 것이 소용없다
(원칙: `ims-principles.md` §1-a).

⇒ 순액·대응 out 탐색에 `occurred_on = r.occurred_on` 추가.
⭐ 날짜로 좁혀도 안전한 근거: **leg 쌍은 항상 같은 날 안에서 완결**된다
(문서×날짜 346개 중 `pair1_broken 0` · `pair2_broken 0` · `orphan 0`).

⭐ **함께 고친 것** — `transfer_out_unpaired` 집계도 `(doc, sku, wh, day)` 로.
순액을 날짜별로 처리하면서 감지망만 문서 단위면 **첫날 짝이 처리된 순간 둘째 날의
짝 없는 out 이 감지에서 빠진다** — 같은 부류의 조용한 결함이다.

📌 [실측] 실데이터에 여러 날에 걸친 쌍은 **0건**이라 이 수정으로 `apply` 결과가
하나도 바뀌지 않았다. ⭐ **그것이 검증이었다.**

**⭐ 확정된 규칙 (이번 작업)**

| | |
|---|---|
| `assemble` | `in` 이 `seq_hint 1` 이라 먼저 오므로 **그 자리에서 `out` 을 먼저 소진** · `unit_cost` = 부품 원가 합 ÷ 완제품 수량(`assembly_sum`) · `parent_layer_id` null(부품이 여럿) |
| 조립 부품 부족 | 있는 만큼만 쓰고 원가가 낮아진다(`assembly_partial_cost`) · ⚠️ **원가 미상 레이어를 만들지 않는다** — 완제품은 어차피 새 레이어이고 부족한 것은 「원가의 일부」이지 「재고」가 아니다(⚠️ 트랜스퍼와 판단이 다르다) |
| 조립 완제품 키 둘 이상 | ⚠️ **예외를 던진다** — 부품 원가를 배분할 근거가 없고 조용히 합치면 이중 계상 |
| `credit_in` 갈래 ① | 원 판매의 소비 기록 단가로 되짚는다(`return_restore` · `parent` = 원 레이어) · 되짚는 순서는 소비된 순서 |
| `credit_in` 갈래 ② | 원 판매가 원장에 없으면 남은 레이어 가중평균(`layer_avg`) |
| ⭐ 반품 매칭 키 | ⚠️ **창고를 넣지 않는다** — 원가는 물건을 따라가고 레이어는 **반품 행의 창고**에 만든다. 📌 토론토 출고 → 에드먼튼 반품이 가능하다(Caleb 확인 · 재고 문제로 토론토에서 보냈는데 반품은 가까운 곳으로). [실측] 되짚기 가능한 9건은 전부 같은 창고였다 |
| `received_on`(반품) | **물건이 돌아온 날** (원 판매 시점이 아니다) |

**⭐ PO landed 코스트 적재 완료 (2026-09-10)**

마이그레이션 `20260910132601`.
⭐ **`inv_layer_cost_add` 가 처음으로 돌았다** — 표만 있고 행 0건이던 것이 채워졌다.

| | |
|---|---|
| `landed_rows` | ⭐ **434** (408키 + 날짜가 여럿인 26키의 추가 행) |
| `landed_amount` | ⭐ **$2,215.65** — `inv_cost` landed 합계와 일치 |
| `landed_orphan` | ⭐ **0** — 대응 `purchase` 레이어를 전부 찾았다 |
| 재고 평가액 | $3,073,830.45 → **$3,075,916.90** (+$2,086.45) |

⚠️ **수량 카운터는 전부 불변**이다(`short_events 0` · `skipped_by_type {}` ·
레이어 수 동일). ⭐ 원가만 얹었으므로 그것이 맞다.

**⭐ `landed` 총액 $2,215.65 중 $2,086.45 만 평가액에 반영됐다**
— 차이 **$129.20** 은 **이미 팔린 몫**이다(`remaining_cost` 에 안 들어간다).
⇒ ⭐ **그것이 「뒤늦게 붙은 원가 차액」의 실물이다**(§13 미결).

**[실측 2026-09-10 · 재복사 후]**

```
inv_cost
  goods   730행 · 21 PO · $476,203.27 · 08-21~09-09
  landed  434행 · 11 PO · 405 SKU · $2,215.65 · 08-22~08-31

landed 키 (doc_number, line_ref, sku, warehouse) 408개
  ⭐ has_layer  408 · no_layer 0    전부 purchase 레이어와 조인된다
  ⭐ multi_bin    0                 합산이 필요한 키가 없다
  ⚠️ multi_date  26                 같은 라인에 landed 가 여러 날짜로 온다
  ⭐ amt_neg      0
```

⭐ **434 − 408 = 26 이 `multi_date` 와 같다** ⇒ 통관·freight·관세가 **각각 다른
인보이스 날짜**로 오고, `inv_layer_cost_add` 유니크 키에 `occurred_on` 이 있어
**별개 행으로 공존**한다. 설계가 맞았다.

**⭐ 확정된 규칙**

| | |
|---|---|
| 어디서 얹나 | ⚠️ **`inv_layer_apply` 안**에서. 밖(별도 RPC)에서 채우면 다음 `apply` 의 전량 delete 에 사라진다 |
| 언제 | 날짜순 루프가 **끝난 뒤** — `layer_id` 를 찾아야 하므로 레이어가 다 만들어져야 한다 |
| ⚠️ `unit_cost` | **`goods` 만 유지한다.** `landed` 는 `cost_add` 로 별도다 — ⭐ 어느 조각이 언제 붙었는지 남기는 설계다. 소비 기록의 `unit_cost` 도 안 바뀐다(「소비 시점 값을 굳힌다」) ⇒ **그래서 차액이 계산 가능하다** |
| bin | 같은 키에 bin 이 여럿이면 **합산**(실측 0건이지만 방어) |
| ⚠️ `ref_number` | **null 이다** — PO landed 는 `InventoryMovements` 에서 오므로 Service Invoice 번호가 오지 않는다. 📌 트랜스퍼 운송비는 `ManualJournals.Reference` 로 오므로 그때는 채워진다 |
| ⚠️ 음수 가드 | `inv_layer_cost_add` 에 `amount >= 0` **CHECK 가 없다**(`kind` CHECK 와 5키 유니크만) ⇒ 가드가 유일한 방어다. 키 합이 음수면 예외 |

⚠️ **CHECK 를 걸지 않은 이유** — 정당한 음수 조정(랜디드 과다 계상 되돌림)이
나올 수 있어 표본 없이 정할 수 없다. ⭐ **가드가 차단하고 그때 실물로 판단한다**
(⚠️ 「나중에 보자」가 아니다 — 지금 차단돼 있고 조용히 통과하지 않는다).

**⭐ 트랜스퍼 운송비 — 배분까지 완료 · 수집기는 미착수 (2026-09-10)**

마이그레이션 `20260910141553`.

### ⚠️ Cin7 은 문서 단위로만 준다 — 배분은 우리가 한다

[실측 GAS 프로브 2026-09-10 · `TR-04175`·`TR-03975`]

```
목록:  CostDistributionType = "Cost"          ⭐ 원가 비례
상세 최상위 키 (10):
  Lines · ManualJournals · ManualJournalsDistributedCosts · Status · TaskID ·
  From · To · Reference · DepartureDate · CompletionDate

⭐ ManualJournalsDistributedCosts = ARRAY(0)   ← 둘 다 빈 배열
⭐ Lines[0] 원가류 필드 = (없음)               ← TransferQuantity 만
```

⇒ ⭐ **Cin7 화면의 `Distribute journals using the product: Cost` 는 내부 설정이고
결과를 API 로 주지 않는다.**

### ~~⭐ `ManualJournals` 구조 — 키 여섯 · `IsSystem` 이 관건~~ → ⚠️⚠️ [정정 2026-09-10] 아래 표는 창작이었다

~~`Debit · Credit · Reference · Date · Amount · IsSystem`~~

~~TR-03975 ARRAY(2) · [0] Debit=_1150040007_ · Credit=_59_ · Ref=TR-03975 · 08-26 · 4878.11 · IsSystem=true · [1] Debit=_59_ · Credit=_136_ · Ref=B6900109 · 08-27 · 398.75 · IsSystem=false~~
~~TR-04175 ARRAY(1) · [0] Debit=_59_ · Credit=_136_ · Ref=B6913286 · 09-04 · 249.33 · IsSystem=false~~

~~⚠️⚠️ `IsSystem` 으로 거른다. 배열 길이로 판단하면 틀린다. · `true` = 운송중 계정 이동(`_1150040007_` In Transit ↔ `_59_`) · 금액이 재고 자체(4,878.11) · `false` = 운송비 · `TR-04175` 는 시스템 저널이 아예 없어 1행~~

### ⭐ [정정 2026-09-10 · GAS 프로브 실측 14:40 · 세 문서] `ManualJournals` 실물 — `IsSystem` 은 존재하지 않는다

```
트랜스퍼 /stockTransfer?TaskID=   응답 최상위 키 18개(세 문서 동일):
  TaskID · Status · From · FromLocation · To · ToLocation · Number · CostDistributionType ·
  InTransitAccount · DepartureDate · CompletionDate · RequiredByDate · Reference · LastModifiedOn ·
  Lines · ManualJournals · SkipOrder · Order

ManualJournals: ARRAY(1) — ⭐ 저널이 바로 원소다 (Lines 겹이 없다)
  원소 키 11개: TaskID · ID · Reference · Amount · Date · Debit · Credit ·
    ManualJournalsDistributedCosts · vDimensionDefaultValueStockTransferJournals ·
    ValidationText · ValidationState
  ⚠️⚠️ IsSystem 없음 — 응답 전체 문자열 검색 0건 (237,148 · 94,981 · 192,365자)

  TR-03975  {"Reference":"B6900109","Amount":398.75,"Date":"2026-08-27T00:00:00","Debit":"_59_","Credit":"_136_", …}
  TR-03976  {"Reference":"B6900109","Amount":229.2, "Date":"2026-08-27T00:00:00","Debit":"_59_","Credit":"_136_", …}
  TR-04175  {"Reference":"B6913286","Amount":249.33,"Date":"2026-09-04T00:00:00","Debit":"_59_","Credit":"_136_", …}
```

- ⭐ **운송중 계정 이동은 `ManualJournals` 로 오지 않는다** — Cin7 이 내부에서 처리하고 헤더 `InTransitAccount: "_1150040007_"` 에 계정 코드만 알려준다. [실측] `Debit`/`Credit` 을 가진 객체는 중첩 전체에서 `root.ManualJournals[0]` 하나뿐(세 문서 동일). `4878.11` 은 응답 어디에도 없다.
- ⭐ **필터 = `Debit === '_59_'`(재고) AND `Credit === '_136_'`(Freight-COS)** — 둘 다 화이트리스트(다른 값은 `skip_non_inventory_debit`·`skip_non_freight_credit` 로 세고 경고 · 새 계정 신호). 「배열 길이로 판단하면 틀린다」의 근거(2행 vs 1행)도 사라졐다 — 세 문서 모두 1행. 배열이므로 **전량 순회**는 유지(인보이스 여러 장 ⬜ 표본 없음).
- ⭐ **`TR-03975`·`TR-03976` 은 같은 인보이스 `B6900109`** 를 398.75 / 229.2 로 나눠 갖는다 — 「인보이스 1장 → 문서 여럿」의 실물.

**⭐ 발주와 트랜스퍼는 구조가 완전히 다르다 — 이번 사고의 뿌리**

```
발주 /advanced-purchase?ID=   [실측 GAS 2026-09-10 15:06 · PO-01111]
ManualJournals: ARRAY(1)
  원소 키 4개: TaskID · InvoicingAndReceivingNumber · Status · Lines   ← ⭐ Lines 겹이 한 번 더 있다
  Lines[0] {"Reference":"Stock in Transit","Amount":91877.51,"Date":"2026-09-02T00:00:00",
            "Debit":"_59_","Credit":"_1150040012_","IsSystem":true}
  Lines[1] {"Reference":"16898","Amount":90,"Date":"2026-08-27T00:00:00",
            "Debit":"_59_","Credit":"_135_","IsSystem":false}
```
⭐ **발주에는 `IsSystem` 이 실재하고 `true`/`false` 가 다 온다** ⇒ `inv-cost/index.ts` 의 `l?.IsSystem === false`(발주 `mj_user_lines`)는 **정상**이다.

| | In Transit | 비용 계정 |
|---|---|---|
| 발주 | `_1150040012_` | `_135_` (landed) |
| 트랜스퍼 | `_1150040007_` | `_136_` (운송비) |

⚠️ ⬜ `_135_`·`_136_` 의 계정명은 미확인(Cin7 화면에는 `_136_: Freight - COS`). 필터에는 영향 없음 — 각 축에서 실측된 값을 화이트리스트로 쓴다.

⚠️⚠️ **[사고 2026-09-10] 프로브 출력을 요약하다 없는 필드를 창작했다.**
어제 프로브 결과를 문서·주석·테스트 픽스처에 옮기면서 **발주 구조를 트랜스퍼에 섞어** `IsSystem` 을 적었다(`IsSystem` true/false 짝 = 발주 `ManualJournals[0].Lines` 의 모양 · 「`Debit=_1150040007_`」 = 트랜스퍼 헤더 `InTransitAccount` 를 저널로 오독 · `4878.11` = 응답에 없음). ⇒ EF 필터가 `j?.IsSystem === false` 로 `undefined === false` → false 를 만들어 **전량 걸러졌고**, 다섯 회차 `docs_processed 0` 을 「도달 실패」로 오진하며 하한·정렬·캡을 파느라 반나절을 썼다.
⚠️⚠️ **테스트 25개가 전부 PASS 였는데 그중 7개가 창작 필드 위에서만 성립했다** — 픽스처를 내 요약으로 만들었기 때문이다.
⇒ ⭐ **API 응답 구조는 요약하지 말고 원문(JSON)을 그대로 옮긴다.**
⇒ ⭐ **픽스처는 프로브 실측 원문으로 만든다** — 요약으로 만든 픽스처는 자기 요약을 검증할 뿐이다.
⚠️ 그리고 **두 엔드포인트의 구조를 같다고 가정하지 않는다**(발주 ≠ 트랜스퍼).

### ⭐ 저널은 나중에 붙는다 — 커서 축은 `LastModifiedOn`

```
TR-03975  Departure 08-14 · Completion 08-26 · 저널 08-27
TR-04175  Departure 08-21 · Completion 09-02 · 저널 09-04
```

⚠️ **`TR-04175` 는 09-09 실측에서 저널이 없었고 09-10 에 붙었다**
(`LastModifiedOn = 2026-09-10T15:32:06`).
⇒ ⭐ 저널 날짜는 도착일 +1~2일이지만 **문서 갱신 시점은 훨씬 늦다.**
⚠️ 수집 커서는 `LastModifiedOn` 이어야 한다 — `CompletionDate` 로 잡으면 놓친다.

### A. 새 표 `inv_doc_cost` — 문서 단위 금액

⭐ **왜 표를 따로 두나** — `inv_cost` 는 `sku`·`line_ref`·`qty`·`unit_cost` 가
**전부 `not null`** 이라 문서 단위 금액을 담을 수 없다.
⚠️ 가짜 SKU 를 채우면 「모르면 비워둔다」와 반대이고 `inv_cost_sku_idx` 가 오염된다.

| | |
|---|---|
| 키 | `(doc_type, doc_number, kind, ref_number, occurred_on)` |
| ⚠️ `amount` 를 키에서 뺐다 | 금액이 정정될 수 있어 upsert 로 덮어써야 한다(`inv_cost` 와 같은 이유) |
| `ref_number` 가 키에 있다 | 인보이스가 여러 장 붙으면 각각 남는다 |
| ~~`IsSystem: true`~~ → 화이트리스트(Debit `_59_`·Credit `_136_`)에 걸린 행 | ⚠️ **담지 않는다.** ~~IsSystem=true~~ 는 오지 않는 행이었다([정정 2026-09-10] 위 절). 필요하면 `raw` 에 `ManualJournals` 배열 전체를 남긴다 |
| `amount >= 0` CHECK | ⚠️ **걸지 않았다** — 정정이 음수로 올 수 있다. 배분 단계 가드가 방어한다 |
| ⬜ 미확인 | 같은 인보이스가 **같은 날 두 줄**로 오는 경우 — 그러면 유니크에 걸려 하나만 남는다(표본 없음 · 두 문서 모두 1행) |

### B. ⭐ 배분은 EF 가 아니라 `inv_layer_apply` 에서 한다

⚠️⚠️ **이것이 이번 설계의 핵심 판단이다.** 이유 둘:

1. ⚠️ **의존 방향** — 수집기가 `inv_layer` 를 읽으면 방향이 역전된다.
   `ims-principles.md`: 「모듈은 레고처럼 · 접점은 사건 하나」. 지금 레이어가
   원장·원가를 **읽기만** 하는데 반대 의존이 생기면 그 원칙이 깨진다
2. ⚠️⚠️ **불변 조건(§3)** — 배분 결과가 `inv_cost` 에 굳으면 **레이어 규칙이
   바뀌어도 배분은 안 바뀐다.** ⭐ `apply` 에서 하면 매번 다시 계산된다

📌 불변 조건에 원천이 하나 늘었다:
`inv_ledger + inv_cost + inv_doc_cost + inv_snapshot`

**배분 규칙**

| | |
|---|---|
| 대상 레이어 | `origin_type='transfer'` · 그 `doc_number` · ⚠️ **`warehouse <> 'IN_TRANSIT'`** — 운송비는 **도착지 재고**에 녹는다(`Debit='_59_'` · 실무 목적이 에드먼튼 gross 마진 정확도다) |
| 기준 | ⭐ **`unit_cost × qty`** (`remaining` 이 **아니다**) — 배분은 **입고 시점의 원가 구성**을 따르고 그 뒤 얼마가 팔렸는지와 무관하다 |
| 끝수 | 마지막 레이어에 잔액(①의 「remainder on last」와 같은 관례) · 소수 6자리 |

### C. ⭐ 실측 — 배분 검증 통과

`scripts/testdb/transfer_freight_sample.sql`(프로브 실측 2건)로 검증.

| | |
|---|---|
| `freight_rows` | **57** (`TR-04175` 도착 레이어 수) |
| `freight_amount` | ⭐ **$249.33** — 문서 금액 전액 |
| `freight_docs` | 1 |
| ⚠️ `freight_no_basis` / `_amount` | **1 / $398.75** |
| `freight_orphan` | ⭐ **0** |
| 재고 평가액 | $3,075,916.90 → **$3,076,166.23** (⭐ 정확히 +$249.33) |

⭐ **평가액이 배분액 전액만큼 올랐다** ⇒ `TR-04175` 로 온 재고가 아직 하나도
안 팔렸다는 뜻이다(9/02 도착 · 저널 9/04).
📌 PO landed 는 $2,215.65 중 $2,086.45 만 반영됐다(차액 $129.20 = 이미 팔린 몫) —
8월 건이라 판매가 있었다.

⚠️ 수량 카운터는 전부 불변(`short_events 0` · `skipped_by_type {}`).

### ⚠️⚠️ D. 배분 기준이 0 인 문서는 운송비를 버린다

[실측] `TR-03975` **$398.75** 가 `freight_no_basis` 로 떨어졌다.
⚠️ 그 문서는 **기초 이전 출발**(8/14 출발 · leg 1·2 가 `since` 로 원장에 없음)이라
IN_TRANSIT 레이어가 없고, 도착 레이어가 `cost_source='unknown'` · `unit_cost 0` 이다
⇒ **원가 비례로 나눌 기준이 0 이다.**

⇒ ⭐ **수량 비례로 배분하지 않는다** (Caleb 판정 2026-09-10). 이유 둘:
- ⚠️ Cin7 은 `CostDistributionType='Cost'` 다 — 수량 비례로 하면 **방식이 달라지고**
  Cin7 평가액 대조에서 「방식이 달라 차이가 난다」가 또 생긴다.
  ⭐ 방식이 같아야 차이의 원인을 규명할 수 있다
- ⭐ **재기준선이 지운다** — 기준선을 다시 잡으면 그 레이어에 원가가 붙어
  저절로 배분된다

⚠️ **대가: 그만큼 우리 평가액이 Cin7 보다 낮다.** Cin7 은 그 운송비를 재고에
녹였다(`Debit='_59_'`).
⇒ ⭐ **`freight_no_basis_amount` 가 그 차이의 크기를 알려주는 창구다** —
Cin7 대조(③)에서 **이 값만큼은 설명된 차이**다.

⬜ **미확인 — 실물 비중**: 표본이 둘뿐이라 전체 트랜스퍼 중 몇 %가 `no_basis` 인지
모른다. 수집기가 붙으면 그 비율이 나온다.

**⭐ ②-a 수집기(`inv-doc-cost` EF) — 작동 확인 (2026-09-10)**

⭐ **EF 를 운영에 배포하고 `?dry` 로 추출이 되는 것을 확인했다.**

```
from_since=2026-08-26T20:51:06.373Z   (TR-03975 의 LastModifiedOn)

collector_version   inv-doc-cost@2026-09-10.2
lmo_floor           2026-08-26T20:51:06.373Z
candidates          533 · detail_fetched 40 (캡)
⭐ docs_processed    1
⭐ rows_built        1
⭐ amount_built      398.75
journal_lines       kept 1 · non_inventory_debit 0 · non_freight_credit 0 ·
                    no_reference 0 · zero_amount 0 · bad_amount 0 · no_date 0
dispositions        processed 1 · skip_no_journal 39
samples[0]          TR-03975 · transfer_freight · 398.75 · 2026-08-27 ·
                    B6900109 · Debit _59_ · Credit _136_ · raw 에 원문 11키
```

⭐ **화이트리스트 밖 계정이 0건**이다(`non_inventory_debit`·`non_freight_credit`) —
표본 범위에서 `Debit=_59_` · `Credit=_136_` 이 유일한 조합이다.

### ⚠️ `from_since`·`recheck_since` 가 시각을 받는다 (`20260910` EF 수정)

⚠️ **축이 안 맞았다** — 커서 키는 `LastModifiedOn` 밀리초까지인데 하한 비교가
`updated.slice(0, 10)` 로 날짜만 봤다. 하루 안에 문서가 많으면 **캡 안에서 특정
문서에 닿을 수 없다.**

⇒ 날짜(`YYYY-MM-DD`) 또는 ISO 시각(`…THH:MM[:SS[.mmm]][Z]`)을 받고, 파싱 실패는
**400** 이다. 응답 키 `lmo_floor_date` → **`lmo_floor`**(실제 적용된 하한).

⭐ **밀리초 자릿수 위험은 없다** — `normLmo` 가 `LMO_RE` 정규식으로 파싱해
밀리초를 3자리 `padEnd` 하므로 `.9Z` vs `.10Z` 뒤집힘이 불가능하다.
정렬·하한·커서 판정 세 곳 모두 정규화 키를 쓴다.
(📌 정규화 **전** 원문 비교에는 뒤집힘이 실재했다 — Cin7 이 끝 0 을 잘라 `.3Z`/`.373Z` 처럼
접두 관계가 생기면 `'Z' > 숫자` 라 짧은 쪽이 뒤로 갔다. [실측 테스트 DB `inv_doc_state` transfer 349건]
3자리 209 · 2자리 129 · 1자리 10 · 소수부 없음 1. `test-invdoccost.mjs` ⑬-b 가 그 뒤집힘을 재현한다 — 정규화가 그것을 없앤 것.)
⚠️ `toISOString()` 을 쓰지 않는다 — `inv-cost` 관례가 「절대 시각으로 파싱하지
않는다」이고 `new Date()` 를 거치면 타임존·정밀도에서 값이 바뀔 위험이 있다.
정규식 불일치는 원문 그대로 두고 `lmo_unnormalized` 로 센다(실측 0).

### ⭐ 진단 필드 셋 (정식 · 임시 아님)

| | |
|---|---|
| `candidate_head` | 커서 필터 뒤 후보 앞 10건 `{n, doc_number, key}` — ⭐ 특정 문서가 후보에 있는지·몇 번째인지 ⇒ 캡 안/밖이 바로 보인다 |
| `skipped_docs` | 상세를 봤는데 `processed` 가 아닌 문서 `{doc_number, disposition, mj_len}` · 최대 50 |
| `skipped_docs_truncated` | 50 을 넘어 목록에서 빠진 수 |

⭐ **`mj_len` 이 원인을 가른다** — `null` = 키 자체가 없음 · `0` = 빈 배열 ·
`1 이상` = 배열은 읽었고 **우리 필터가 걸렀다**.
📌 [실사고] 이 필드가 없어 「도달 실패인지 파싱 결함인지」를 다섯 회차 동안
구분할 수 없었다.

### ⚠️⚠️ `dry` 로만 돌았다 — 그리고 그 이유

⚠️ **`commit=1` 은 실패한다.** `inv_doc_cost` 표가 **운영에 없다.**

```
운영 migration list: 20260910132601 · 141553 · 152545 전부 Remote 공백
⇒ EF 는 운영에 배포됐는데 쓸 표가 없다
```

⚠️ **원인은 마이그레이션 사슬이다** — `20260910141553` 이 **`inv_doc_cost` 표**
(수집기가 쓰는 것 · 운영에 필요)와 **운송비 배분**(원가 레이어 계열 · 운영에 안 올림)을
**한 파일에 담고 있어** 갈라 올릴 수 없다.
⇒ `20260910152545`(표 정의만 복제 · `if not exists`)를 따로 만들었으나,
⚠️ `db push` 가 밀린 것을 전부 올리므로 그 하나만 올릴 수 없다.

⚠️ **`repair` 로 우회하지 않는다** — 운영에 없는 것을 「적용됨」으로 표시하면
거짓 기록이 되고 나중에 진짜 올릴 때 막힌다.

⇒ ⏸ **마이그레이션 구조 정리는 미뤘다**(2026-09-10 판정 · 원장 창 판단 대기).
📌 그때까지 **수집기 검증은 `?dry` 로 한다** — 추출 로직은 위와 같이 확인됐다.
⭐ 그리고 미뤄도 잃는 것이 적다 — 배분(`inv_layer_apply`)이 테스트에만 있어
운영에 운송비를 쌓아도 지금은 쓸 데가 없다.

### ⚠️ 그래서 남는 미결

- ⏸ **`commit=1`** — 마이그레이션 구조 정리 뒤
- ⏸ **`freight_no_basis` 실물 비중** — ⚠️ 표본이 둘뿐이라 전체 트랜스퍼 중 몇 %가
  배분 불가인지 모른다. ⭐ `commit=1` 이 돌아야 분모가 나온다
  ⇒ ⚠️ **그것이 ③(Cin7 대조)에서 설명해야 할 차이의 크기다**
- ⬜ **`Reference` 가 같은 인보이스가 여러 문서에 쪼개진다** — [실측]
  `B6900109` 가 `TR-03975`(398.75)·`TR-03976`(229.2)에 나뉘어 있다.
  ⭐ 우리는 문서 단위로 받으므로 처리에 문제가 없다.
  ⚠️ ⬜ 같은 인보이스가 **같은 문서에 두 줄**로 오는 경우는 표본이 없다
  (그러면 `inv_doc_cost_uq` 에 걸려 하나만 남는다 · 코드는 합산 후 경고)
- ⬜ **목록을 매 회차 전량 순회한다** — `stockTransferList` 에 `UpdatedSince`
  계열 파라미터가 **없다**(파라미터는 `Page`·`Limit`·`Status`·`Search` 넷뿐).
  [실측] 4,675건 · 5페이지 · 목록만 수 초. ⚠️ 문서가 늘면 비용이 커진다

### ⚠️ 조회 함정 (실측)

- ⚠️ **`Search` 가 문서번호로 안 걸린다** — 쓰지 마라
- ⚠️ **목록이 최근순이 아니다**(`TR-03975` 가 6페이지에서 나왔다)
- ⚠️ `Status=COMPLETED` 서버 필터를 쓰되 **코드에서 다시 확인**한다
  (`skip_not_completed` · 0 이 아니면 서버 필터가 샜다는 신호)
- ⚠️ **`FromLocation`/`ToLocation` 문자열로 거르지 마라** — 「창고: bin」 형태라
  bin 트랜스퍼가 섞인다. ⭐ `From <> To` (GUID)로 거른다
  [실측] 이름 필터 784건 vs GUID 12건

**⬜ 남은 것**

- ⏸ **`adjust_existing` 남은 레이어 0** — 실측 0건 · 방어 됨 ·
  `adjust_unknown_layers` 가 창구
- ⏸ **`transfer_orphan_in` 방어 경로** — 실측 0건 · 실동작 미검증
- ⏸ **Simple `landed` 표본** — 북키퍼가 `PO-01215` 에 `Expense` 링크 →
  운영 `inv-cost` 재수집 → **`inv_layer_cost_add` 의 첫 시험**
- ✅ **PO landed 를 레이어에 얹기 (2026-09-10 완료 · 위 소절)**

⭐ **남은 순서 — 이 순서를 지킨다**

```
②-b ✅ 트랜스퍼 운송비 — 배분 (2026-09-10 완료)
②-a ⬜ 트랜스퍼 운송비 — 수집기 (inv_doc_cost 를 채운다)
③ Cin7 평가액과 대조 (SKU·창고별)
④ 그다음 PO 모듈
```

**②-b ✅ 배분 완료 (2026-09-10 · 위 소절)**

**②-a ⭐ 수집기 작동 확인 완료 (2026-09-10 · 위 소절) · ⏸ `commit=1` 은 대기**

⭐ 추출 로직은 `?dry` 로 검증됐다(`docs_processed 1` · `398.75`).
⏸ 실제 수집은 마이그레이션 구조 정리 뒤 — `inv_doc_cost` 표가 운영에 없다.

~~**②-a ⬜ 수집기 — 미착수. `inv_doc_cost` 를 채운다**~~ (아래는 착수 전 메모 · 「별도 EF 인지」는 별도 EF 로 결정됐다)

⭐ **배분이 빠졌으므로 훨씬 단순해졌다** — EF 가 할 일은
`stockTransferList` 순회 → 상세 → `ManualJournals` 에서 ~~`IsSystem=false` ·~~ `Debit='_59_'` **AND `Credit='_136_'`**
만([정정 2026-09-10] `IsSystem` 은 트랜스퍼에 없는 필드) → `inv_doc_cost` upsert 뿐이다.
⭐ **`inv_layer` 를 안 보고 배분하지 않는다.**

⚠️ 정할 것 둘:
- **별도 EF 인지 `inv-cost` 확장인지** — [보고 2026-09-10 `/tmp/inv-cost-report.md`]
  `inv-cost` 는 948줄이고 입력 축이 `purchaseList` 하나다.
  `doc_type` 이 `buildCostRows` 의 `rows.push` 에 `"purchase"` 리터럴로 박혀 있고,
  `DocMode` 타입이 두 값으로 좁아 분기 셋을 고쳐야 한다.
  ⚠️ 커서도 `source_key='cost'` 하나다
- **커서 축** — ⭐ `LastModifiedOn` (위 참조 · `CompletionDate` 로 잡으면 놓친다).
  별도 EF 면 `source_key='cost_transfer'` 로 독립

📌 수집기가 붙으면 `scripts/testdb/transfer_freight_sample.sql` 은 필요 없어지지만
**지우지 말고 「수집기 이전 검증용」으로 남긴다** — 배분 로직만 따로 시험할 수 있다.

⚠️ **②를 먼저 하는 이유** — ③(대조)을 먼저 하면 차이가 나도 **우리 결함인지
빠진 원가인지 가릴 수 없다.** 트랜스퍼 운송비는 재고 단가의 **16%** 규모라
(실측 `TR-03975` 1,550개 $398.75 = 개당 $0.26) 에드먼튼 원가가 그만큼
과소평가돼 있다.
⚠️ **④를 마지막에 두는 이유** — 발주 화면에 「이 SKU 에 얼마 묶여 있다」를
띄우려면 원가가 온전해야 한다.

📌 **②의 표본이 늘었다** — [Caleb 확인 2026-09-10] **9월 트랜스퍼 운송비가
오늘 Cin7 에 업데이트됐다.** ⚠️ 어제 실측은 5문서(`TR-03975`·`03976`·`03546~48` ·
`03531~34` 계열)뿐이었고 `im_rows` 2 이상을 못 봤다 ⇒ 인보이스 여러 장이 한 문서에
붙은 경우를 이제 볼 수 있다.
⚠️ **우리는 아직 수집하지 않으므로 `inv_cost` 에는 없다.**

~~⚠️ ②를 어느 대화에서 할지 결정 필요 — `inv-cost` 는 원장 수집기지만, 배분 규칙
(`CostDistributionType='Cost'` · **원가 비례**)이 **레이어 원가를 써야 계산**되므로
원가 층 설계와 묶여 있다.~~
[정정 2026-09-10] 배분은 `inv_layer_apply` 로 갔다(위 소절 B) — 수집기는 `inv_doc_cost` 만 채운다(②-a).

- ⬜ **트랜스퍼 운송비 수집** — ~~`inv-cost` 확장(별도 작업).~~
  `stockTransfer.ManualJournals` · `Debit='_59_'` 만 ·
  ~~배분이 `CostDistributionType='Cost'`(원가 비례)라 **이 함수가 만든 레이어의
  원가가 있어야 계산된다** ⇒ 순서가 고정이다~~
  [정정 2026-09-10] 별도 EF 인지 확장인지는 미정(②-a) · 배분은 `apply` 안이라 수집기는 레이어 원가를 보지 않는다.
- ⬜ **Cin7 평가액과 본격 대조** — ⭐ 방식이 같으므로(둘 다 FIFO) 차이가 나면
  원인을 규명한다. ⚠️ **창고별로** 비교하면 Cin7 FIFO 의 단위(SKU vs SKU×창고)도
  함께 확인된다
- ⬜ **뒤늦게 붙은 원가 차액의 회계 처리** — 실측 후 회계사와 상의

**13. 미결 (다음 세션)**

⚠️ **순서와 완료 여부는 §12 끝의 「⬜ 남은 것」이 정본이다** — 아래 항목은
개별 설명이고, ②③④ 순서와 그 이유는 그쪽에 있다.

- ✅ **사건 적재 함수 — 형태 확정 · `po_in`+`sale_out` 구현 완료 (2026-09-09).**
  날짜순 루프 하나 + 사건별 보조 함수 · 전량 재생성(커서 없음) ·
  `order by occurred_on, seq_hint, id` 가 「같은 날은 유입 먼저」를 보장한다
  (마이그레이션 `20260909155320` · 성능 2,708 ms).
  ⬜ **남은 사건 7종** — 다음은 `transfer`(6,763행 · 에드먼튼의 유일한 유입 경로),
  그다음 `adjust_existing`(±) · `adjust_new` · `assemble` · `credit_in`.
  ⚠️ `adjust_existing` 은 `seq_hint` 1(41행 양수)·2(116행 음수) 양쪽에 있어
  `event_type` 만으로 분기하면 틀린다
- ⬜ **트랜스퍼 운송비 수집** — `inv-cost` 확장. `stockTransfer.ManualJournals` 에서
  `Debit='_59_'` 만. 배분은 `CostDistributionType='Cost'`(원가 비례)라 **우리가 한다**
  (Cin7 이 라인별 배분을 주지 않는다 — `ManualJournalsDistributedCosts` 는 빈 배열)
- ⬜ **뒤늦게 붙은 원가 차액의 회계 처리** — 실측 후 회계사와 상의.
  트랜스퍼는 완료일 +1~2일이라 차액이 작다. PO landed 는 평균 1주일
- ⬜ **우리 FIFO 금액 vs Cin7 평가액 대조** — ~~⚠️ Cin7 이 **SKU 단위 가중평균**일 가능성이
  크다(기초 스냅샷에서 같은 SKU 의 bin 별 단가가 사실상 동일 · 13,830칸 중 다른 것 13개 ·
  최악 1.09배). **「정확히 일치」를 목표로 삼지 말 것**~~
  ⚠️⚠️ **[정정 2026-09-09 · Caleb 확인] Cin7 도 FIFO 다.**
  [실측 화면] 제품 마스터의 `Costing method` 가 **FIFO** 다(SKU 마다 설정).
  ⇒ ⭐ **「방식이 다르니 당연하다」로 넘어갈 자리가 없다** — 금액이 벌어지면
  원인을 규명해야 한다.
  ⚠️ **그러나 완전히 같을 수는 없다.** 우리 기초는 「8/20 평가액을 뭉친 한 덩어리」라
  **그 시점 Cin7 의 레이어 구조를 이어받지 못했다.** ⇒ 기초분이 소진될 때까지
  구조적 차이가 남는다(재고의 96%가 기초분이므로 1년 이상).
  ⬜ **미확인 — Cin7 FIFO 의 단위가 SKU 인지 SKU×창고인지.**
  ⭐ 실무 정황은 SKU×창고 쪽이다 — 트랜스퍼 운송비가 `Debit='_59_'` 로
  **에드먼튼 재고에 녹으므로** 창고별 원가가 갈려야 한다.
  ⇒ ⚠️ 대조 단계에서 **창고별로** 비교하면 답이 나온다(에드먼튼이 크게 벗어나면
  SKU 단위 · 맞으면 SKU×창고).
  📌 종전 판단의 근거였던 「기초 스냅샷에서 같은 SKU 의 bin 별 단가가 사실상 동일」
  (13,830칸 중 다른 것 13개 · 최악 1.09배)은 **여전히 사실**이다. 다만 그것은
  「가중평균이다」가 아니라 **「bin 단위로 레이어를 가르지 않는다」**는 뜻이다.
  📌 같은 문장이 `20260908202555_inv_layer_seed_baseline.sql` 헤더와 `asung-inv-ledger` 스킬에도
  있었고 같은 정정을 붙였다.
- ⬜ **북키퍼가 Service Invoice 를 PO 에 매칭하지 않고 P&L 에 남기는 경우**가 있는지 —
  실무 확인 필요. 있으면 그 운송비·통관비는 재고 원가에 **영구히 안 붙어** FIFO 금액이
  구조적으로 낮게 나온다

**14. 실무 흐름 (2026-09-08 Caleb 확인)**
- **PO 랜디드 코스트** — 통관·freight·관세를 각각 Service Invoice 로 만들면
  Freight & Duty COS 계정에 잡히고, 북키퍼가 해당 PO 에 매칭하면 P&L 에서 빠져
  **재고 원가로 녹는다.** 평균 1주일
- **트랜스퍼 운송비** — 같은 방식. Service Invoice 를 트랜스퍼에 매칭한다.
  ⭐ 초창기에는 비용 처리했으나 **에드먼튼 매출의 gross 마진을 정확히 계산하기 위해**
  재고에 녹이는 방식으로 바꿨다
- ⚠️ **인보이스 ↔ 문서는 다:다다** — 인보이스 1장이 문서 여럿에
  (실측 `B6880391` → `TR-03531~34` / `B6887959` → `TR-03546~48`),
  문서 1개에 인보이스 여럿(통관+freight+관세). ⭐ **Cin7 이 문서 단위로 배분해 주므로
  우리는 인보이스를 직접 읽지 않는다**
- **재고 나이는 PO 단위로 본다.** stock aging summary 는 계속 볼 수 있어야 한다.
  ⚠️ 8/20 이전 재고의 나이 소급은 설계 범위 밖이다(현 시스템에서 별도로 본다)
- **원가 0 재고는 서플라이어 프로모션 무상 재고다** — 진짜 0 이므로 그대로 0 으로
  소진한다. 예외 처리하지 않는다

---

## 4부. 미확정 · 위험

### 구현 전 미확정이었던 것 — 전부 닫힘 (2026-08-16)

1. ~~발주 입고 날짜~~ → **입고 기록의 라인 날짜.** 4건 전수 일치
2. ~~조립 문서 날짜~~ → **오더 생성 시점에 실제로 재고가 움직인다.** 별도 확인 불필요
3. ~~번들 판매마다 조립 문서가 생기는지~~ → **예외 3건은 조정으로 재고를 잡은 것.**
   원장은 조정도 판매도 다 기록하므로 놓치지 않는다

### 다음 단계에서 정해야 하는 것

- ~~**`raw` 저장 단위**~~ → **확정 (2026-08-17 ②-a 구현)**: 그 행을 만든 라인 원본 + 최소
  문서 머리말 + 우리가 적용한 계산 규칙 문자열 + 수집 코드 버전. 문서 전체 금지 ·
  고객명·주소 미포함.
- ~~**문서 라인의 안정 식별자**~~ → ~~확정 (2026-08-17): ProductID + 판매만 예외. "발주
  CardID 는 raw 원문에만(키로는 안 씀 — 소스 간 통일)"~~ → ⚠️ **2026-08-18 재확정 — 그 결론이
  하루 만에 뒤집혔다**: 발주도 `CardID` 다(빈 분할·같은 빈 날짜 분리 실재 — PO-00944, `.7` dry
  `merged_lines: 5` 로 실증). **"소스 간 통일" 이 틀린 우선순위였다 — 통일보다 정확성이 우선.**
  현행 규칙은 위 「중복 방지」 절의 소스별 표가 정본.

### ⚠️ commit=1 을 켜기 전에 닫아야 하는 것 (2026-08-18) — ✅ **6개 전부 통과 (2026-08-20)**

**현황 (정본 — 세 곳이 이 표기를 공유한다: 이 절 · `asung-inv-ledger` 스킬 §1 · 그날 세션 문서)**:
```
☑1 CardID · 👁2 adv_no_putaway(관찰) · ☑3 재등장 전제 ·
🟡4 ON CONFLICT(코드 완료 — ⚠️ 실동작 미검증, 원장이 비어 있어 첫 회차 insert_skipped=0) ·
☑5 캡·페이싱 · ☑6 커서 seed
```
⇒ **게이트 전부 통과. 단계는 ⑥ shadow 대조로 넘어간다.**
⚠️ 4번만 🟡 로 남는다 — **코드는 완료지만 실동작은 원장에 행이 쌓인 뒤에야 검증된다**
(첫 회차는 원장이 비어 `insert_skipped` 가 구조적으로 0).

☑ 1. ~~**CardID 재수집 안정성**~~ → **닫힘 (2026-08-18 실측)**: PO-01117 51줄 전부
     Apply 직후(SR)→Convert 후(PA)→승인 후까지 `CardID` 동일 — Convert 는 재생성이 아니라 이동
👁 2. ~~**adv_no_putaway 경로 실행 확인**~~ → **게이트에서 「관찰 대기」로 하향 (2026-08-19).**
     **WMS 는 put-away 없이 Apply 하지 않는다**(Caleb 확인 + 코드) — Simple 은 `Convert` 시 라인이
     SR→PA 로 이동하고, Advanced 는 stage2 가 `/advanced-purchase/put-away` 를 반드시 호출한다.
     ⇒ **「Advanced 인데 PA 없음」은 우리 시스템이 만들 수 없는 상태**다(Advanced 80건+ 관측 전부 0).
     발생 가능 경로는 **WMS 밖**(사람이 Cin7 화면에서 직접 입고 후 선반 미지정)과
     **stage2 부분 실패 방치**뿐 — 즉 이 코드는 **예외 상황 감지기**이지 정상 흐름의 일부가 아니다.
     실패 방향도 안전하다(기표를 **안 하는** 쪽이지 틀린 행을 만드는 쪽이 아니다).
     ⇒ **⑤ 진입을 막지 않는다.** dry 응답의 `adv_no_putaway`·`adv_no_putaway_samples` 로 관찰한다.
     ⚠️⚠️ **복귀 조건: Advanced PO 기본값을 켜면 다시 게이트로 올린다** — 그때는 WMS 가 Advanced
     경로로 쓰므로 stage1 성공 / stage2 실패 문서가 실제로 생길 수 있고, 그것이 정확히 이 경로가
     잡아야 할 상황이다(아래 「검토 중 — Advanced PO 기본값」과 연동).
☑ 3. ~~**재등장 전제**~~ → **닫힘 (2026-08-19 실측).** put-away 승인이 `LastUpdatedDate` 를 갱신해
     다음 회차 `UpdatedSince` 에 재등장한다. [근거] PO-01117 은 2026-08-18 저녁 `PA=[DRAFT]` 라
     기표 보류(`blocks_skipped: putaway:DRAFT 1`)됐고, 다음 날 아침 `PA=[AUTHORISED]` 로 승인되자
     **같은 창의 `ledger_rows` 가 676 → 727 로 정확히 +51**(PA 라인 수) 늘며 들어왔다.
     ⇒ **「보류하되 커서는 전진」 설계가 유실을 만들지 않는다**는 것이 실증됐다.
🟡 4. **ON CONFLICT — 코드 완료, 실동작 미검증 (2026-08-19).** `inv_conflicts` 테이블 신설
     (마이그레이션 `20260819000000`) + `inv-collect` 에 감지 로직(`writeLedgerDetectingConflicts`,
     `@2026-08-19.1`). 배포·적용 완료 — 설계·경위는 아래 3번 항목이 정본.
     ⚠️ **첫 검증은 ⑤에서 `commit=1` 을 처음 켤 때다** — dry 는 쓰지 않아 버려짐 자체가 없다.
☑ 5. ~~**캡·페이싱**~~ → **닫힘 (2026-08-19 — 페이싱 수정 배포 후 6종 동시 실행에서 429 0건).**
     `DETAIL_SLEEP_MS=1200` · `_shared/cin7.ts` 백오프를 `Retry-After` 기반(재시도 1회·최대 60초)
     으로 교체 후 배포(5개 EF 전부 — 공용 파일이라 함께 나간다).
     [검증] 단일 소스 발주 `ledger_rows 727` — **페이싱 전과 동일**(속도만 바뀌고 결과는 안 바뀐다).
     **6종 동시 실행 `?dry=1`: `rate_limited` 가 전 소스 false.**
     ⚠️ **다만 `TIME_BUDGET_MS=120초` 안에 6종을 다 돌 수 없다** — 첫 소스(adjustment, 5,009행)가
     예산을 거의 소진하고 `transfer` 는 0행 `capped:"time"`, 나머지 4종은
     `aborted: "time budget exhausted before this source"` 로 시작조차 못 했다.
     ⇒ **한 회차에 6종을 다 돌린다는 전제 자체를 버린다.** ⑤에서 **소스별 cron 분리**로 간다
     (판매·발주는 자주, 반품은 드물게 — [실측] 반품은 하루 4건).
     📌 **이것은 페이싱 실패가 아니라 시간 가드의 정상 동작이다 — 판정 기준은 `rate_limited` 다.**
     ✅ **하루치 회귀 검증 통과 (2026-08-20)** — 발주 `candidates 20→29 · ledger_rows 829→1,164` ·
     판매 `145→219`(캡 40 고정이라 `ledger_rows 563` 동일) · 반품 `4→5 · rows 2→6`.
     **경고 0 · 미지 어휘 0 · `UNMAPPED` 0 · `merged_lines` 0 — 이틀 연속.**
     📌 발주의 `sr AUTHORISED` 가 1→0 이 된 것은 **PO-01112 가 Advanced 로 Convert 됐기 때문**이다
     (전날 Simple 로 Apply → 다음 날 Convert). PA 카운트가 15→25 로 늘어난 것과 짝이다 —
     같은 문서가 축을 갈아탄 것이지 유실이 아니다(1부 발주 절 「Simple 분기 실동작」과 같은 사건의 후속).
     ⚠️ **판매 캡이 계속 벌어진다** — 145→219. dry 는 커서를 안 쓰므로 **늘 앞 40건만** 본다.
     ⑤에서 커서가 돌기 시작하면 **초기에 밀린 것을 소화하는 구간**이 생긴다(5분 주기면 결국
     따라잡지만, 그동안 원장이 현실보다 뒤처져 보인다 — 대조를 그 구간에 돌리면 오해한다).
☑ 6. ~~**이동(번호 커서) 소스의 커서 seed**~~ → **닫힘 (2026-08-20).** 종전 문제: `from_cursor`
     없이 부르면 `warnings: ["NO FLOOR - scanning from the very first document"]` 와 함께
     TR-00001(2025-10-27)부터 전량을 훑는다([실측 2026-08-19] 3,172행). dry 에서는 정상 동작이지만
     쓰기를 켠 뒤 seed 없이 돌면 **매 회차 처음부터 재처리**한다(전량 축 3종 공통 — 조정·조립도 같은 구조).

     `inv_sync_state` 는 **비어 있었다**(`commit=1` 을 한 번도 안 켰으므로) — 충돌 없이 seed 했다.

     | `source_key` | `last_cursor` | 근거 |
     |---|---|---|
     | `transfer` | **`TR-01327`** | 버려진 DRAFT 4건(TR-00303·00708·01073·01327)을 넘긴다 |
     | `adjustment` | **`ST-00646`** | 버려진 DRAFT 6건(ST-00339~00646)을 넘긴다. 그 위로는 비종결 없음 |
     | `assembly` | **`FG-00128`** | 최대 번호 · **비종결 0건** |
     | `sale`·`purchase`·`creditnote` | **`2026-08-20T23:42:47.855Z`** | 스냅샷 `taken_at` |

     ⚠️ **`since=2026-08-20` 은 커서와 별개이며 매 호출에 필요하다.** 코드가
     `occurred_on > since`(**엄격히 큼** — `makeSink`, "경계일 제외" 주석)를 쓰므로 **8/20 전체가
     제외된다** — 그날 낮 사건은 스냅샷에 이미 녹아 있고, 19:42 이후에는 창고가 멈춰 사건이 없다.
     `since=2026-08-19` 로 하면 8/20 낮이 **이중 계상**된다.

     **⚠️ floor 선택의 원칙 — 되돌릴 수 있는 쪽**
     transfer 의 `TR-03539(ORDERED)` 는 **버려진 것으로 보이나 미확인**(에드먼튼 문의 대기)이다.
     - `TR-03539` 로 seed → 그 이하는 **영영 안 본다.** 진행 중이었다면 그 이동이 통째로 빠지고,
       **지나간 뒤라 알아채기 어렵다**
     - `TR-01327` 로 seed → 커서가 `TR-03539` 앞에서 멈출 뿐 **유실은 없다.** 느릴 뿐이고, 확인 후
       `update inv_sync_state set last_cursor='TR-03539' where source_key='transfer';` 한 줄로 올린다

     ⇒ **낮은 쪽을 골랐다. 커서를 올리는 건 언제든 되지만 내리는 건 이미 지나간 뒤다.**
     📌 `IN TRANSIT` 2건(TR-03975·03976)은 floor 보다 훨씬 위라 도착 행이 유실되지 않는다.
     dry 에서 `hold_intransit_before_since: 2` 로 정확히 분류됐다 — 설계 주석의
     「since 경계 아티팩트」가 실제로 작동한다.

     📌 **seed 산출은 GAS 프로브 1회**로 했다(`ppa_cursorSeed` — 목록 전량 + Status 분포 + 비종결 번호).
     ⚠️ **엔드포인트·필드명을 추측하지 않았다**: `stockadjustmentList`(소문자 a) · 번호는
     `StocktakeNumber` · assembly 는 `finishedGoodsList` → 배열키 **`FinishedGoods`**
     (`FinishedGoodsList` 아님) · 번호는 `AssemblyNumber`. 응답에서 배열형 키를 찾아내는 방식으로 확정했다.
     ⚠️ transfer 42페이지를 400ms 로 돌다 **429** 를 맞았다(분당 150콜 — 한도 60 초과).
     산출값은 이미 얻었으므로 재실행은 불필요했다.

### ✅ ⑤ 쓰기 가동 (2026-08-20 밤)

소스 하나씩 **수동 1회** → 확인 → **pg_cron 등록** 순으로 갔다.

**수동 1회 결과 — 전 소스 `written=0`.** 스냅샷 이후 사건이 없어서다. 경로는 검증됐다:
`mode: commit` · **커서 전진** · `warnings []` · `conflicts_detected 0`.

⚠️ **`precision_skipped` 가 「목록은 많은데 후보가 0」을 설명한다.** `UpdatedSince` 는 **날짜 단위**라
겹치게 받고(커서 − 1일), 커서 **시각**보다 이전에 갱신된 것을 우리 쪽에서 다시 걸러낸다.
[실측] 발주 88 = 45(서비스 전용) + **43(precision)** · 판매 273 = **179(precision)** + 78(미출하) + 16(void).
📌 **이 필드를 검증 커맨드에 넣을 것** — 없으면 「왜 안 들어왔지」를 추적할 수 없다.

⚠️⚠️ **회차 소요는 소스마다 다르다 — 캡이 아니라 「상세를 봐야 하는가」가 가른다:**
- **adjustment**: 586건이 **한 회차에 끝났다**(`detail_fetched: 0`) — `skip_since` 판정이
  **목록 레벨에서** 되므로 상세를 한 번도 안 부른다
- **transfer**: 캡 40에 걸린다(`detail_capped_remaining: 1,787`) — `DepartureDate`·`CompletionDate`
  **두 날짜를 봐야** 해서 목록만으로 판정이 안 되는 문서가 많다

⇒ **「후보 수 ÷ 캡」으로 회차를 추정하면 틀린다.** [실측] transfer 5회차에 **214건 전진**
(`TR-01368` → `TR-01582`) — 회차당 40건이 넘는 것은 목록에서 걸러지는 것이 섞이기 때문이다.

#### pg_cron 등록 (2026-08-21 UTC · jobid 6~11)

| jobid | jobname | schedule | 실행 분 |
|---|---|---|---|
| 6 | `inv-collect-transfer` | `2-57/5 * * * *` | 2·7·12… |
| 7 | `inv-collect-sale` | `4-59/5 * * * *` | 4·9·14… |
| 8 | `inv-collect-purchase` | `8-53/15 * * * *` | 8·23·38·53 (⚠️ 2026-08-21 정정 — 최초 `7-52/15`) |
| 9 | `inv-collect-adjustment` | `11 * * * *` | 매시 11분 |
| 10 | `inv-collect-assembly` | `13 * * * *` | 매시 13분 |
| 11 | `inv-collect-creditnote` | `16 * * * *` | 매시 16분 |

기록은 `supabase/ops/cron.sql`(마이그레이션 아님 — 재해복구용). ⚠️ 시크릿은 그 파일에서 placeholder.

⚠️ **분(minute)을 어긋나게 배치했다** — `hello` 폴링(jobid 1, `*/5`)과 **같은 WMS 키를 공유**하므로
한도 60콜/60초를 나눠 쓴다(위 1번 「같은 키를 쓰는 주체끼리는 한도를 공유한다」의 실행).

⚠️ **[2026-08-21 정정] `purchase` 최초 등록 `7-52/15` → `8-53/15` (실서버 변경 완료 · jobid 8 확인).**
분 어긋내기의 목표였던 `hello` 와는 처음부터 6잡 전부 안 겹쳤다(의도대로). 그런데 **원장끼리가**
겹쳤다 — 최초 등록값 7·22·37·52 는 **transfer(`2-57/5`)의 부분집합**이라 4회 실행이 전부 동시였다.
둘 다 `inv-collect` 이고 같은 WMS 키를 쓰므로 각각 최대 40콜×1,200ms(≈48초)면
**합산 분당 ~100콜 = 한도 60의 1.7배**였다(드러나는 신호는 `rate_limited` 하나다).

📌 **교훈: `hello` 와의 겹침만 보고 원장 잡끼리를 안 봤다** — **분 집합을 전수 계산할 것.**
잡이 늘면 확인해야 하는 쌍은 제곱으로 는다. 눈으로 세지 말 것.

**현재 겹침 0** (전수 계산 확인):

| 잡 | 분 집합 |
|---|---|
| `hello` `*/5` | 0·5·10·…·55 |
| transfer `2-57/5` | 2·7·12·…·57 |
| sale `4-59/5` | 4·9·14·…·59 |
| purchase `8-53/15` | 8·23·38·53 |
| adjustment 11 · assembly 13 · creditnote 16 | 단발 |
⚠️ **URL 에 `since=2026-08-20` 이 반드시 들어간다** — 빠지면 스냅샷 이전 사건이 원장에 들어온다
(위 「날짜 커서 축과 이벤트 날짜는 분리돼 있다」 절이 근거).
⚠️ **소스별로 나눈 이유**: `TIME_BUDGET_MS=120초` 안에 6종을 다 돌 수 없다 — 첫 소스가 예산을
소진하면 나머지는 `aborted`(2026-08-19 실측, 위 5번).

#### ✅ 가동 첫날 검증 (2026-08-21)

pg_cron 6잡 전부 `last_run_at` 갱신 확인.

**커서 전진**: `transfer` `TR-01327` → **`TR-03974`**(밤사이 2,647건 소화) ·
`adjustment` `ST-01232` → `ST-01233` · 나머지는 시각 커서 정상 전진.

⭐ **첫 실물 대조 통과** — `ST-01233`(파손 조정, `Reference: Damaged - AS (Check)`):

| SKU | Cin7 Variance | 원장 `qty_delta` |
|---|---|---|
| AS91598 | −1 | −1 |
| AS01401BLA | −3 | −3 |
| AS01400BLA | −20 | −20 |

📌 **Cin7 은 `New Quantity`(절대값), 원장은 증감분** — 코드가 올바르게 변환하고 있다.
bin(`B011202`·`B011102`)까지 정확히 들어갔다.

✅ **`since` 가 제 일을 했다** — `TR-03539` 는 「오늘 적용」했지만 **사건 날짜가 8/4**
(`Stock sent/received date` 둘 다 08/04/2026)라 정확히 제외됐다. 그 재고는 이미 스냅샷에 녹아 있다.
📌 **`UpdatedSince`(문서 갱신)와 사건 날짜가 독립된 축**이라는 설계 전제의 실물 사례다.

✅ **트랜스퍼 4행 구조 실동작** — `TR-04166`~`04170` 각 4행·합계 0
(출발 − / IN_TRANSIT + / IN_TRANSIT − / 도착 +).

📌 **`TR-01327` seed 판단이 옳았음이 확인됐다** — `TR-03539` 는 **버려진 것이 아니라 실제 적용 대상**
이었다. `TR-03539` 로 seed 했다면 **실재하는 이동이 원장에서 통째로 빠졌을 것**이고, 지나간 뒤라
알아채기 어려웠다. 「되돌릴 수 있는 쪽을 고른다」가 실제로 값어치를 했다.

⚠️ **다음 걸림돌은 `TR-03975`·`03976`(IN TRANSIT)** — 스냅샷 이전 출발이라
`hold_intransit_before_since` 로 분류돼 커서가 대기 중이다. **도착하면 전진**한다.
📌 그때가 설계 주석의 **「since 경계 아티팩트」 첫 실물**이다(1·2행이 since 에 걸러져
IN_TRANSIT 이 음수로 남는다) — 원장을 확인할 것.

⚠️ **검증 범위를 정직하게**: Cin7 과 대조한 것은 **조정 1건뿐**이다. 판매 45건·발주 2건은 미대조이고,
`inv_conflicts` 는 아직 빈 상태(원장이 갓 채워져 두 번째 회차부터 의미). **⑥(shadow 대조)가
필요한 이유가 이것이다** — 낱건 눈 대조는 표본일 뿐, 전량 대조는 ⑥의 일이다.

### ✅ ⑥ shadow 대조 가동 (2026-08-24 — 마이그레이션 `20260824130759` · cron 2잡 등록)

`inv_compare` 는 20260816 부터 테이블만 있고 채우는 코드가 0곳이었다 — 이번에 실행기를 만들었다.

**구조** (설계 결정 2026-08-24 Caleb 확정):
- **Cin7 현재고 = `inv-snapshot` 재사용**(검증된 all-or-nothing — 재구현 안 함). cron URL 에
  날짜를 박을 수 없어 **`?key=auto-compare` 리터럴일 때만** EF 가 `YYYY-MM-DD-compare` 를 생성한다
  — 「자동 생성 금지」의 원래 의도(키가 실수로 겹치거나 의미 없는 값이 되는 것)에 어긋나지 않는
  **명시적으로 요청한 자동화**다. ⚠️ `-initial` 은 여전히 수동 명시만.
- **대조 = RPC `inv_compare_run(p_key)`** — 전부 DB 안이라 Cin7 왕복 0. 원장 잔고(최신 `-initial`
  기준선 + `inv_ledger` 전 사건 합) FULL JOIN Cin7 현재고(compare 스냅샷 bin 접기), SKU×창고 단위.
- **verdict 최소 규칙**: `diff=0 → match`(카운트만) · `IN_TRANSIT → explained` · **나머지 전부
  `unknown`**. ⚠️ `missing_event`·`calc_error` 는 자동 판정하지 않는다 — 원인 조사의 결론이지
  계산 결과가 아니다. 사람이 unknown 을 닫으며 기록하는 값이다.
- **`inv_compare` 에는 diff≠0 행만**(match 는 `inv_compare_runs.match_count` 로). 대가 = 「어느
  SKU 가 언제부터 맞았나」의 행 단위 이력 없음 — 필요해지면 전 쌍 기록 + 14일 reap 으로 바꾼다.
- **`inv_compare_runs`**(wms_health_runs 동형): 카운트 + unknown 상위 20 샘플 + **`cursors`
  jsonb**(대조 시점의 `inv_sync_state` 전체 — 「타이밍 차이」 재확인의 유일한 근거: unknown 이
  다음날 match 로 돌아오면 이 커서 위치가 증거).
- **보존**: compare 스냅샷 14일(`not like '%-initial'` — **기준선 불가침을 코드로 강제**) ·
  runs 90일 — RPC 끝에서 reap(health_snapshot 패턴).
- **IN_TRANSIT 짝 없음 = 정상**: `inv-snapshot` 은 `OnHand` 만 읽는다(index.ts — `InTransit`
  컬럼 미참조). ⭐ [실측 2026-08-24 BMA15710] Cin7 도 운송 중을 `ON HAND` 에서 빼고 별도
  `IN TRANSIT` 컬럼으로 관리(에드먼튼 ON HAND 0 · IN TRANSIT 204) — **원장의 합성 창고와 같은
  모델**이라 창고별 수치가 자연히 맞는다. [원장 대조] 토론토 910 · 에드먼튼 16 · IN_TRANSIT 204
  — SKU 단위 완전 일치. **스냅샷+사건 누적이 작동한다는 첫 증명.**
- ✅ **cron 2잡 등록**(기록 = cron.sql 잡 12·13 — 첫 수동 실행 확인 후 등록, ⑤ 가동과 같은 순서):
  스냅샷 `inv-snapshot-compare` `21 5 * * *`(05:21 UTC = 토론토 01:21 EDT·에드먼튼 23:21 MDT —
  두 창고 마감 후) → RPC `inv-compare-run` `36 5 * * *`. 분 21·36 은 빈 분(전수 계산 — 빈 분 집합
  {1,3,6,18,21,26,28,31,33,36,41,43,46,48,51,56,58}. 2026-08-21 purchase 겹침 교훈).
  ⚠️⚠️ **둘 사이의 15분 간격이 설계의 핵심이다** — 대조는 「그 스냅샷을 찍은 시각의 원장」과 맞춰야
  한다. 스냅샷과 대조가 벌어지면 그사이 판매가 원장에만 반영돼 **정상 재고가 불일치로 보인다**
  (아래 「대조를 잘못 돌리면 생기는 오탐」 (b) — 실측 134건). 15분은 스냅샷 실행 43초의 여유분이지
  임의 값이 아니다. **cron 이 이 규율을 구조적으로 지킨다 — 깨는 것은 수동 실행뿐이다.**
  ⚠️ 등록 실물은 항상 `select jobid, jobname, schedule, active from cron.job order by jobid;` 로
  확인할 것.
  ⚠️⚠️ **[정정 2026-08-26] 이 문서와 cron.sql 이 03:21·03:36 으로 기록하고 있었으나 실물은
  05:21·05:36 이다** — `cron.job` + `inv_snapshot.taken_at`(01:21 토론토 · 8/25·8/26 이틀 연속) +
  `inv_compare_runs.ran_at`(01:36 토론토) **세 경로가 일치**했다. 재해복구용 기록이 실서버와
  어긋나 있었다(그대로 복구하면 2시간 엉뚱한 시각에 재등록). **15분 간격은 그대로다** — 정정은
  시(hour)만 옮겼다.
  (같은 날 `inv-sku-types` `26 3 * * *` 도 등록하려 했으나 미등록으로 남았다 —
  [실측 2026-08-24] `cron.job` 에 없었다. 경위·영향은 아래 FINAL-SALE 처방 절.
  ✅ **2026-08-26 등록 완료 — [실측] jobid 15.**)
- [검증 2026-08-24 로컬] `db reset` 재생 + 테스트 6케이스 전 통과(verdict 분포 · diff≠0 만 기록 ·
  generated diff · 재실행 멱등 · 스냅샷 없음 가드 · **14일 reap + `-initial` 불가침**) + 기존
  스위트 회귀(팩 9 · 픽 12).

#### ⚠️ 첫 회차 예측 (2026-08-24 — 실행 전에 적는다)

**예측된 unknown 이 예측대로 나오면 그 자체가 대조 장치의 검증이고, 예측 밖 unknown 이 나오면
그게 ⑥의 첫 수확이다.**

1. **match 압도 다수(추정 97%+)** — 8/20 이후 사건이 있는 SKU 자체가 소수. BMA15710 완전 일치가 표본.
2. **explained = IN_TRANSIT 잔량 보유 SKU 소수** — TR-03975/76 + 8/21 이후 신규 이동분.
3. ⚠️ **예측 unknown ①: TR-03975/76 의 출발 창고 쌍** — Cin7 은 출발분을 이미 뺐는데(BMA15710
   모델) 원장은 커서가 그 문서 **앞에서 대기 중**(`hold_intransit_before_since`)이라 아직 안 뺐다
   → `ledger_qty > cin7_qty`. 원인을 이미 알지만 최소 규칙상 unknown 으로 나온다 — 그 두 TR 로
   설명하고 닫는다(도착 후 since 경계 아티팩트와 함께 자연 소멸).
4. **예측 unknown ②: 당일 타이밍** — 스냅샷(05:21) 직전 마지막 수집 회차 이후 사건. 밤이라 거의 0.
5. 그 밖의 unknown = **수집 누락·계산 오류의 첫 신호** — 하나씩 원인을 찾아 수집을 고치는 것이
   2단계(대조)의 본업이다.

⬜ **첫 실행은 수동**: ① `curl …/inv-snapshot?key=auto-compare` (wrote ≈ 13,800 · aborted null 확인)
② `select inv_compare_run('YYYY-MM-DD-compare')` ③ 결과가 위 예측과 맞는지 확인 → 그 뒤 cron 등록.

#### ✅ 첫 회차 실측 (2026-08-24) — unknown 1건 = ⑥의 첫 수확

**`compared_pairs 13,836 · match 13,835 · explained 316(IN_TRANSIT) · unknown 1`.**

⚠️⚠️ **카운트 관계 — 셋을 더하면 안 된다**: `compared_pairs = match + unknown`
(13,836 = 13,835 + 1) 이고 **explained(IN_TRANSIT)는 pairs 밖의 별도 집계**다 — IN_TRANSIT 은
Cin7 에 짝이 없어 비교 쌍에서 제외되고 따로 센다(RPC 의 `filter (warehouse <> 'IN_TRANSIT')`).
📌 **첫 회차에서 13,835+316+1=14,152 로 셋을 배타 합산해 "정합이 안 맞는다"고 오독했다** —
같은 오해를 반복하지 않도록 남긴다. ⚠️ 316 은 "IN_TRANSIT 넷 잔량 ≠ 0 인 SKU 수" — 진짜 운송 중
+ **transfer 커서가 아직 도착을 못 본 문서**(출발만 기록)가 섞여 있다. 커서가 따라잡으면 줄어야
정상 — 추이를 볼 것.

**unknown 1건 = `FINAL-SALE`**(Asung Trading Inc. · 원장 −34 · Cin7 0 · SO-15097 sale_out 8/21).
Cin7 에서 **`Type=Non-inventory`**(파손품을 adjustment 로 이미 뺀 뒤 판매할 때 쓰는 껍데기 SKU —
Cin7 은 재고를 안 움직인다)인데 판매 수집이 Pick.Lines 를 무필터로 잡아 재고 사건으로 쌓았다.
📌 **대조 장치가 없었으면 조용히 누적됐을 것** — ⑥의 존재 이유가 첫 회차에 실증됐다.
⚠️ `IsService` 와는 **다른 개념**(cin7-api 스킬 15번) — FINAL-SALE 은 IsService=false 라
기존 `IsServiceOnly` 필터의 틈에 빠졌다.

##### ✅ 처방 — 비재고 게이트 (같은 날 구현·배포 · 마이그레이션 `20260824140345`)

- **`inv_sku_types` 캐시 + EF `inv-sku-types`**(cron 일 1회 예정 — ⚠️⚠️ **[실측 2026-08-24]
  cron 미등록**: `cron.job` 에 없다(발견 경위: WMS 자동 Hold 가 jobid 14 를 받았고, 12·13 다음이
  14 라는 사실이 곧 미등록의 증거 — jobid 는 시퀀스라 재사용되지 않는다). **영향**: 캐시가
  갱신되지 않아 48h 뒤부터 `cache STALE` 경고 · 게이트는 옛 목록으로 작동하나 **새 비재고 SKU 를
  못 막는다**. 등록 = `ops/cron.sql` 해당 절 실행 후 **받은 jobid 를 그 파일에 반영**) — `/product` 전량에서
  `Type ≠ 'Stock'` 인 SKU 만 통째 교체(all-or-nothing). ⚠️ **상수로 박지 않았다**: 상품이 늘면
  코드 배포가 필요해지고, 새 비재고 SKU 는 **대조에서 unknown 이 날 때까지 모른다**(오늘이 그 사례).
- ⚠️ **왜 별도 EF 인가** — inv-collect 안에 두면 6잡이 각자 갱신을 시도하고(동시 실행 시 중복)
  소스별 회차가 서로의 캐시 상태에 의존한다. 별도 EF 는 **실패가 격리된다**.
- **게이트는 `makeSink` 의 push 진입부 한 곳**(since 필터 **앞**). 6소스가 전부 그 지점을 지나므로
  (배출구 = 공유 루프 2곳뿐: ②-a 조정·이동·조립 / ②-b 판매·발주·반품) **새 소스도 자동 적용**된다.
- **판정은 「테이블에 있으면 차단」** — `product_type`·`is_service` **값을 보지 않는다**.
  로드 쿼리부터 `select=sku,refreshed_at` 이다. 값이 아니라 **행의 존재**가 판정이고, 그 행을
  만드는 조건이 `Type≠'Stock'` 하나이므로 결과적으로 product_type 기준이다.
- ⚠️ **캐시 미스는 fail-open(통과 + 경고)** — 원장은 shadow 이고 **대조가 안전망**이다.
  차단으로 막으면 비재고 몇 건 때문에 **정상 재고 수집까지 멈춘다**. 경고 3분기(EMPTY·STALE 48h·
  UNREADABLE)를 응답 `non_stock_gate` 에 실어 **조용한 폴백을 금지**한다.
- ⚠️ **저장 범위 계약 — Stock 은 넣지 않는다.** 넣으면 소비자의 무페이지 단일 조회가
  PostgREST 1,000행 캡에서 조용히 잘리고 **잘린 뒤의 비재고 SKU 가 게이트를 통과**한다(규칙 20 계열).
  마이그레이션 주석에 계약을 박고, 게이트 로드에 **800행 근접 경보**(캡 도달 **전에** 알린다) +
  `caps-ok` 사유 주석으로 고정했다. 커밋 훅이 이 조회를 실제로 막아 세워 생긴 결론이다.
- **게이트 테스트 8케이스** `scripts/test-invcollect-gate.mjs` — ⚠️ 원본 파일에서 `makeSink` 를
  **실행 시마다 원문 추출**해 node 로 돌리므로 **구현이 바뀌면 테스트가 따라온다**.
  ⑤~⑦은 정적 검사(배출구 정확히 2곳 · 두 호출 모두 `nonStockSkus` 전달 · 폴백 경고·노출 존재)
  — **세 번째 배출구를 만들면 깨진다**(게이트를 우회하는 새 경로가 곧 다음 FINAL-SALE 이다).
  ⚠️ 이 방식을 택한 이유: **게이트를 실서버 dry 로 검증할 수단이 없다** — 커서를 무시하고 특정
  창을 강제 조회하는 파라미터가 없다(`from_cursor` 함정 절 참조).

**정정 — 이미 쌓인 −34**: **상쇄 행**(`source='manual'` · `event_type='manual_reversal'` ·
`line_ref` 에 reversal · raw 에 사유)으로 처리. ⚠️⚠️ **물리 삭제는 하지 않았다** —
append-only 의 **첫 위반이 「사소한 1건」인 것이 가장 위험**하다(4부 3번 「정석 = 역분개」의 첫 실사용).
⬜ 상쇄 후 `unknown 0` 확인은 **아직 못 했다** — 같은 날 재대조가 스냅샷 결손에 오염됐기 때문
(아래 (c)). **내일 아침 cron 첫 자동 회차가 첫 정상 확인이다.**

📌 **실측 비-Stock 45건**(`Non Inventory` 2 + `Service` 43). ⚠️ `_5_`·`_6_`(DEPRECATED)는 Cin7 이
`Service` 로 분류한다 — 어느 쪽이든 Stock 이 아니므로 차단되고, 분류가 바뀌면 EF 의 `type_changed`
diff 가 매 회차 보고한다(**거르지는 않는다** — Cin7 이 마스터를 바꾼 것은 신호이지 우리가 판정할 일이
아니다).
📌 ⚠️ **`is_service` 컬럼은 전 행 false 다** — `productList` 응답에 그 필드가 없어 기본값이 저장될
뿐이고 **판정에 쓰이지 않는다**(위 「행의 존재가 판정」). 오해를 부르는 컬럼이므로 **정리 후보**.

📌 **예측 채점**: ① TR-03975/76 출발 창고 unknown — **안 나왔고, 안 나온 게 맞다**(그 출발은
스냅샷 **이전**이라 기준선에 이미 빠져 있었다 — 예측이 그 점을 놓쳤다. 예측 실패의 원인까지
남긴다) ② 당일 타이밍 unknown — 0(예측대로) ③ **예측 밖 unknown 1건(FINAL-SALE)= 첫 수확**
(예측 문구 그대로: "예측 밖 unknown 이 나오면 그게 ⑥의 첫 수확이다").

#### ⚠️⚠️ 대조를 잘못 돌리면 생기는 오탐 (2026-08-24 — 같은 날 3회차 실측)

**같은 날 대조를 세 번 돌렸고 결과가 매번 달랐다. 진짜 발견은 1차 한 건뿐이다.**

| 회차 | unknown | 정체 |
|---|---|---|
| 1차 (스냅샷 직후) | **1** | ⭐ 진짜 결함 (`FINAL-SALE`) |
| 2차 (6시간 뒤) | 134 | ⚠️ **낡은 스냅샷** — 원장만 앞서 나간 것 |
| 3차 (재촬영 직후) | 165 | ⚠️ **불완전 스냅샷** — Cin7 이 적게 줬다 |

📌 **셋 다 「원장이 틀렸다」로 읽힐 수 있었다.** 대조 장치는 **어떻게 돌리느냐가 결과를 만든다** —
숫자를 보기 전에 **무엇과 무엇을 맞췄는지**를 먼저 확인할 것.

##### (a) ⚠️ 같은 키로 재촬영하면 첫 값이 남는다

`inv-snapshot` 은 `ignore-duplicates` 라 **당일 첫 스냅샷이 대조 기준**이다. 오후에
`key=auto-compare` 를 다시 불러도 `wrote: 5`(그사이 새로 생긴 조합만)로 끝나고 **13,800여 행은
아침 값 그대로**다. ⇒ **재촬영하려면 다른 키를 써야 한다.**
📌 이 동작 자체는 의도된 안전장치다(같은 날 두 번 찍어 기준이 흔들리는 것을 막는다) — 다만
**"다시 찍었으니 최신이겠지"가 성립하지 않는다**는 것을 알고 써야 한다.

##### (b) ⚠️⚠️ 대조는 스냅샷과 붙어 있어야 한다

아침 13:19 스냅샷에 **저녁 원장**을 맞추면, 그사이 판매가 **원장에만** 반영돼 **134건이 어긋난
것처럼** 보인다. [실측] `ARD62157` diff −45 = 원장의 `SO-15102`(당일 판매) 그대로 ·
`FAN03021` −32 · `AS91651` −24 — **차이가 원장 사건과 정확히 일치**했다(원장은 맞고 기준이 낡았다).
📌 **cron 설계(스냅샷 21분 → 대조 36분, 15분 간격)가 이것을 구조적으로 막는다.** 규율을 깬 것은
수동 실행이었다. ⇒ ⚠️ **자동화가 준비된 것을 수동으로 급히 돌리지 말 것.**

##### (c) ⚠️⚠️ 스냅샷이 조용히 불완전할 수 있다 — **처방 절반(기록 완료 · 판정 미결)**

```
아침 13:19  Cin7 목록 22,079건 · 23페이지 → 13,829행
밤   15:17  Cin7 목록 21,877건 · 22페이지 → 13,787행   ← 190건 적고 페이지도 1 적다
                                                       · 소요 1분44초(평소 43초)
```
⚠️ 응답은 **`truncated: false` · `rate_limited: false`** 였다 — 받은 만큼은 다 받았으니 거짓말이
아니지만 **Cin7 이 애초에 적게 줬다.** `list_total == received_rows` 가 성립하므로
**all-or-nothing 가드가 구조적으로 못 잡는다**(가드는 "받기로 한 만큼 받았나"만 본다).
[실증] `AS93125` 토론토 2,662개가 **그 스냅샷에만** 없었다 — Cin7 화면에는 그대로 있었고
Movements 에 8/20 이후 사건도 없었다. ⇒ **unknown 165건은 원장 문제가 아니라 스냅샷 결손**이었다.

⚠️⚠️ **이번엔 대조용이라 무해했다. 그러나 기준선(`-initial`)이 이렇게 찍혔다면 원장 전체가
잘못된 출발점을 갖는다** — 그리고 그것은 **어느 대조에서도 드러나지 않는다**(기준선이 곧 진실의
정의이므로). 📌 다행히 `2026-08-20-initial` 은 정상 촬영됐다(23페이지 22,079건 · 검증 완료).

🔵 **처방 절반 구현 — 회차 로그 신설(2026-08-26) · 판정은 아직 없다**

**왜 절반인가**: 급감 검사는 「직전 회차와 비교」인데, **지난 회차 값이 어디에도 남지 않았다.**
필요한 값(`list_total`·`pages_scanned`·`duration_ms`)이 `summary` 로 **이미 계산되고도**
응답으로 나간 뒤 사라졌다. ⇒ 판정보다 **기록이 선행 조건**이었다.

- **`inv_snapshot_runs`**(마이그레이션 `20260826130408`) — `inv-snapshot` 이 회차당 1행.
  급감 3축 + 회차 실적(`received_rows`·`insert_rows`·`wrote`·`existing_rows_before`·
  `db_rows_after`·`truncated`·`rate_limited`) + `warehouses`/`summary` 원본 jsonb.
- ⚠️ **`insert_rows` 는 `inv_snapshot` 의 `count(*)` 로는 못 얻는 값**이다 —
  같은 키 재촬영이 **누적 합집합**이 되므로(§4-a). per-run 행 수는 EF 만 안다.
- ⚠️ **`aborted` 회차도 기록한다** — 중단된 회차가 가장 알고 싶은 회차다.
- ⚠️ **`dry` 는 기록하지 않는다**(`logRun` 헬퍼 첫 줄에서 차단).
  분기 순서상 `aborted` 검사가 `dry` 보다 앞이라 **dry+중단이 로그 경로로 들어온다.**
  수동 dry 는 429·time 으로 끊기기 가장 쉬운 호출이라 기준선을 오염시킨다.
- ⚠️ **로그 실패가 스냅샷을 죽이지 않는다**(try/catch → `summary.run_log_error`).
  스냅샷은 하루 한 번뿐이라 놓치면 **그날 대조가 통째로 날아간다.**
- **DELETE/TRUNCATE 회수** — `inv_compare_runs` 와 다르다. 그쪽은 재계산 가능한 결과지만
  회차 로그는 재생성 불가능한 증거이고, **직전 행이 지워지면 급감 판정 자체가 불가능**해진다.

⬜ **남은 것 — 판정(임계값)**. 정상 회차의 변동 폭을 며칠 본 뒤에 정한다.
⚠️⚠️ **판정 축은 행 수가 아니다**: [실측] 08-26 정상 회차 **−66행** vs 08-24 불량 회차 **−42행** —
**행 수로는 정상과 불량을 가를 수 없다.** 그날을 실제로 가른 것은
`list_total`(22,079→21,877) · `pages_scanned`(23→22) · `duration_ms`(43초→1분44초) 셋이다.
⚠️ 「거부 vs 경고」도 여전히 미결이다 — **거부는 그날 스냅샷이 아예 안 찍히는 것**이고,
그러면 그날 대조가 통째로 사라진다(원장의 유일한 안전망이 하루 없어진다).

📌 회차 간 비교는 **행이 둘 이상 쌓인 뒤**부터 가능하다:
```sql
select ran_at, list_total, pages_scanned, duration_ms, insert_rows,
       list_total - lag(list_total) over (order by ran_at) as total_diff
from inv_snapshot_runs order by ran_at desc limit 7;
```

📌 **등록 후 검증은 결과물로만 한다** — `cron.job_run_details` 의 `succeeded` 는 아무것도 보장하지
않는다(2026-08-19 실측 — HTTP 요청을 **띄운 것**까지만 본다). `inv_sync_state.last_run_at` 갱신과
`last_cursor` 전진이 유일한 증거다.

⚠️ placeholder 잔존 확인은 **값을 노출하지 않는 방식으로**:
```sql
select jobname,
       command like '%SECRET%'             as still_placeholder,
       command like '%since=2026-08-20%'   as has_since
from cron.job where jobname like 'inv-collect%';
```

---

### 신규 미확정 (2026-08-18 저녁 관찰에서 등록)

~~⚠️ 화면 `Authorized` vs API `PutAway.Status=DRAFT` 불일치 (2026-08-18 미판정)~~ →
✅ **닫힘 (2026-08-19 09:01 재조회)**: **지연이었다.** PO-01117 의 PA 블록이 `AUTHORISED` 51줄로
바뀌었다(`LastUpdated` 08-19 12:36Z). **화면이 먼저 바뀌고 API 가 따라온다** — 원장은
`AUTHORISED` 만 기표하므로 그 구간의 문서는 **다음 회차에 기표된다**(유실 아님, 지연).
⚠️ 그래서 `putaway:DRAFT` 는 오류 신호가 아니라 **진행 중 상태**다(스킬 함정 표 참조).

~~⚠️ 미달 입고분의 행방 (미확인)~~ → ✅ **닫힘 (2026-08-19)**: PO 수량을 인보이스 수량으로
채워 문서를 닫고(PA `CAN01620` 168→192), 차액은 `ST-01220` adjustment 로 되돌렸다.
전체 흐름은 1부 「⭐ 미달·초과 입고의 실무 흐름」이 정본 — **원장에 추가 이벤트가 생기는 것이
맞고**(po_in + adjustment), 총량은 adjustment 절대값이 흡수한다.

### 쓰기를 켜기 전에 결정해야 하는 것 (2026-08-17 등록 — ~~넷 다 열려 있음~~)

**현황 (2026-08-20 ⑤ 가동 시점)**: ☑1 캡·페이싱(소스별 cron 분리까지 완료) ·
☑2 `since` 영속화(**cron URL 에 박는 쪽으로 결정** — 아래 2번) ·
🟡3 DO NOTHING(감지·경고는 구현 완료 · 역분개는 자리 단위 과제로 이월) ·
🟡4 시크릿 교체(교체 완료 · **cron 헤더 갱신이 실제로 먹었는지는 미확인** — 아래 4번).
⇒ **⑤ 진입에 열린 항목은 없다.** 3·4 는 성격이 다른 잔여다(3=설계 이월 · 4=실행 확인 대기).

1. **상세 캡·페이싱** — ✅ **[실측 2026-08-18] Cin7 한도 = 60콜/60초 · 애플리케이션 키 단위 ·
   회복 31초(표기 60초). 200 응답에는 ratelimit 헤더가 없어 사전 제어 불가.**
   (선행 조건이었던 「Cin7 실제 한도 확인」이 이날 밤 GAS 프로브로 충족됐다 —
   `cin7-api` 스킬 11번 · 세션 문서 §9.)
   ⇒ **`DETAIL_SLEEP_MS=700ms` 는 분당 85.7콜로 한도의 1.43배 — 초과가 확정됐다.**
   **1,200ms(분당 50콜)로 올린다.** 상세 40건 = 28초 → 48초. 60초 창 안에 들어간다.
   ⇒ **`MAX_DETAIL_PER_SOURCE=40` 의 근거가 생겼다**: 사전 제어가 불가능하므로 회차당 호출 수를
   미리 묶는 것이 유일한 예방책이고, `1,200ms × 40 = 48초 < 60초 창`이다.
   ⚠️ 캡을 올리려면 **간격도 함께** 봐야 한다 — 캡 55 × 1,200ms = 66초로 창을 넘는다.
   ⚠️ **재시도 백오프를 고쳐야 한다** — 현재 1.5s→3s(최대 4.5초)는 실측 회복 31초의 1/7 이라
   사실상 재시도가 아니다. **`Retry-After` 를 읽어 그만큼 대기**(없으면 60초)로 바꾼다.
   ⚠️ **같은 키를 쓰는 주체끼리는 한도를 공유한다.** ~~WMS 키를 `hello`(5분 폴링)·`receiving`·
   `inv-collect` 가 함께 쓰는지 코드 확인이 남았다~~ → **공유가 확정**(`inv-collect` 상수 주석:
   `hello`·`receiving`·`product-images`·`inv-snapshot` 이 같은 WMS 키를 쓴다).
   ✅ **첫 수단을 실행했다 (2026-08-20)** — pg_cron 6잡의 **분(minute)을 `hello` 의 `*/5` 와
   어긋나게** 배치(위 「⑤ 쓰기 가동」 표).
   ✅ **2026-08-19 코드 반영 완료** — `DETAIL_SLEEP_MS=1200` · `_shared/cin7.ts` 백오프를
   `Retry-After` 기반(재시도 1회, 최대 60초)으로 교체. `MAX_DETAIL_PER_SOURCE=40` 은 유지(근거 주석 추가).
   ✅ **검증 완료 (2026-08-19 — 게이트 5번 ☑)**: 단일 소스 발주 `ledger_rows 727` 로 **페이싱 전과
   동일**(속도만 바뀌고 결과는 안 바뀐다) · **6종 동시 실행 `?dry=1` 에서 `rate_limited` 가 전 소스
   false — 429 한 건도 없음.**
   ✅ **소스별 cron 주기 분리 완료 (2026-08-20 — 위 「⑤ 쓰기 가동」 표 jobid 6~11).**
   ~~⚠️ 남은 것~~: `TIME_BUDGET_MS=120초` 안에 6종을 다 돌릴 수 없다.
   [실측 2026-08-19 동시 실행] 첫 소스 adjustment(5,009행)가 예산을 거의 소진 → `transfer` 0행
   `capped:"time"` → 나머지 4종 `aborted: "time budget exhausted before this source"`.
   ⇒ 한 회차에 전 소스를 돌린다는 전제 자체를 버린다.
   📌 **이 결과는 페이싱 실패가 아니라 시간 가드의 정상 동작이다** — 페이싱의 판정 기준은
   `rate_limited` 이고, 그것이 전 소스 false 였다. 두 가드를 섞어 읽지 말 것.
   ⚠️ **별건 등록**: `hello:31 DETAIL_DELAY_MS=250ms` = **분당 240콜 = 한도의 4배**.
   평시에는 기억 스킵(`wms_polled_sales`)으로 상세가 회차당 0~1건이라 버스트가 없고 429 시
   `sleep(60000)` 자체 방어도 있으나, **기억이 빈 첫 회차·TTL herd·배포 직후**에 4배 버스트가 된다.
   수동 dry 는 소스 하나씩 · 사이 90초.
   **선택지: `inv-collect` 전용 애플리케이션 키 발급.** 한도가 키 단위이므로 원장 수집이 60콜을
   통째로 쓸 수 있고, `hello` 폴링·`receiving` Apply 와 완전히 분리된다. 캡을 올릴 여지도 커진다.
   ⚠️ **Cin7 약관상 허용 여부 미확인** — 계정당 제한의 취지를 키 발급으로 넓히는 셈이라,
   기술적으로 되는 것과 해도 되는 것은 다르다. 필요해지면 Cin7 지원팀에 문의할 것.
   ⚠️ 키가 늘면 교체·분실 관리 지점도 는다(2026-08-18 시크릿 노출·교체 경험 — 아래 4번).
   📌 **지금 당장은 불필요하다** — 1,200ms × 40콜 = 48초로 60초 창 안이고, 폴링이 회차당 2~3콜을
   쓰는 것을 감안해도 여유가 있다. **캡을 크게 올리고 싶어질 때의 카드**로 남긴다.
   ⚠️ **[실측 2026-08-19] 판매는 하루치 창(`from_since=2026-08-18`)에서 후보 145 — 캡 40의 3.6배다**
   (발주 1.5~1.8배보다 심하다). pg_cron 회차마다 40건씩 처리하면 **하루치를 따라잡는 데 4회차**가
   필요하다. 커서가 이어받으므로 유실은 없으나 **실시간성이 떨어진다** — 매일 shadow 대조를 돌릴 때
   전날 판매가 아직 안 들어와 있을 수 있다.
   ⇒ ⚠️ **해결은 캡 상향이 아니다.** `1,200ms × 40 = 48초`로 이미 60초 창의 80%를 쓴다 —
   캡 55면 66초로 창을 넘는다. **회차 주기를 짧게 가져가는 쪽**이 답이고, 소스별로 캡·주기를
   다르게 두는 안을 ⑤에서 정한다(판매·발주는 자주, 반품은 드물게 — [실측] 반품은 하루 4건).
2. **`since`(이벤트 날짜 필터)의 영속화** — 커서 시작점은 저장된 커서로 자동 영속되지만
   `since` 는 그런 경로가 없어 매번 파라미터다. ~~cron URL 에 박으면 스냅샷 재촬영 때 cron 을
   손으로 고쳐야 한다 → `inv_sync_state` 에 담는 안 검토.~~
   ⇒ **2026-08-20 결정: cron URL 에 박았다**(`since=2026-08-20` — 6잡 전부).
   **비용을 알고 고른 것이다** — 스냅샷을 다시 찍으면 `cron.job` 의 command 6줄을 손으로 고쳐야 한다.
   테이블 영속화는 코드 변경이 필요하고 ⑤ 진입을 늦추는데, 스냅샷 재촬영은 **드문 사건**이라
   수작업 6줄이 더 싸다. 📌 재촬영 절차에 「cron 6잡의 `since` 갱신」을 반드시 포함할 것 —
   빠뜨리면 **조용히 이중 계상**된다(원장이 부풀고, 대조에서 원인 불명 차이로만 보인다).
   ⚠️ 위 「날짜 커서 축」 절의 `since`/`from_since` 이름 혼동 주의.
3. ⚠️ **문서 수정 시 중복 무시(DO NOTHING)가 수정을 버린다** — 증분 축 3종은 수정되는
   문서들이다. 갱신 축이 수정된 문서를 다시 물어와도 유니크 키 충돌로 조용히 버려져 **옛
   수량이 원장에 남고 대조에서 "설명 불가 차이"로만 나타난다.** 유니크 키는 재실행 멱등성용
   설계지 수정 반영을 고려하지 않았다. 정석 = 충돌인데 값이 다른 행 감지 → 시끄럽게 알림 →
   역분개 + 재기표.
   [2026-08-18 확인] **PO 번호는 수정으로 바뀌지 않는다.** 따라서 「수정 = 새 번호 = 새 키」
   기대는 성립하지 않고 **이 항목은 소멸하지 않는다.** 수정 유형별로 증상이 갈린다 —
   ① **수량 정정**(같은 빈·같은 CardID): 키가 동일 → DO NOTHING → 옛 수량이 남고 차이가
     조용히 묻힌다
   ② **빈 변경**: 빈이 키에 있어 충돌 없이 **두 벌**
   ③ **블록 VOID 후 재입력**: 새 CardID 로 **두 벌**(원장은 append-only 라 옛 행이 안
     지워진다 — 역분개 필요)
   ~~⚠️ 셋 다 감지 장치가 없고 대조 단계의 숫자 차이로만 드러난다.~~ → ✅ **2026-08-19 구현됨 —
   아래 「감지 장치」 절.** 종전 서술은 그날 오전까지의 상태다(가설 → 실례 → 구현이 **하루 안에**
   진행됐다). ⑤ 전에 최소한 **「충돌인데 값이 다른 행」 감지·경고**가 필요하고 실무 빈도에 따라
   역분개 설계까지 갈 수 있다 — 는 판단은 그대로 유효하고, **앞부분(감지·경고)이 닫혔다.**
   📌 ~~실무 빈도 확인 대기(Caleb)~~ → **[실측 2026-08-19] 시나리오 ①(수량 정정)이 실재하며,
   예외가 아니라 정규 절차다.** PO-01117 의 PA 라인 `CAN01620` 이 **168 → 192** 로 바뀌었다 —
   **`CardID`·bin·문서번호 전부 동일**, 수량만 다르다(위 1부 「미달·초과 입고의 실무 흐름」
   3단계 = PO 를 닫기 위한 채움). `DO NOTHING` 이면 192 는 조용히 버려진다.
   ⚠️ **다만 긴급도는 낮아진다** — adjustment 가 절대값이라 뒤이어 오는 조정이 총량을 확정한다.
   168 이 남아도 `adjustment 177` 을 만나면 잔고는 177 이 된다.
   ⇒ **총량 정확도**: adjustment 가 흡수 · **bin 단위 정확도**: 어긋난 채 남음 · **구간 오차**:
   PO 채움과 adjustment 사이 구간은 틀리다(대조에서 **타이밍 차이**로 잡힌다).
   ⇒ ~~**⑤ 전에는 「키는 같은데 값이 다른 행」 감지·경고만으로 충분하다.**~~ → **그 판단대로
   구현했다 (2026-08-19 — 아래).** 없으면 **bin 이 언제부터 어긋났는지조차 알 수 없다.**
   역분개(−168, +192) 설계는 자리 단위 원장으로 올릴 때로 미룬다(그 판단은 불변).
   (②·③의 빈도는 여전히 미확인 — 빈 재배치는 별도 트랜스퍼 문서라는 실측이 있어(스킬 15건)
   **②는 안 일어날 가능성이 크다.**)

   **감지 장치 — `inv_conflicts` (2026-08-19 구현 · 게이트 4번 🟡)**
   마이그레이션 `20260819000000_inv_conflicts` + `inv-collect` 의 `writeLedgerDetectingConflicts`
   (`@2026-08-19.1`). 배포·적용 완료.
   - **동작**: 배치 INSERT 를 `return=representation`(`select=` 키 7종으로 좁힘)으로 보내 **실삽입
     행**을 받고, 보낸 것과 **키 7종으로 매칭**해(배열 순서 아님 — PostgREST 순서 보장 근거 없음)
     버려진 행을 특정한다 → 그 행들만 **`line_ref` 50개 청크**로 조회해(doc_number 단독 조회는
     대형 트랜스퍼 1,376행/문서에서 1000행 캡에 잘린다 — 규칙 20) `qty_delta`·`occurred_on` 비교
     → **다르면 `inv_conflicts` 에 INSERT**. 같으면 무동작(정상적인 재수집).
   - **②-a·②-b 양쪽 삽입 지점 모두** 적용.
   - ⚠️ **쓰기를 차단하지 않는다** — 수량 정정은 정상 실무라 차단하면 원장이 자주 멈춘다. 기록만 한다.
   - ⚠️ **원장 행은 고치지도 지우지도 않는다**(append-only). 역분개는 자리 단위 원장 과제.
   - ⚠️ **첫 검증은 ⑤에서 `commit=1` 을 처음 켤 때다** — dry 는 쓰지 않아 버려짐 자체가 없다.
     그때 볼 것: `insert_skipped ≠ 0` 은 **정상**(재수집) · `conflicts_detected ≠ 0` 이면
     `select * from inv_conflicts order by detected_at desc`.
   - 📌 **첫 회차는 `insert_skipped` 가 0**이다(원장이 비어 있으므로) — **두 번째 회차부터** 의미가 있다.
   - 📌 **부수 의미 변경**: `R.written` 이 「시도 행수」 → **「실삽입 행수」**(재수집 중복 제외).
     `inv_sync_state.note` 에 `rows=N inserted=M` 병기. **소비처 0 확인 후** 변경 —
     ⑥ 대조 화면이 생기면 이 정의가 기준이다.
4. **수집 시크릿 교체** — 대화에 노출됨. 교체 시 cron 등록분과 검증 커맨드 동시 갱신.
   ✅ **`WMS_CRON_SECRET` 교체 완료 (2026-08-18)** — `cron.job` 4·5(`wms-image-sync`,
   `-retry`) 헤더도 함께 갱신했다고 기록돼 있다(`2026-08-18-purchase-putaway-axis.md` §7).
   ⚠️ **그러나 갱신이 실제로 먹었는지는 실행으로 확인되지 않았다 — 다음 실행 확인 필요.**
   실행 시각 **매일 08:30 · 09:30(토론토)** = `product-images` EF. 헤더가 옛 값이면
   **401 로 조용히 죽는다**(호출자가 사람이 아니라 cron 이라 아무도 모른다).
   확인: `cron.job_run_details` 에서 해당 jobid 의 최근 실행 `status`·`return_message`.
   실패 시 처방: `cron.job` 의 command 안 헤더 값을 새 시크릿으로 갱신.
   ⚠️ Supabase Secrets 는 저장값을 다시 보여주지 않으므로(digest 만) **값을 잃었으면 재교체가
   유일한 길.** 📌 jobid 1(폴링)은 이 시크릿을 쓰지 않아 무관 — 폴링이 도는 것을 근거로
   "cron 은 정상" 이라고 판단하지 말 것.

### 검토 중 — Advanced PO 기본값 (2026-08-18 · 두 번 논의하고 조사 직전 중단)

Cin7 설정 `Default load advance purchase order` 를 켜는 안. **판정은 아직 안 났다.**

**동기**: Simple PO 는 인보이스 수량과 입고 수량이 다르면 승인이 거부된다(`cin7-api` 스킬 14번
Simple 제약 절). 해소하려면 **사람이 매번 Cin7 에서 `Convert`** 를 눌러야 한다. 처음부터
Advanced 면 이 수작업이 사라진다.

**원장 쪽 이득**: `Type` 전환이 없어지므로 **모든 PO 가 두 번 `UpdatedSince` 에 잡히는 현상이
줄고**, `ON CONFLICT DO NOTHING` 위험(위 3번)의 한 축이 사라진다. 캡 압박(위 1번 — 후보가
캡의 1.5~1.8배인 원인이 전환에 따른 재등장)도 함께 완화된다.

**확인된 것 (2026-08-18 — 코드 실물 확인)**:
- ✅ WMS 의 Advanced 쓰기 경로는 이미 완비돼 있다 — stage1(`/advanced-purchase/stock` DRAFT →
  되읽기 → authorize) · stage2(`/advanced-purchase/put-away` **단일 AUTHORISED POST** 로 전 bin
  배치 후 `LocationID` 되읽기 검증). 그 앞에 **전 bin GUID 해석 pre-flight 하드 게이트**가 있어
  하나라도 못 풀면 불가역 지점 전에 전량 중단한다. **bin 이 빠질 걱정은 코드상 없다.**
- ✅ I&R 그룹 타깃 강제 · 외부 그룹 라인 감지 시 중단 — PO-01094 실사고 재발 봉쇄됨.
- ⚠️ **그러나 Advanced 는 「승인된 인보이스가 정확히 1개」가 아니면 Apply 자체가 중단된다**
  (`advInvoiceTargets` = `Status ∈ {AUTHORISED, PAID}` **이면서** `TaskID` 보유 → **0개 = throw ·
  2개+ = throw**, 다중 인보이스 부분 출하 미지원). Simple 은 Invoice First 게이트만 통과하면
  되므로 **Advanced 가 더 엄격하다.**
- ⚠️ **인보이스가 둘 이상인 PO 가 실재한다** — PO-00703(`InvoiceNumber: "5520110085, 5520110086"`).
  ⚠️ 단 이것은 대화 중 관찰이고 프로브 산출물로 기록돼 있지 않다 — 아래 조사에서 재확인된다.

~~**미확인 (다음 작업)**: 분할 인보이스 PO 의 비율.~~ → ✅ **조사 완료 (2026-08-19).**
조사 방법은 예정대로였다 — GAS 로 `Invoice` 블록 중 `Status ∈ {AUTHORISED, PAID}` 이면서 `TaskID`
를 가진 것의 개수를 센다(**Advanced 게이트 `advInvoiceTargets` 와 정확히 같은 조건** — 결과가 곧
Apply 가능 여부다). 판정 기준이었던 것: **0~2% → 켜도 됨 · 3~10% → 예외 처리 설계 선행 ·
10%+ → 켜면 안 됨**(Convert 수작업보다 Apply 불가가 훨씬 나쁘다).

✅ **[조사 2026-08-19] GAS 프로브 `ppa_invoiceSplit`, `UpdatedSince=2026-07-01` · 대상 128건**
(VOID·`IsServiceOnly` 제외).

결과: `{0개: 18, 1개: 101, 2개+: 9}` — 표면상 Apply 불가 **21.1%**. **그러나 성격이 갈린다:**
- **2개+ 9건은 전부 「앞으로 반복되지 않는 것」** — 8건은 **WMS 도입 이전**, 나머지 1건은
  **PO-01130**(크레딧 노트 예외, Apply 안 하기로 정한 건 — `asung-wms` 규칙 20 `externally_applied`).
  Caleb 확인: **「한 PO = 한 I&R」을 약속으로 정해 운영 중**이므로 신규 PO 에서는 발생하지 않는다.
- **0개 18건 중 대부분은 인보이스 미발행**(`ORDERED`·`NOT AVAILABLE`) — **Simple 에서도 Invoice
  First 게이트에 막힌다. Advanced 때문에 새로 생기는 문제가 아니다.**
- ⚠️ **0개 중 3건(PO-00874·00893·01112)은 인보이스가 `AUTHORISED`/`PAID` 인데 `TaskID` 가 없다.**
  셋 다 Simple 이다 — **Simple 의 `Invoice` 블록에는 `TaskID` 가 없는 것으로 보이나 미확인**.
  사실이면 **이 조사 방식이 Advanced 기본값 상황을 정확히 반영하지 못한 것**이다(처음부터
  Advanced 면 `TaskID` 가 붙는다).

⇒ **구조적 장벽 없음.** 남은 확인 2가지:
① `TaskID` 없는 3건의 정체 ② **Advanced PO 1건으로 실물 Apply 검증**(설정을 켜기 전 필수).
📌 **①은 ②로 자연히 답이 나온다** — **다음 리시빙 때 PO 하나를 수동으로 `Convert` 해놓고 Apply**
하면 설정을 안 켜고도 Advanced 경로 전체가 검증된다.

⚠️ **설정을 켜면 모든 PO 가 한꺼번에 그 경로로 간다.** 되돌릴 수는 있으나 그사이 잘못 들어간
입고는 손으로 정리해야 한다. **켜기 전에 Advanced PO 1건으로 실물 Apply 검증**을 거칠 것.

### ⚠️⚠️ 「수집 후 취소」 (2026-09-01 발견 · 미해결)

수집 시 `VOIDED` 는 제외하지만, **이미 원장에 들어온 문서가 나중에 취소되면 상쇄가 안 된다.**

```
[실사고 FG-00131] 8/28 13:13 수집 시 COMPLETED → 현재 VOIDED
검산: UNF18050 기초 84(8/20) − 우리 18 = 66,  Cin7 72  ⇒ 한 건 몫(6)이 남았다
```

⚠️ **빈도와 기제**: 조립 132건 중 VOIDED 86건이지만 **업무상 취소가 아니라 Cin7 의 기계적
동작**이다 — ①SO 편집 시 자동조립 재생성(`by System`) · ②트랜스퍼 픽용 SO 를 VOID 하면
딸린 자동조립도 VOID([실증 `SO-14692`] · 설계된 동작).
조립 자체는 **월 20건 안팎의 드문 작업**이다.
⭐ **우리가 걸리는 조건은 좁다** — 수집 주기 안에 상태 변화가 안 끝날 때뿐이고
[실측] 8월 VOID 12건 중 1건(`FG-00131`)이었다.
⚠️ **다만 기제 ②는 창이 길고 정기적**이라 앞으로 더 잘 걸릴 수 있다.
⚠️ 구멍 자체는 전 축 공통이지만 **판매·트랜스퍼는 사람이 Void 할 때뿐**이라 성격이 다르다.
⚠️ **전 축 공통**이다 — 판매·트랜스퍼·조정 모두 같은 구조.
📌 **소멸 감지(`inv_missing_lines`)는 라인 삭제만 잡는다** — 문서 전체 취소는 **다른 축**이다.

⬜ 처방 미착수. 조립은 목록에 `Status` 가 있어 상세 없이 판정 가능하다.

### ⚠️⚠️ 소멸 감지가 못 보는 축 둘 (2026-09-02 발견 · ✅ 1단계 2026-09-03 · ⬜ 2단계)

소멸 감지는 「언제 편집됐나」를 알아야 판정하는데 **목록에 그 값이 없는 축**이 있다.

| 축 | `LastModifiedOn` | 소멸 감지 |
|---|---|---|
| `transfer` | ✅ | 작동 |
| **`adjustment`** | ❌ | ⚠️ **사각지대** |
| **`assembly`** | ❌ | ⚠️ **사각지대** |

⇒ **그 둘의 라인 삭제·교체는 아무도 못 본다.**
[실측] `missing_lines_skipped_no_lmo` **33 = `detail_fetched` 33** — 전부 A 집합에서 빠졌다.
⭐ **`bin` 단위 대조만이 잡는다**(실사고 `ST-01283` — 에드먼튼 2칸 → 17칸으로 드러났다).

📌 부수 효과: 그 둘은 `inv_doc_state` 도 같은 값을 쓰므로 **매 회차 상세를 다 부른다.**
비용은 크지만 덕분에 재조회 창(결함 E 처방)이 잘 작동한다.

### ✅ 1단계 (2026-09-03 · `inv-collect@2026-09-03.1`)

**판정은 하되 표에 넣지 않는다.** 응답·회차 로그에 `missing_lines_unkeyed`·`_docs`·
`_sample`(≤200행)·`_truncated` 로만 남긴다.

⚠️⚠️ **「G4 조건만 완화」로는 안 된다.**
```
MISSING_CONFLICT = LEDGER_CONFLICT + ",last_modified_on"
```
그 값이 **유니크 키의 일부**이고 「같은 편집의 재검출은 do-nothing」으로 **중복 폭주를 막는
장치**다. 값 없이 넣으면 매 회차 재적재로 **회차 캡 500 을 소진해 진짜 소멸을 가린다**
(2026-08-31 사고와 같은 모양). 📌 G4 주석의 「키가 무너진다」는 옳았다.

⬜ **2단계**: `lmo` 자리에 **A 집합 키의 해시**(콘텐츠 지문)를 넣어 「내용이 그대로면 같은
지문 → do-nothing」으로 만든다. ⚠️ 마이그레이션(컬럼 추가 + 유니크 재생성)이 필요하다 —
1단계 실물을 보고 판단한다.

### ⚠️⚠️ 플립 — 이관 (2026-09-07 등록 · ⬜ 미설계)

⭐ **테스트 DB 는 버리는 물건이 아니다 — 그것이 내일의 운영이다.**

```
Cin7 의 오늘 상태 = 내일 IMS 의 시작 상태
```
📌 **연속성이 조건이다** — 창고가 다음 날 아침 출근해서 **어제와 같은 상태**를 봐야 한다.

⇒ ⚠️⚠️ **그러므로 진짜 시험은 「로직이 맞나」가 아니라 「이관이 되나」다.**
**로직이 아무리 맞아도 이관이 안 되면 못 켠다.**

**넘겨야 하는 것 — 재고 숫자만이 아니다**

| | |
|---|---|
| 재고(SKU × 창고 × bin) | ⭐ **재기준선과 같은 모양** — 절차가 있다(§기준선 「재기준선 절차」) |
| ⚠️ **미출고 오더** | [실측 2026-09-06] 그날 기준 **31건 · 6,105개**. 다음 날 나가야 한다 |
| ⚠️ **열린 PO** | 발주했는데 아직 안 들어온 것 |
| ⚠️ **백오더** | 스플릿된 잔여분 — **오버셀 정책의 결과**다 |
| ⚠️ **IN_TRANSIT** | 운송 중 트랜스퍼(실측 512칸) |
| ❌ **원가 레이어** | **Cin7 이 안 준다** — 평가액만 있고 레이어가 없다 |

⚠️ **마지막은 넘길 수 없다.** ⇒ **플립 날 원가는 근사치로 시작**하고,
**그 재고가 소진될 때까지 정확하지 않다.**

**⭐ 그리고 「넘기는 것」만으로는 부족하다 — SO 모듈이 Cin7 의 일을 받아야 한다**

⚠️ 위 표는 **플립 날 한 번 옮길 것**이다. 아래는 **그 뒤로 계속 해야 할 일**이다.

| 지금 Cin7 이 하는 일 | ⬜ SO 모듈이 받아야 할 것 |
|---|---|
| 주문 서밋 → `Allocated` | **언제 잡고 언제 푸는가** |
| ⭐ **오버셀 허용** | 재고 0 이어도 주문을 받는다 — **정책이지 결함이 아니다** |
| ⭐ **백오더 스플릿** | 한 오더를 **「보낼 것」과 「나중 것」**으로 가른다 (위 표의 「백오더」가 그 결과다) |
| 실시간 `Available` 을 Shopify 로 | ⚠️ **누가 밀어넣는가** |
| ⭐ **수요 데이터** | **프리오더가 예측의 입력**이다 — 그 축을 잃으면 안 된다 |

📌 ⚠️⚠️ **마지막 둘을 못 넘기면 Cin7 을 끌 수 없다. 재고 대조가 아무리 깨끗해도 그렇다.**

⇒ ⭐ 그래서 **`Available` 을 지금 만들지 않는다** — 원천이 여기 서야 생긴다
(§재고 마스터 계약 「가용(Available)」).

⇒ ⭐ **테스트 DB 에서 이 이관을 리허설한다.** 복사를 여러 번 해보며 절차를 다듬는 것이
그 환경의 **가장 큰 값**이다. ⚠️ 플립 자체는 **되돌릴 수 없다.**

⚠️ **회계는 마지막이다** — 원가가 근사치면 **COGS 도 근사치**이고 그것은 세금에 닿는다.
⬜ **회계연도 경계에 맞추는 것**을 검토해야 한다(⚠️ 회계사와 상의할 영역 · 기술 판단 아님).
📌 지금 Cin7 ↔ QBO 가 이미 연동돼 있다 — **그 경로를 우리가 대신하는 것**이 마지막 일이다.

### 알고 시작하는 위험

**원가 0 재고** — 조정으로 재고를 새로 잡을 때 단가 0으로 들어오는 경우가 있다. 원가 단계에서 공짜 재고가 쌓인다. Cin7 쪽에서도 고치는 방법이 확립돼 있지 않다.

**원본 데이터 오류** — 상품 코드와 실제 포장 단위가 다른 사례를 발견했다. 원장은 이걸 그대로 물려받는다. 원장이 틀린 게 아니라 원본이 틀린 것인데, 나중에 구별하기 어렵다.

**부분 선적 시 과다 차감 가능** — 판매 수량은 Pick 기준인데 배송 문서에 수량 필드가 없어
선택의 여지가 없었다(1부 판매 출고 절). 부분 선적이 실재해도 API 로는 탐지 불가 —
Movements 대조가 유일한 발견 경로다. 감수하는 구조적 한계.

**전체 합계를 확인할 방법** — 상품 하나씩은 Cin7 의 이동 이력으로 검산이 된다(세 건 전수 일치 확인). 그런데 창고 전체 합계를 확인할 API 경로가 없다. 화면 리포트를 사람이 보고 대조하는 수밖에 없다.

**사람이 손으로 하는 단계** — 매니저가 오더를 WMS 로 넘기는 작업이 수동이다. 재고 움직임과는 무관하지만, 대조할 때 "왜 이건 WMS 에 없지"를 매번 설명해야 한다.

---

## 부록 — 조사에서 확정된 사실

| 항목 | 결과 |
|------|------|
| 재고 차감 시점 | 배송(Ship). 픽·팩은 배정일 뿐 |
| 조정 문서의 수량 | 증감분이 아니라 조정 후 목표 수량 |
| 조정 문서의 당시수량 | 그 시점 값으로 박제됨 → 소급 계산 가능 |
| 신규 재고 조정 | 규칙이 다름. 수량이 그대로 증가분 |
| 창고 판정 | 위치 마스터의 상위 항목으로 판정. 2단 구조, 창고 3개 |
| 창고 이동 날짜 | 나가는 쪽=출발일, 들어오는 쪽=완료일. 전수 일치 |
| 이동 중 물량 | 어느 창고에도 안 잡힘 |
| 이동의 성격 | 97%가 같은 창고 안 자리 이동 |
| 묶음 상품 | 평소 재고 없음. 조립될 때만 잠깐 생김. 조립 문서가 구성을 담고 있음 |
| 발주 입고 날짜 | 입고 기록의 **라인**에 붙어 있음. 4건 전수 일치 |
| 조립 시점 | 오더 생성 때. 출고는 배송 때 — 그 사이 0~15일 |
| 같은 날 순서 | 문서 번호로는 못 정함. **유입 먼저, 유출 나중** |
| 이동 이력 | 전체가 남아 있음. 누적 = 현재 재고 (3건 전수 일치) |
| 매칭 키 | WMS 오더 862건 전부 Cin7 번호 보유, 중복 0 |
| 스냅샷 크기 | 약 1만 4천 줄, 1분 |
| 하루 수집량 | 문서 목록 10페이지 안팎 |
| 발주 입고 축 | **Advanced=PutAway · Simple=StockReceived** — SR 은 bin 이 구조적으로 없다(LocationID null 또는 창고 GUID) |
| 발주 입고 블록 상태 | 화이트리스트 — ~~SR 기준 AUTHORISED·빈 문자열(08-17)~~ → **PA 축 기준 AUTHORISED 만**(08-18 · 빈 문자열 예외는 PO-01128 이 SR 블록이라 근거 소멸) |
| 발주 목록 StockReceivedStatus | 상세 블록 상태와 **상관없음**(양방향 불일치) — 게이트로 쓰면 실재 입고가 문서째 사라진다(4건 12,552u) |
| 반품 목록 행 단위 | CN 단위가 아니라 **sale 단위** — 목록 상태값으로 거르면 승인 CN 유실 |
| 갱신 축 vs 이벤트 날짜 | **분리돼 있다** — 8월 갱신 문서가 4월 이벤트를 품는다. `since=` 가 스냅샷 경계 방어 |
